\documentclass[a4paper, 12pt]{article}
\usepackage[margin=2.5cm]{geometry}
%\usepackage{geometry}[2cm,2cm,2cm,2cm]
\usepackage[utf8]{inputenc}
\usepackage[nosumlimits]{amsmath}
\usepackage{amssymb,stmaryrd,amsthm,MnSymbol}
\usepackage{amsfonts,amssymb,amsopn}
 \usepackage[colorlinks=true,
            linkcolor=red,
            filecolor=red,
            citecolor=blue,
            urlcolor=blue,]{hyperref}
\usepackage[dvipsnames]{xcolor}
\usepackage{bbold}
\usepackage{phoenician}

\usepackage{comment}
\usepackage{graphicx}
\usepackage{mathtools}
\usepackage{multicol}
\usepackage{tikz, tikz-cd}
\usetikzlibrary{arrows.meta, decorations.pathreplacing, calligraphy, positioning, calc}
\usepackage{adjustbox}

\usepackage{aligned-overset}
\usepackage{enumerate}
\usepackage[ruled]{algorithm2e}
\usepackage{wrapfig}
\usepackage{placeins}

\usepackage{nicematrix}

\usepackage{caption, subcaption}
% for basis function plots
\newlength{\twosubht}
\newsavebox{\twosubbox}

\usepackage{natbib}
\let\OLDthebibliography\thebibliography
\renewcommand\thebibliography[1]{
  \OLDthebibliography{#1}
  \setlength{\parskip}{1pt}
  \setlength{\itemsep}{0pt plus 0.3ex}
}
\bibliographystyle{elsarticle-harv}

\usepackage{multirow}
\usepackage{booktabs}

%opening
\title{Hierarchical Subdivision $k$-Form Spaces on the Matrix Level}
\author{Robert Piel\footnote{University of Surrey, School of Mathematics and Physics, UK (r.piel@surrey.ac.uk), ORCID: \url{https://orcid.org/0009-0001-8681-2493}} \ and
Werner Bauer\footnote{University of Surrey, School of Mathematics and Physics, UK (w.bauer@surrey.ac.uk), ORCID: \url{https://orcid.org/0000-0002-5040-4287}}
}


\newcommand{\todo}[1]{\vspace{5 mm}\par \noindent
	\framebox{\begin{minipage}[c]{0.95 \textwidth}
			\tt #1 \end{minipage}}\vspace{5 mm}\par}
\newcommand{\werner}[1]{{\color{magenta}WB says: #1}}
\newcommand{\robert}[1]{{\color{orange} #1}}
\usepackage[normalem]{ulem}  % \sout for struck-through deletions (normalem keeps \emph italic)
\newcommand{\partitionStar}{\F\V_\C}                         % partition star of a vertex class
\newcommand{\partitionStarl}[1]{\F\V_{\C, #1}}
\newcommand{\partitionStarC}[1]{\F\V_{#1}}
\newcommand{\partitionStarCl}[2]{\F\V_{#1,#2}}
\newcommand{\vertclass}[1]{\widehat{v}_{#1}}                 % vertex equivalence class across levels
\newcommand{\ancestor}[1]{\big\lceil #1 \big\rceil_{\C}}     % coarsen a fine face to its covering active cell of \C
\newcommand{\quadriF}[1]{\mathcal{Q}^{f}_{#1}}                  % flat single-level quadrisection on partition #1
\newcommand{\quadriV}[2]{\mathcal{Q}^{\vertclass{} \to #2}_{#1}} % graded quadrisection of a star up to target level #2 on partition #1
\newcommand{\sourcelevel}{\varsigma}                            % coarsest occupied level of a partition star
\newcommand{\targetlevel}{\tau}                                 % marking target level
\newcommand{\Ltwoerror}{e_{L^2}}
\newcommand{\incl}[1]{{\color{red}#1}}
\newcommand{\checkit}[1]{{\color{red}#1}}
\newcommand{\RobertChanged}[1]{{\color{brown}#1}}
%\renewcommand{\checkit}[1]{{\color{red}#1}}
\newcommand{\tbd}[1]{{\color{red}#1}}
\newcommand{\highlight}[1]{{\color{cyan} #1}}
% narrative guidance (draft scaffolding): lime-coloured text that reminds the
% reader/author how a piece fits the overall story. Concrete reader-facing
% guidance is used bare as \guidance{...}; not-yet-final refactoring reminders
% are wrapped as \todo{\guidance{...}}. Switch lime!70!black to lime for pure lime.
\newcommand{\guidance}[1]{{\color{lime!70!black} #1}}

% colors
\newcommand{\blue}{\color{blue}}
\newcommand{\red}{\color{red}}
\newcommand{\green}{\color{ForestGreen}}
\newcommand{\brickred}{\color{BrickRed}}

% highlighting
\newcommand{\hlcyan}[1]{{\sethlcolor{cyan}\hl{#1}}}
\newcommand{\hlorange}[1]{{\sethlcolor{orange}\hl{#1}}}

% handling simplices
\newcommand{\T}{\mathcal{T}}
\newcommand{\K}{\mathcal{K}}
\newcommand{\V}{\mathcal{V}}
\newcommand{\E}{\mathcal{E}}
\newcommand{\F}{\mathcal{F}}

\newcommand{\simplexname}{\sigma}
\newcommand{\simplex}[3]{\freeQ{\simplexname}{#1}{#2}{#3}}
\newcommand{\simplexnametwo}{\kappa}
\newcommand{\simplextwo}[3]{\freeQ{\simplexnametwo}{#1}{#2}{#3}}
\newcommand{\simplexsubset}[2]{\freeVec{\Delta}{#1}{#2}}

\newcommand{\vertex}[2]{\freeQ{v}{}{#1}{#2}}
\newcommand{\vertexpos}[2]{\freeQ{\Vec{v}}{}{#1}{#2}}
\newcommand{\edge}[2]{\freeQ{e}{}{#1}{#2}}
\newcommand{\face}[2]{\freeQ{f}{}{#1}{#2}}
\newcommand{\barface}[2]{\overline{\face{#1}{#2}}}

% knob 1: raise the underbrace label toward the brace/equation (increase to pull closer)
\newlength{\ubraise}
\setlength{\ubraise}{2pt}
\newcommand{\ublabel}[1]{\smash{\raisebox{\ubraise}{$\scriptstyle #1$}}}

% knob 2: gap between the equation and the brace (default is 3pt; decrease to pull the brace up)
\newlength{\ubbracegap}
\setlength{\ubbracegap}{-2pt}
\makeatletter
\newcommand{\myunderbrace}[1]{%
  \mathop{\vtop{\m@th\ialign{##\crcr
      $\hfil\displaystyle{#1}\hfil$\crcr
      \noalign{\kern\ubbracegap\nointerlineskip}%
      \upbracefill\crcr\noalign{\kern3\p@}\crcr}}}\limits}
\makeatother

% adjacency relations
\newcommand{\KmKnl}[3]{\K^{#1}\K^{#2}_{#3}}
\newcommand{\VFl}[1]{\V\F_{#1}}
\newcommand{\EFl}[1]{\E\F_{#1}}
\newcommand{\FFl}[1]{\F\F_{#1}}
\newcommand{\VEl}[1]{\V\E_{#1}}
\newcommand{\EEl}[1]{\E\E_{#1}}
\newcommand{\FEl}[1]{\F\E_{#1}}
\newcommand{\VVl}[1]{\V\V_{#1}}
\newcommand{\EVl}[1]{\E\V_{#1}}
\newcommand{\FVl}[1]{\F\V_{#1}}

\newcommand{\KKltoL}[3]{\K^{#1}\K^{#1}_{#2 \to #3}}
\newcommand{\VVltoL}[2]{\V\V_{#1 \to #2}}
\newcommand{\EEltoL}[2]{\E\E_{#1 \to #2}}
\newcommand{\FFltoL}[2]{\F\F_{#1 \to #2}}

% indices for components
\newcommand{\coarseIdxone}{{\brickred i}}
\newcommand{\coarseIdxtwo}{{\brickred m}}
\newcommand{\fineIdxone}{{\brickred j}}
\newcommand{\fineIdxtwo}{{\brickred n}}

% indices for levels
\renewcommand{\l}{{\green\ell}}
\renewcommand{\L}{{\green L}}

\newcommand{\lzero}{{\green 0}}
\newcommand{\lone}{{\green {\l'}  }}
\newcommand{\ltwo}{{\green {\l''} }}
\newcommand{\lthree}{{\green {\l'''} }}

% numbers
\newcommand{\nc}{{\color{Magenta} n_c}}
\newcommand{\nr}{{\color{Magenta} n_r}}
\newcommand{\ns}{{\color{Magenta} n_s}}

% refinement stencils
\newcommand{\RSt}{\mathrm{R}}
\newcommand{\cRSt}{\RSt_{c}}
\newcommand{\R}{\mathcal{R}}

% derivative stencil
\newcommand{\DSt}{\mathrm{D}}

% macros that determine the general format
% constrained quantity
\newcommand{\constrQ}[5]{ ( {#1}^{#2}_{#3 \vert #4} )_{#5}} % e.g. \constr_vec{c}{k}{\ell}{L}{i} renders as (c^k_{l|L})_i
% constrained vector (no index i)
\newcommand{\constrVec}[4]{ {#1}^{#2}_{#3 \vert #4} }
% free quantity
\newcommand{\freeQ}[4]{ ( {#1}^{#2}_{#3} )_{#4}}
% free vector (no index i)
\newcommand{\freeVec}[3]{ {#1}^{#2}_{#3}}

% subdiv operators
\newcommand{\Sop}[3]{\mathcal{S}^{#1}_{{#2} \to {#3}}}
\newcommand{\Smat}[3]{\mathbf{\mathrm{S}}^{#1}_{#2 \to #3}}
\newcommand{\Smatij}[5]{\left(\mathbf{\mathrm{S}}^{#1}_{#2 \to #3}\right)_{#4 #5}}

% accumulated subdiv matrix
\newcommand{\Aop}[3]{\mathcal{A}^{#1}_{{#2} \to {#3}}}
\newcommand{\Amat}[3]{\mathbf{\mathrm{A}}^{#1}_{#2 \to #3}}
\newcommand{\Amatij}[5]{\left(\mathbf{\mathrm{A}}^{#1}_{#2 \to #3}\right)_{#4 #5}}

\newcommand{\primalAop}[4]{ {\mathcal{A}^{#1}_{{#2} \to {#3}|{#4} }}  }
\newcommand{\dualAop}[4]{ {\mathcal{B}^{#1}_{{#2}|\,{#3} \rightarrow {#4}} }}

% Whit and Loop, and their Aops
\newcommand{\Whit}{\mathrm{Whit}}
\newcommand{\Loop}{\mathrm{Loop}}
\newcommand{\AopWhit}[2]{\Aop{\Whit}{#1}{#2}}
\newcommand{\AopLoop}[2]{\Aop{\Loop}{#1}{#2}}

% derivative
\newcommand{\extd}{\mathbf{d}}
\newcommand{\auxextd}{\overset{\sim}{\vphantom{a}\smash{\mathbf{d}}}} % auxiliary derivative
\newcommand{\Dmat}[2]{\mathbf{D}^{#1}_{#2}} % derivative matrix
\newcommand{\Dmatij}[4]{\big(\mathbf{D}^{#1}_{#2}\big)_{#3 #4}} % derivative matrix with components

\newcommand{\dd}{\mathrm{d}_d} % discrete exterior derivative
\newcommand{\ddk}[1]{\mathrm{d}_d^{#1}}
\newcommand{\ddkl}[2]{\mathrm{d}_{d,#2}^{#1}}

% FEEC spaces
\newcommand{\FEECspace}[2]{\Lambda^{#1}_{#2}}
\newcommand{\FEECbasis}{\psi}
\newcommand{\FEECbases}{\Psi}
\newcommand{\FEECspaceZero}[2]{\mathring{\Lambda}^{#1}_{#2}}

% subdiv spaces
\newcommand{\SDFspace}[3]{\mathcal{S}\Lambda^{#1}_{#2 \vert #3}}
\newcommand{\SDFbasis}{\phi}
\newcommand{\SDFbases}{\Phi}
\newcommand{\SDFbasesZero}{\mathring{\SDFbases}}
\newcommand{\SDFspaceZero}[3]{\mathring{\mathcal{S}}\Lambda^{#1}_{#2 \vert #3}}

% operators for BCs
\newcommand{\trace}{\mathrm{tr}}
\newcommand{\Vint}{\mathring{\V}}
\newcommand{\Eint}{\mathring{\E}}
\newcommand{\Fint}{\mathring{\F}}
\newcommand{\Kint}{\mathring{\K}^k}
\newcommand{\BCAop}[3]{\mathring{\mathcal{A}} {}^{#1}_{#2 \to #3}}
\newcommand{\BCAmat}[3]{\mathring{\mathrm{A}} {}^{#1}_{#2 \to #3}}
\newcommand{\BCBEop}[4]{\mathring{\mathcal{B}} {}^{#1}_{#2 \vert #3 \to #4}}

% hierarchical spaces
\newcommand{\C}{\heth}
\newcommand{\I}{\mathcal{I}}
\newcommand{\II}{\mathbb{I}}
\newcommand{\calP}{\mathcal{P}}
\newcommand{\heth}{\textphnc{H}}

\newcommand{\activeDoFs}{\mathbb{A}}
\newcommand{\inactiveDoFs}{\mathbb{I}}
\newcommand{\D}{\activeDoFs}
\newcommand{\activeDoFSet}[2]{\activeDoFs^{#1}_{#2}}
\newcommand{\activeSDFspaces}[2]{\mathcal{S}\activeDoFSet{#1}{#2}}

\newcommand{\hierSDFspace}[3]{\mathcal{S}\activeDoFs^{#1}_{#2 \vert #3}}
\newcommand{\inactiveSDFspace}[3]{\mathcal{S}\inactiveDoFs^{#1}_{#2 \vert #3}}
\newcommand{\coarseInactiveSDFspace}[3]{\mathcal{S}\inactiveDoFs^{#1, \mathrm{c}}_{#2 \vert #3}}
\newcommand{\fineInactiveSDFspace}[3]{\mathcal{S}\inactiveDoFs^{#1, \mathrm{f}}_{#2 \vert #3}}

% general maths
\renewcommand{\powerset}{\mathbb{P}}
\newcommand{\inclusion}{\iota}
\newcommand{\auxincl}{\overset{\sim}{\vphantom{a}\smash{\iota}}} % auxiliary derivative
\newcommand{\support}{\mathrm{supp}}
\newcommand{\pfrac}[2]{\frac{\partial #1}{\partial #2}}
\newcommand{\M}{\mathcal{M}}
\newcommand{\vvert}[1]{\left\vert #1 \right\vert}
\newcommand{\dimlk}[2]{\big\vert \K^{#2}_{#1} \big\vert}
\newcommand{\sumdimlk}[2]{\vert \K^{#2}_{#1} \vert}
\newcommand{\closure}[1]{\overset{\circ}{#1} {}}
\renewcommand{\complement}{\mathsf{c}}

% widecheck and widehat
\DeclareFontFamily{U}{mathx}{}
\DeclareFontShape{U}{mathx}{m}{n}{<-> mathx10}{}
\DeclareSymbolFont{mathx}{U}{mathx}{m}{n}
\DeclareMathAccent{\widehat}{0}{mathx}{"70}
\DeclareMathAccent{\widecheck}{0}{mathx}{"71}

% macros for specific spaces
\newcommand{\CG}{\mathrm{CG}}
\newcommand{\NED}{\mathrm{NED}}
\newcommand{\DG}{\mathrm{DG}}
\newcommand{\Hcurl}{H(\text{curl})}
\newcommand{\HcurlM}[1]{H\left(\text{curl}, #1\right)}
\newcommand{\HocurlM}[1]{\mathring{H}\left(\text{curl}, #1\right)}

%%% to be removed %%%
\newcommand{\rmA}{A}

%% maths environments
\newtheorem{theorem}{Theorem}[section]
\newtheorem{definition}[theorem]{Definition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{example}[theorem]{Example}
\newtheorem{remark}[theorem]{Remark}
\newtheorem{assumption}[theorem]{Assumption}
\newtheorem{numberedproof}[theorem]{Proof}


%% adjust equation numbering to IMA style
\numberwithin{equation}{section}
\numberwithin{algocf}{section}

\begin{document}

\maketitle

\vspace{-2.0em}

\begin{abstract}
We provide the algorithmic and computational complement to the theoretical framework of~\cite{Piel.2026} for hierarchical subdivision $k$-form spaces on triangular meshes. The central observation is that every basis function is represented by a column of the accumulated subdivision matrix. Selecting active degrees of freedom results in collecting their corresponding columns into the hierarchical subdivision matrix. This sparse matrix reduces all downstream operations like matrix assembly or residual evaluation to standard sparse linear algebra on the finest mesh $\T_\L$ without recomputing any integrals. We give explicit algorithms for constructing the active degrees of freedom from a subdivision partition, including a frontier-sweep procedure for the interface basis functions at level boundaries.

For the adaptive solution of PDEs, we describe a \textsc{Solve}--\textsc{Estimate}--\textsc{Mark}--\textsc{Refine}--\textsc{Pad} loop in which the non-standard \textsc{Pad} step enforces the subdivision partition condition after Dörfler marking \robert{by alternating pinch resolution, inadmissible-faces padding, and grading to a fixed point, refining the coarse regions that would otherwise break de Rham exactness}. Numerical experiments on the Maxwell eigenvalue problem confirm that exact subdivision partitions reproduce the reference spectrum without spurious modes and adaptive experiments on the mixed vector Laplace equation demonstrate \robert{our} adaptive loop.
\end{abstract}

\todo{\guidance{Scoping (narrative point~1). Weave one bridging clause into the abstract so its arc matches the paper: after ``\ldots\ reduces all downstream operations to standard sparse linear algebra'', add that \emph{the substantive question is then purely combinatorial --- which column selections are admissible --- which we answer via the subdivision-partition condition and enforce during adaptivity with the} \textsc{Pad} \emph{step}. This signals that the matrix picture is the setup and the admissibility/padding machinery is the body of the work.}}


\tableofcontents

\section{Introduction}

The finite element exterior calculus (FEEC)~\cite{Arnold.FEEC} provides a systematic framework for constructing stable, structure-preserving discretisations of partial differential equations (PDEs) involving differential forms. Its central structure is the discrete de Rham complex: a sequence of finite-dimensional function spaces connected by the exterior derivative $\extd$. Exactness of this complex is the key algebraic property that prevents spurious modes in Maxwell eigenvalue problems, guarantees inf-sup stability in mixed formulations, and underpins the commuting diagram that makes error analysis tractable. Classical realisations of the FEEC use the lowest-order Whitney forms, also known as the continuous Galerkin ($\mathrm{CG}_1$), N\'ed\'elec ($\mathrm{NED}_1$), and discontinuous ($\mathrm{DG}_0$) spaces.

Subdivision algorithms, originally developed for geometric modelling, produce limit functions that are globally smooth on arbitrary triangular meshes. For example, Loop's scheme~\citep{Loop.1987} generates globally $\mathcal{C}^1$ scalar limit functions. Wang~\citep{Wang.2006,Wang.2008} introduce subdivision schemes for $1$- and $2$-forms that commute with the discrete exterior derivative from Discrete Exterior Calculus (DEC, \cite{Hirani.2003,Desbrun.2006}). These subdivision schemes give rise to subdivision $k$-form spaces in the DEC sense \citep{Goes.2015}. \cite{Piel.2026} translate their work to finite elements and introduce a framework for subdivision-based finite elements that form discrete de Rham complexes on meshes with uniform refinement.

Such uniform subdivision spaces assign the same resolution everywhere: every basis function has a support whose size is determined by the coarse mesh $\T_\lzero$. This is adequate for globally smooth solutions but wasteful when the PDE solution is rough only in a small part of the domain. In isogeometric analysis, the analogous limitation of globally refined B-spline spaces has been addressed through hierarchical B-spline spaces~\citep{Evans.2020}, which mix basis functions from different refinement levels to obtain spatially varying resolution while retaining linear independence. Extending this idea to spaces of differential forms is non-trivial: activating basis functions at different levels can break the exactness of the de Rham complex unless the selection is governed by a compatible combinatorial rule.
The analogous construction for B-spline de Rham complexes has been studied in~\cite{Buffa2011,Evans.2020,Shepherd.2024,Dijkstra.2025}, where admissibility conditions on the hierarchical mesh ensure exactness of the complex.
A central mechanism in these works is a \emph{completion step} that, after marking basis functions for refinement, enforces a compatibility condition on the active set: in~\cite{Cabanas.2026} this takes the form of \emph{$L$-chain completion}, which forces all basis functions in a connecting $L$-chain to be refined simultaneously whenever a conflicting pair is detected.
\checkit{The subdivision \textsc{Pad} step described in Section~4 serves an analogous role (restoring the subdivision partition condition after marking), but the precise correspondence between $L$-chain completion and the partition condition, and whether one implies the other, remains to be investigated.}

This paper provides the matrix-level description and algorithms for hierarchical subdivision $k$-form spaces that preserve the de Rham complex, serving as the practical and computational complement to~\cite{Piel.2026}. The key computational objects are the subdivision matrices $\Amat{k}{\l}{\L}$, which represent level-$\l$ basis functions in the Whintney basis of the fine assembly mesh $\T_\L$. A hierarchical space is formed by selecting one column of these matrices per active basis function, according to a \emph{subdivision partition} of $\T_\L$, a combinatorial condition on the patch decomposition of the domain. This condition, characterised in~\cite{Piel.2026}, simultaneously guarantees linear independence and exactness of the complex. Once the hierarchical subdivision matrix $\Amat{k}{\activeDoFs}{\L}$ is assembled, all downstream operations (exterior derivative, mass and stiffness matrices, residual evaluation) reduce to standard sparse linear algebra on $\T_\L$ without recomputing any integrals.

\paragraph*{Contributions.} This paper makes the following contributions:
\begin{itemize}
    \item A matrix-level construction of hierarchical subdivision $k$-form spaces and a practical algorithm for assembling the active degrees of freedom and the hierarchical subdivision matrix (Sections~3 and~4).
    \item An adaptive refinement loop \textsc{Solve}--\textsc{Estimate}--\textsc{Mark}--\textsc{Pad}--\textsc{Refine}, including a non-standard padding step that restores the subdivision partition condition after marking (Section~4).
    \item Numerical verification via the Maxwell eigenvalue problem, confirming that the exact de Rham complex is preserved, and adaptive convergence experiments for the vector Laplace equation with multi-scale solutions (Section~5).
\end{itemize}

\todo{\guidance{Scoping (narrative point~1). Re-split the first bullet so the two layers are billed honestly instead of the combinatorial machinery hiding inside ``a practical algorithm'': (i) the \emph{matrix representation} that reduces the hierarchical space to a column selection from $\{\Amat{k}{\l}{\L}\}_\l$ (Section~2), and (ii) the \emph{combinatorial admissibility theory plus the} \textsc{Pad} \emph{repair algorithm} that governs which selections are legal and restores legality during adaptivity (Sections~3--4). Admissibility/padding is most of the paper and should headline as its own contribution.}}

\section{Subdivision $k$-form spaces: from uniform to hierarchical}

\subsection{Subdivision basis functions}

This section collects the most important properties of the uniform subdivision $k$-form spaces introduced in~\cite{Piel.2026}. We focus on the aspects that enable the construction of hierarchical subdivision $k$-form spaces in a similar way as~\cite{Evans.2020} did for B-splines.

Starting from an initial coarse triangular mesh $\T_\l$ at level $\l = \lzero$, the \emph{subdivision algorithm} produces a sequence of increasingly fine meshes $\T_{\lzero+1}, \ldots, \T_\L$ by repeatedly applying two steps: a \emph{topological refinement} that splits every triangle into four, and a \emph{geometric averaging} step that repositions vertices as weighted averages of neighbouring coarse vertices. We use Loop's subdivision scheme~\cite{Loop.1987} throughout. The resulting \emph{mesh hierarchy} $\{\T_\l\}_{\l=\lzero}^{\L}$ ranges from the coarsest level $\lzero$ to the finest, the \emph{assembly mesh} $\T_\L$. At each level $\l$, we denote the sets of vertices, edges, and faces by $\K^0_\l = \V_\l$, $\K^1_\l = \E_\l$, and $\K^2_\l = \F_\l$, with cardinalities $|\K^k_\l|$.

On each mesh $\T_\l$, the lowest-order Whitney $k$-form spaces are the standard FEEC spaces
\begin{equation}
    \FEECspace{0}{\l} = \mathrm{CG}_1(\T_\l), \qquad \FEECspace{1}{\l} = \mathrm{RT}_0(\T_\l), \qquad \FEECspace{2}{\l} = \mathrm{DG}_0(\T_\l),
\end{equation}
with basis functions $\freeQ{\FEECbasis}{k}{\l}{\coarseIdxone}$ indexed by the $k$-simplices $\simplex{k}{\l}{\coarseIdxone} \in \K^k_\l$.

The subdivision algorithm induces a linear \emph{subdivision operator} $\Sop{k}{\l}{\l+1}: \FEECspace{k}{\l} \to \FEECspace{k}{\l+1}$ between Whitney spaces on consecutive levels. Composing these one-step operators gives the \emph{accumulated subdivision operator}
\begin{equation}
    \Aop{k}{\l}{\L} \coloneqq \Sop{k}{\L-1}{\L} \circ \cdots \circ \Sop{k}{\l}{\l+1},
\end{equation}
whose coordinate representation with respect to the Whitney bases of $\FEECspace{k}{\l}$ and $\FEECspace{k}{\L}$ is the \emph{subdivision matrix} $\Amat{k}{\l}{\L}$ of size $|\K^k_\L| \times |\K^k_\l|$. Column $\coarseIdxone$ of $\Amat{k}{\l}{\L}$ is the coordinate vector of the image of $\freeQ{\FEECbasis}{k}{\l}{\coarseIdxone}$ in the fine Whitney basis of $\FEECspace{k}{\L}$.

The \emph{subdivision $k$-form space} on level $\l$ is defined as the image of $\FEECspace{k}{\l}$ under $\Aop{k}{\l}{\L}$,
\begin{equation}
    \SDFspace{k}{\l}{\L}\big(\T_\l\big) \coloneqq \Aop{k}{\l}{\L}\!\big(\FEECspace{k}{\l}\big),
\end{equation}
spanned by the \emph{subdivision basis functions} $\constrQ{\SDFbasis}{k}{\l}{\L}{\coarseIdxone} \coloneqq \Aop{k}{\l}{\L}\, \freeQ{\FEECbasis}{k}{\l}{\coarseIdxone}$, i.e.\ the columns of $\Amat{k}{\l}{\L}$ expressed in the fine Whitney basis. At the finest level, $\SDFspace{k}{\L}{\L} = \FEECspace{k}{\L}$ by definition. The key properties of the spaces $\SDFspace{k}{\l}{\L}$ were established in~\cite{Piel.2026} and will be recalled in Section~\ref{sec:construction_of_hierarchical_subdiv_spaces}.

\subsection{Computational representations}
\label{subsec:computational_representations}

A $k$-form $\constrVec{\omega}{k}{\l}{\L} \in \SDFspace{k}{\l}{\L}$ can be written as
\begin{equation}
    \constrVec{\omega}{k}{\l}{\L} = \sum\limits_{\coarseIdxone = 1}^{\sumdimlk{\l}{k}} \freeQ{c}{k}{\l}{\coarseIdxone} \constrQ{\SDFbasis}{k}{\l}{\L}{\coarseIdxone} = \sum\limits_{i = 1}^{\sumdimlk{\l}{k}} \freeQ{c}{k}{\l}{\coarseIdxone} \Big( \sum\limits_{\fineIdxone = 1}^{\sumdimlk{\L}{k}} \Amatij{k}{\l}{\L}{\fineIdxone}{\coarseIdxone} \freeQ{\FEECbasis}{k}{\L}{\fineIdxone} \Big).
\end{equation}
We can vectorise this equation by introducing the vectors
\begin{equation}
    \freeVec{\bar{c}}{k}{\l} \coloneqq \begin{bmatrix}
        \freeQ{c}{k}{\l}{1} \\
        \freeQ{c}{k}{\l}{2} \\
        \vdots \\
        \freeQ{c}{k}{\l}{\sumdimlk{\l}{k}}
    \end{bmatrix}, \qquad 
    \constrVec{\SDFbases}{k}{\l}{\L} \coloneqq \begin{bmatrix}
        \constrQ{\SDFbasis}{k}{\l}{\L}{1} \\
        \constrQ{\SDFbasis}{k}{\l}{\L}{2} \\
        \vdots \\
        \constrQ{\SDFbasis}{k}{\l}{\L}{\sumdimlk{\l}{k}}
    \end{bmatrix}, \qquad 
    \freeVec{\FEECbases}{k}{\L} \coloneqq \begin{bmatrix}
        \freeQ{\FEECbasis}{k}{\L}{1} \\
        \freeQ{\FEECbasis}{k}{\L}{2} \\
        \vdots \\
        \freeQ{\FEECbasis}{k}{\L}{\sumdimlk{\L}{k}}
    \end{bmatrix}.
\end{equation}
We can then re-write $\constrVec{\omega}{k}{\l}{\L} \in \SDFspace{k}{\l}{\L}$ as
\begin{equation}
    \constrVec{\omega}{k}{\l}{\L} = \big( \constrVec{\SDFbases}{k}{\l}{\L} \big)^T \cdot \freeVec{\bar{c}}{k}{\l} = \big( \freeVec{\FEECbases}{k}{\L} \big)^T \cdot \Amat{k}{\l}{\L} \cdot \freeVec{\bar{c}}{k}{\l}.
\end{equation}
This implies
\begin{equation}
    \big( \freeVec{\FEECbases}{k}{\L} \big)^T \cdot \Amat{k}{\l}{\L} = \big( \constrVec{\SDFbases}{k}{\l}{\L} \big)^T = \begin{bmatrix}
        \constrQ{\SDFbasis}{k}{\l}{\L}{1} & \constrQ{\SDFbasis}{k}{\l}{\L}{2} & \dots & \constrQ{\SDFbasis}{k}{\l}{\L}{\sumdimlk{\l}{k}}
    \end{bmatrix}
\end{equation}
and thus shows that the columns of the subdivision matrix $\Amat{k}{\l}{\L}$ contain the coefficients of the subdivision basis functions $\constrQ{\SDFbasis}{k}{\l}{\L}{\coarseIdxone}$ in the fine FEEC space $\FEECspace{k}{\L}$.

Uniform subdivision spaces $\SDFspace{k}{\lone}{\L}$ resolve the domain at a single global scale, which is wasteful when the PDE solution contains localised features. Hierarchical spaces break this uniformity by mixing basis functions from \emph{different} levels $\lone$: as illustrated in Fig.~\ref{fig:hier_basis_funs}, a level-$0$ function (left) has broad support capturing large-scale variation, while a level-$2$ function (right) has a much smaller support and can resolve local detail. Selecting which levels to activate in which part of the domain yields an approximation space whose resolution adapts spatially to the solution.

In the uniform space $\SDFspace{k}{\l}{\L}$, every $k$-simplex of $\T_\l$ contributes a basis function, so $\activeDoFSet{k}{\l} = \K^k_\l$. The hierarchical space relaxes this: it selects $\activeDoFSet{k}{\l} \subset \K^k_\l$ at each level, mixing coarse basis functions (broad support, large-scale variation) and fine basis functions (small support, local detail) to adapt spatial resolution to the solution. Not every choice of $\activeDoFSet{k}{\l}$ yields a well-behaved space. For example, the active basis functions may fail to be linearly independent, or the spaces may fail to constitute an exact differential complex. Section~\ref{sec:construction_of_hierarchical_subdiv_spaces} develops the theory of admissible choices.

The same representation extends to the hierarchical space $\hierSDFspace{k}{\C}{\L}$. A form $\constrVec{\omega}{k}{\activeDoFs}{\L} \in \hierSDFspace{k}{\C}{\L}$ is a linear combination of active basis functions drawn from all levels,
\begin{equation}
    \label{eq:hierarchical_form_sum}
    \constrVec{\omega}{k}{\activeDoFs}{\L}
    = \sum\limits_{\l=\lzero}^{\L} \; \sum\limits_{\simplex{k}{\l}{\coarseIdxone} \in \activeDoFSet{k}{\l}}
        \freeQ{c}{k}{\l}{\coarseIdxone} \, \constrQ{\SDFbasis}{k}{\l}{\L}{\coarseIdxone}.
\end{equation}
The active DoFs use the same simplex indices $\coarseIdxone$ as in the uniform space $\SDFspace{k}{\l}{\L}$, but restricted to $\activeDoFSet{k}{\l} \subset \K^k_\l$. To form explicit vectors we enumerate the active simplices on level $\l$ in increasing index order as $\simplex{k}{\l}{\coarseIdxone_1}, \ldots, \simplex{k}{\l}{\coarseIdxone_{|\activeDoFSet{k}{\l}|}}$ with $\coarseIdxone_1 < \cdots < \coarseIdxone_{|\activeDoFSet{k}{\l}|}$. The concatenated basis-function and coefficient vectors, grouped by level with blocks separated by vertical bars, are
\begin{equation}
    \label{eq:hierarchical_dof_vector}
    \big(\freeVec{\bar{c}}{k}{\activeDoFs}\big)^T
    = \Bigl[\;
        \underbrace{
            \freeQ{c}{k}{0}{\coarseIdxone_1}
            \;\; \cdots \;\;
            \freeQ{c}{k}{0}{\coarseIdxone_{|\activeDoFSet{k}{0}|}}
        }_{\displaystyle (\freeVec{\bar{c}}{k}{0})^T}
        \;\Big|\;
        \underbrace{
            \freeQ{c}{k}{1}{\coarseIdxone_1}
            \;\; \cdots \;\;
            \freeQ{c}{k}{1}{\coarseIdxone_{|\activeDoFSet{k}{1}|}}
        }_{\displaystyle (\freeVec{\bar{c}}{k}{1})^T}
        \;\Big|\;
        \cdots
        \;\Big|\;
        \underbrace{
            \freeQ{c}{k}{\L}{\coarseIdxone_1}
            \;\; \cdots \;\;
            \freeQ{c}{k}{\L}{\coarseIdxone_{|\activeDoFSet{k}{\L}|}}
        }_{\displaystyle (\freeVec{\bar{c}}{k}{\L})^T}
    \;\Bigr],
\end{equation}
\begin{equation}
    \label{eq:hierarchical_basis_vector}
    \big(\constrVec{\SDFbases}{k}{\activeDoFs}{\L}\big)^T
    = \Bigl[\;
        \underbrace{
            \constrQ{\SDFbasis}{k}{0}{\L}{\coarseIdxone_1}
            \;\cdots\;
            \constrQ{\SDFbasis}{k}{0}{\L}{\coarseIdxone_{|\activeDoFSet{k}{0}|}}
        }_{\activeDoFSet{k}{0}}
        \;\Big|\;
        \underbrace{
            \constrQ{\SDFbasis}{k}{1}{\L}{\coarseIdxone_1}
            \;\cdots\;
            \constrQ{\SDFbasis}{k}{1}{\L}{\coarseIdxone_{|\activeDoFSet{k}{1}|}}
        }_{\activeDoFSet{k}{1}}
        \;\Big|\;
        \cdots
        \;\Big|\;
        \underbrace{
            \constrQ{\SDFbasis}{k}{\L}{\L}{\coarseIdxone_1}
            \;\cdots\;
            \constrQ{\SDFbasis}{k}{\L}{\L}{\coarseIdxone_{|\activeDoFSet{k}{\L}|}}
        }_{\activeDoFSet{k}{\L}}
    \;\Bigr].
\end{equation}
With the active subdivision basis $\constrVec{\SDFbases}{k}{\activeDoFs}{\L}$ chosen and the fine Whitney basis $\freeVec{\FEECbases}{k}{\L}$ fixed, the form $\constrVec{\omega}{k}{\activeDoFs}{\L}$ admits two representations that must agree. Equating them and comparing coefficients in $\FEECspace{k}{\L}$ defines the hierarchical subdivision matrix $\Amat{k}{\activeDoFs}{\L}$:
\begin{equation}
    \label{eq:hierarchical_form_compact}
    \constrVec{\omega}{k}{\activeDoFs}{\L}
    = \big(\constrVec{\SDFbases}{k}{\activeDoFs}{\L}\big)^T \cdot \freeVec{\bar{c}}{k}{\activeDoFs}
    = \big(\freeVec{\FEECbases}{k}{\L}\big)^T \cdot \Amat{k}{\activeDoFs}{\L} \cdot \freeVec{\bar{c}}{k}{\activeDoFs}.
\end{equation}
This implies $\big(\freeVec{\FEECbases}{k}{\L}\big)^T \cdot \Amat{k}{\activeDoFs}{\L} = \big(\constrVec{\SDFbases}{k}{\activeDoFs}{\L}\big)^T$, showing that the columns of $\Amat{k}{\activeDoFs}{\L}$ are the coefficients of the active subdivision basis functions in $\FEECspace{k}{\L}$. The level grouping of $\freeVec{\bar{c}}{k}{\activeDoFs}$ mirrors the block column structure of $\Amat{k}{\activeDoFs}{\L}$:
\begin{equation}
    \label{eq:hierarchical_matrix_blocks}
    \Amat{k}{\activeDoFs}{\L}
    =
    \Bigl[
        \underbrace{\bigl(\Amat{k}{0}{\L}\bigr)_{\activeDoFSet{k}{0}}}_{\mathbb{R}^{|\K^k_\L| \times |\activeDoFSet{k}{0}|}}
        \;\Big|\;
        \underbrace{\bigl(\Amat{k}{1}{\L}\bigr)_{\activeDoFSet{k}{1}}}_{\mathbb{R}^{|\K^k_\L| \times |\activeDoFSet{k}{1}|}}
        \;\Big|\;
        \cdots
        \;\Big|\;
        \underbrace{\bigl(\Amat{k}{\L}{\L}\bigr)_{\activeDoFSet{k}{\L}}}_{\mathbb{R}^{|\K^k_\L| \times |\activeDoFSet{k}{\L}|}}
    \Bigr],
\end{equation}
so that the block $(\freeVec{\bar{c}}{k}{\l})^T$ multiplies precisely the $|\activeDoFSet{k}{\l}|$ columns of $\Amat{k}{\activeDoFs}{\L}$ originating from level $\l$, contributing $\sum_{\simplex{k}{\l}{\coarseIdxone} \in \activeDoFSet{k}{\l}} \freeQ{c}{k}{\l}{\coarseIdxone} \, \constrQ{\SDFbasis}{k}{\l}{\L}{\coarseIdxone}$ to $\constrVec{\omega}{k}{\activeDoFs}{\L}$.

In practice it is the $\big(\freeVec{\FEECbases}{k}{\L}\big)^T \cdot \Amat{k}{\activeDoFs}{\L}$ picture that drives the implementation, rather than the $\big(\constrVec{\SDFbases}{k}{\activeDoFs}{\L}\big)^T$ picture. A bilinear form with fine-level matrix $\mathbf{M}$ on $\FEECspace{k}{\L}$ restricts to the hierarchical space as $(\Amat{k}{\activeDoFs}{\L})^T \mathbf{M}\, \Amat{k}{\activeDoFs}{\L}$, so the discrete system is assembled in $\FEECspace{k}{\L}$ via standard FEniCS routines and compressed by two sparse matrix multiplications. The fine Whitney basis $\freeVec{\FEECbases}{k}{\L}$ is handled implicitly by FEniCS and never appears explicitly in code.

The PDE is then solved for the hierarchical DoF vector $\freeVec{\bar{c}}{k}{\activeDoFs}$, which collects the coefficients of the solution in the active subdivision basis. Once $\freeVec{\bar{c}}{k}{\activeDoFs}$ is known, the fine-level Whitney representation of the solution is recovered as $\Amat{k}{\activeDoFs}{\L} \cdot \freeVec{\bar{c}}{k}{\activeDoFs}$. This matrix--vector product is the coordinate expression of the inclusion $\iota \colon \hierSDFspace{k}{\C}{\L} \hookrightarrow \FEECspace{k}{\L}$, and the resulting fine-level vector can be used directly for post-processing, visualisation, and residual estimation on $\T_\L$.

The concrete task that remains is therefore constructing $\Amat{k}{\activeDoFs}{\L}$, which reduces to choosing the active DoFs $\activeDoFSet{k}{\l}$. The remainder of this paper focuses on defining $\activeDoFSet{k}{\l}$ such that the resulting hierarchical spaces are linearly independent and preserve a discrete de Rham complex, and on assembling $\Amat{k}{\activeDoFs}{\L}$ from those choices.

\guidance{From here the matrix picture is fixed. The object $\Amat{k}{\activeDoFs}{\L}$ is always the same kind of thing --- a selection of columns from the hierarchy $\{\Amat{k}{\l}{\L}\}_\l$ --- and every downstream operation is the generic Galerkin restriction of Section~\ref{subsec:computational_representations}, \emph{independent} of which columns are chosen. The entire remaining question is therefore combinatorial: \emph{which} column selections (equivalently, which subdivision partitions) yield a linearly independent basis and a de Rham complex. The rest of the paper characterises the admissible selections (Section~\ref{sec:construction_of_hierarchical_subdiv_spaces}), assembles and adapts them (Section~4), and verifies the resulting exactness (Section~5).}

\section{Constructing hierarchical subdivision spaces}
\label{sec:construction_of_hierarchical_subdiv_spaces}

\guidance{The matrix representation of Section~\ref{subsec:computational_representations} is unchanged throughout this section; here we determine \emph{which} column selections are admissible. The two tools below --- refinement stencils and derivative stencils --- are the visual language for the two admissibility conditions (linear independence and closure of the complex), which the subdivision partition of Section~\ref{subsec:selecting_active_dofs} then enforces geometrically.}

\subsection{Refinement of basis functions}

\begin{proposition}[Prop.~NUM from~\cite{Piel.2026}]
\label{prop:uniform_spaces_nested}
Let $\lzero \leq \l \leq \L$. For a fixed initial meshes $\T_{\l}$ for $\l \in \{\lzero, \hdots, \L\}$, the
corresponding subdivision $k$-form spaces $\SDFspace{k}{\l}{\L}\big(\T_{\l} \big)$ are nested, i.e.
\begin{equation}
    \label{eq:nested_spaces}
    \SDFspace{k}{\l}{\L} \subset  \SDFspace{k}{\l+1}{\L} \subset \hdots \subset \SDFspace{k}{\L}{\L} = \FEECspace{k}{\L},
\end{equation}
and have the dimensions $\dimlk{\l}{k} < \dimlk{\l +1}{k} < \hdots < \dimlk{\L}{k}$.
\end{proposition}

This proposition implies the refinement relation for the subdivision basis functions $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$. The resulting properties regarding the refinability of the basis functions form the backbone of the adaptive method presented in this paper. The following discussion is focused on basis functions on intermediate levels $\lone$ with $\lzero \leq \lone \leq \L$ defined as
\begin{equation}
    \label{equ:basisrel}
    \constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone} = \Aop{k}{\lone}{\L} \freeQ{\FEECbasis}{k}{\lone}{\coarseIdxone} = \sum_{\fineIdxone = 1}^{\sumdimlk{\lone}{k}} \Amatij{k}{\lone}{\L}{\fineIdxone}{\coarseIdxone} \; \freeQ{\FEECbasis}{k}{\L}{\fineIdxone} \, ,
\end{equation}
where $\Amat{k}{\lone}{\L}$ is the subdivision matrix, cf.~\cite{Piel.2026}. Splitting the accumulated subdivision operator $\Aop{k}{\l}{\L}$ reveals the following relation between the basis functions $\constrVec{\Phi}{k}{\lone}{\L}$ and $\constrVec{\Phi}{k}{\lone + 1}{\L}$ on neighbouring levels.

\begin{lemma}[Refinement Relation, {\cite{Piel.2026}}]
    \label{lemma:hiermeshrel}
    Let $\lone$ be an arbitrary level such that $\lzero \leq \lone \leq \L$ and let $\Amat{k}{\l}{\l+1}$ be the subdivision matrix associated to $\Aop{k}{\lone}{\lone+1}$. Then, the basis functions $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$ and $\constrQ{\SDFbasis}{k}{\lone+1}{\L}{\fineIdxone}$ satisfy the following \emph{refinement relation}
    \begin{equation}
        \label{eq:refinement_relation}
        \constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone} = \sum\limits_{\fineIdxone = 1}^{\sumdimlk{\lone+1}{k}} \Amatij{k}{\lone}{\lone+1}{\fineIdxone}{\coarseIdxone} \; \constrQ{\SDFbasis}{k}{\lone+1}{\L}{\fineIdxone} \; .
    \end{equation}
\end{lemma}
\begin{proof}
    See~\cite{Piel.2026}.
\end{proof}

We will capture relationships between different basis functions across the mesh hierarchy through \textit{stencils}. Since there is a one-to-one correspondence between basis functions and simplices, we will formulate these stencils as maps between (sets of) simplices across the hierarchy. Thus, formally, a stencil is a map $\powerset(\K^k_{\lone}) \to \powerset\!\bigl(\bigsqcup_{\l=\lzero}^{\L}\bigsqcup_{k=0}^{2}\K^k_\l\bigr)$, where $\powerset$ denotes the powerset.

We choose to formulate stencils with respect to simplices instead of basis functions because that enables us to use the adjacency operator $\KmKnl{m}{n}{\lone}$ and refinement operators $\KKltoL{k}{\lone}{\ltwo}$ introduced in~\cite{Piel.2026}. The remainder of this section sets up refinement stencils based on the refinement relation~\ref{eq:refinement_relation}, while the next section lays the foundations for the derivative stencil by investigating the relation of basis functions of different form degrees.

The hierarchical refinement relation in Lemma~\ref{lemma:hiermeshrel} provides a way to re-express a basis function $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$ as a linear combination of finer basis functions. This idea of expressing coarse basis functions through finer basis functions is a core concept in this work. The following definition introduces the corresponding formalism called refinement stencils. After that, we highlight one particular known refinement stencil and then present a construction for more general refinement stencils.

\begin{definition}[Refinement Stencil]
    \label{def:deep_ref_stencil}
    Let $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$ denote the basis function of $\SDFspace{k}{\lone}{\L}$ that is associated to the simplex $\simplex{k}{\lone}{\coarseIdxone}$.
    A refinement stencil is a collection of simplices $\simplex{k}{\ltwo}{\fineIdxone}$ across the finer levels $\ltwo \in \{\lone\!+\!1, \, \hdots, \L\}$ whose associated basis functions $\constrQ{\SDFbasis}{k}{\ltwo}{\L}{\fineIdxone}$ can exactly re-express $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$. More concretely, a refinement stencil $\RSt\big(\simplex{k}{\lone}{\coarseIdxone}\big)$ is a map
    \begin{equation}
        \label{equ:refstencilunion}
        \RSt : \K^k_{\lone} \to \powerset \Big(\bigcup_{\ltwo = \lone+1}^{\L} \K^k_{\ltwo} \Big), \quad  \simplex{k}{\lone}{\coarseIdxone} \mapsto \RSt \big( \simplex{k}{\lone}{\coarseIdxone} \big)
    \end{equation}
    such that there exist coefficients $\constrQ{c}{k}{\ltwo}{\lone}{\fineIdxone} \neq 0$ satisfying
    \begin{equation}
        \constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone} = \sum_{\simplex{k}{\ltwo}{\fineIdxone} \in \RSt \big( \simplex{k}{\lone}{\coarseIdxone} \big)} \constrQ{c}{k}{\robert{\ltwo}}{\robert{\lone}}{\fineIdxone} \; \constrQ{\SDFbasis}{k}{\ltwo}{\L}{\fineIdxone}.
    \end{equation}
\end{definition}

\todo{\guidance{Trim (narrative point~2). Corollary~\ref{cor:refinement_support} below and the ``uniqueness is lost'' example (Eq.~\eqref{eq:example_ref_stencil} and its $n$-alternatives discussion) are formal weight with no payoff here --- neither is used by any algorithm or by the admissibility statement, and they are what make this subsection read as a dead-end ladder. Cut both; keep the Refinement Relation (Lemma~\ref{lemma:hiermeshrel}), the canonical refinement stencil, and the figure.}}

The coefficients $\constrQ{c}{k}{\ltwo}{\lone}{\fineIdxone}$ above are connected to the non-zero entries of the subdivision matrices. In fact, we can compute them by tracing back the contributions of certain fine basis functions to the coarse one analogously to the refinement relation~(\ref{eq:refinement_relation}). The only difference is that the refinement stencil $\RSt \big( \simplex{k}{\lone}{\coarseIdxone} \big)$ now contains simplices across different refinement levels, i.e. $\ltwo$ is not constant for $\simplex{k}{\ltwo}{\fineIdxone}$. We are only concerned with the existence of the coefficients $\constrQ{c}{k}{\ltwo}{\lone}{\fineIdxone}$ because our goal is typically not to exactly re-express the coarse basis functions but to test wether they are in the space spanned by some finer basis functions. That is why we are going to formulate indicators for such linear dependence of coarse basis functions based on the existence of refinement stencils.

In the following, we will systematically construct refinement stencils by iterating the canonical refinement stencil defined next. According to eq.~\ref{eq:refinement_relation}, all basis functions $\constrQ{\SDFbasis}{k}{\lone+1}{\L}{\fineIdxone}$ with non-zero entries $\Amatij{k}{\lone}{\lone+1}{\fineIdxone}{\coarseIdxone} \neq 0$ are required to re-express the coarse basis function $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$. The following definition formalises this selection of basis functions.

\begin{definition}[\robert{Canonical} Refinement Stencil]
    Let $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$ denote the basis function of $\SDFspace{k}{\lone}{\L}$ that is associated to the simplex $\simplex{k}{\lone}{\coarseIdxone}$. The canonical refinement $\cRSt\big( \simplex{k}{\lone}{\coarseIdxone}\big)$ of $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$ is the map
    \begin{eqnarray}
        \cRSt: \K^k_{\lone} \to \powerset\big( \K^k_{\lone+1}\big), \qquad \simplex{k}{\lone}{\coarseIdxone} \mapsto \Big\{ \simplex{k}{\lone+1}{\fineIdxone} \in \K^k_{\lone+1} \colon \; \Amatij{k}{\lone}{\lone+1}{\fineIdxone}{\coarseIdxone} \neq 0 \Big\}.
    \end{eqnarray}
\end{definition}

We call this refinement stencil canonical because it is the only one that contains simplices solely on level $\lone\!+\!1$ and is thus uniquely determined by the refinement relation~\ref{eq:refinement_relation}. Once we allow simplices from finer levels $\ltwo > \lone\!+\!1$, uniqueness is lost.

To guarantee linear independence of the chosen basis functions, we need to ensure that every refinement stencil of every active DoFs contains at least one inactive DoF. We refer to \citep{Piel.2026} for this discussion and only use their results here to motivate the choice of active DoFs in Subsection~\ref{subsec:selecting_active_dofs}.

\todo{\guidance{Reframe (narrative point~2). The last sentence undersells --- it reads as ``this formalism was optional''. Replace it with the actual correspondence: \emph{the refinement stencil is the visual test for linear independence --- a selection is independent precisely when every active DoF's refinement stencil still contains an inactive DoF}. State it as the purpose of this subsection, not as a deferral to the companion paper.}}

\begin{figure}
\begin{center}
% Height-based version: every image is sized by its HEIGHT (\rowheight), so
% trimming a side only makes that image narrower -- it never scales beyond the
% row height. \rowgap sets the adjustable vertical gap between the two rows.
\begin{tikzpicture}[
    every node/.style={inner sep=0pt, outer sep=0pt},
    arr/.style={->, >=Stealth, line width=1.5pt, shorten >=2pt, shorten <=2pt}
]

% ============================================================
%  TUNABLES (unitless, in cm)
% ============================================================
\def\rowheight{3.}   % fixed height of EACH row, in cm
\def\rowgap{1.0}    % vertical gap between the two rows, in cm
\def\divideroffset{10}  % how far (in pt) each divider stops short of the top/bottom

% ============================================================
%  COLUMN X POSITIONS
% ============================================================
\def\xA{0}
\def\xB{5.0}
\def\xC{9.5}

% ============================================================
%  ROW Y POSITIONS (row 2 sits one row-height + gap below row 1)
% ============================================================
\def\yOne{0}
\def\yTwo{-\rowheight-\rowgap}

% ============================================================
%  ROW 1 (simplices)
% ============================================================
\node (A1) at (\xA, \yOne)
    {\includegraphics[height=\rowheight cm]{images/stencils/0_form_simplex.pdf}};
\node[xshift=2pt, yshift=10pt, font=\small, text=black] at (A1.center) {$\simplex{0}{\l}{\coarseIdxone}$};

\node (B1) at (\xB, \yOne)
    {\includegraphics[height=\rowheight cm, trim=10pt 0pt 230pt 0pt, clip]{images/stencils/1_form_simplex.pdf}};
\node[xshift=-0pt, yshift=10pt, font=\small, text=black] at (B1.center) {$\simplex{1}{\l}{\coarseIdxone}$};

\node (C1) at (\xC, \yOne)
    {\includegraphics[height=\rowheight cm, trim=10pt 0pt 230pt 0pt, clip]{images/stencils/2_form_simplex.pdf}};
\node[xshift=-12pt, yshift=8pt, font=\small, text=black] at (C1.center) {$\simplex{2}{\l}{\coarseIdxone}$};

% ============================================================
%  ROW 2 (refinement stencils)
% ============================================================
\node (A2) at (\xA, \yTwo)
    {\includegraphics[height=\rowheight cm]{images/stencils/0_form_refinement_stencil.pdf}};

\node (B2) at (\xB, \yTwo)
    {\includegraphics[height=\rowheight cm, trim=10pt 0pt 230pt 0pt, clip]{images/stencils/1_form_refinement_stencil.pdf}};

\node (C2) at (\xC, \yTwo)
    {\includegraphics[height=\rowheight cm, trim=10pt 0pt 230pt 0pt, clip]{images/stencils/2_form_refinement_stencil.pdf}};

% ============================================================
%  VERTICAL ARROWS (one per column, between the two rows)
% ============================================================
\draw[arr] (A1.south) -- (A2.north) node[midway, right, font=\small] {$\;\; \cRSt\big( \simplex{0}{\l}{\coarseIdxone} \big)$};
\draw[arr] ([xshift=-0pt]B1.south) -- ([xshift=-0pt]B2.north) node[midway, right, font=\small] {$\;\; \cRSt\big( \simplex{1}{\l}{\coarseIdxone} \big)$};
\draw[arr] ([xshift=-0pt]C1.south) -- ([xshift=-0pt]C2.north) node[midway, right, font=\small] {$\;\; \cRSt\big( \simplex{2}{\l}{\coarseIdxone} \big)$};

% ============================================================
%  VERTICAL DIVIDERS (thin; one full-height line per column-pair,
%  spanning from the top of row 1 to the bottom of row 2 so the middle
%  gap is covered too). Anchored to the image nodes -> they track
%  trims / column moves and never extend beyond the images.
% ============================================================
% between columns A and B
\draw[line width=0.1pt] ([yshift=-\divideroffset pt]$(A1.north east)!0.5!(B1.north west)$) -- ([yshift=\divideroffset pt]$(A2.south east)!0.5!(B2.south west)$);
% between columns B and C
\draw[line width=0.1pt] ([yshift=-\divideroffset pt]$(B1.north east)!0.5!(C1.north west)$) -- ([yshift=\divideroffset pt]$(B2.south east)!0.5!(C2.south west)$);

\end{tikzpicture}
\end{center}
\vspace{-5mm}
\caption{Refinement stencils of $0$-, $1$- and $2$-form basis functions.}
\end{figure}

\subsection{Derivatives of basis functions}

The previous concepts set up the hierarchical nature of the subdivision $k$-form spaces. Ultimately, they will allow us to pick and choose our active degrees of freedom as we wish (with certain restrictions).

We now set up the differential structure of the spaces. First,~\cite{Piel.2026} shows that the uniform subdivision $k$-form spaces comprise the de Rham complex
\begin{equation}
\begin{tikzcd}
  \constrVec{\mathfrak{R}}{}{\lone}{\L}\colon \quad 0 \arrow[r] & \SDFspace{0}{\lone}{\L} \arrow[r, "\extd"] & \SDFspace{1}{\lone}{\L} \arrow[r, "\extd"] & \SDFspace{2}{\lone}{\L} \arrow[r] & 0.
\end{tikzcd}
\end{equation}
on all intermediate levels $\lone$ with $\lzero \leq \l \leq \lone \leq \L$. Throughout this work, we do not distinguish between $\extd : \FEECspace{k}{\lone} \to \FEECspace{k+1}{\lone}$ and $\auxextd : \SDFspace{k}{\lone}{\L} \to \SDFspace{k+1}{\lone}{\L}$ like in~\cite{Piel.2026}. We will denote both operators by $\extd$ because they largely coincide for our purposes.

The coordinate expression of the action of the exterior derivative $\extd$ on the spaces $\SDFspace{k}{\lone}{\L}$ follows directly from the coarse-level oriented incidence matrix $\Dmat{k}{\lone}$~\citep{Hirani.2003,Desbrun.2006,Piel.2026} by
\begin{equation}
    \label{eq:derivative_relation_basis}
    \extd \constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone} = \sum_{\coarseIdxtwo=1}^{\sumdimlk{\lone}{k+1}} \Dmatij{k}{\lone}{\coarseIdxtwo}{\coarseIdxone} \, \constrQ{\SDFbasis}{k+1}{\lone}{\L}{\coarseIdxtwo}.
\end{equation}

Since $\Dmat{k}{\lone}$ is the signed incidence matrix of $\T_\lone$, only the $(k+1)$-simplices of $\T_\lone$ adjacent to $\simplex{k}{\lone}{\coarseIdxone}$ appear with non-zero coefficients. In the context of splines, this is equivalent to forward-differencing, in particular for triangular splines like box splines~\citep{de_boor_box_1993}.

\begin{definition}[Derivative Stencil]
    \label{def:derivative_stencil}
    Let $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$ be a basis function of $\SDFspace{k}{\lone}{\L}$ associated with the $k$-simplex $\simplex{k}{\lone}{\coarseIdxone} \in \K^k_{\lone}$. The derivative stencil $\DSt$ is a map 
    \begin{equation}
        \DSt : \K^k_{\lone} \to \powerset \big( \K^{k+1}_{\lone} \big), \qquad \simplex{k}{\lone}{\coarseIdxone} \robert{\mapsto} \big\{ \simplex{k+1}{\lone}{\coarseIdxtwo}: \; \Dmatij{k}{\lone}{\coarseIdxtwo}{\coarseIdxone} \neq 0 \big\}.
    \end{equation}
\end{definition}

Once again, we overload the derivative stencil such that it can also act on sets of simplices:
\begin{equation}
    \DSt : \powerset\Big(\K^k_{\lone}\Big) \to \powerset \big( \K^{k+1}_{\lone} \big), \qquad \simplexsubset{k}{\lone} \mapsto \bigcup_{\simplex{k}{\lone}{\coarseIdxone} \in \simplexsubset{k}{\lone}} \DSt \big( \simplex{k}{\lone}{\coarseIdxone} \big)
\end{equation}

This definition together with Eq.~\eqref{eq:derivative_relation_basis} implies the following two propositions.

\begin{proposition}
    \label{prop:d_tilde_on_phi}
    $\robert{\extd} \constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone} = \sum\limits_{\simplex{k+1}{\lone}{\coarseIdxtwo} \in \DSt(\simplex{k}{\lone}{\coarseIdxone})} \Dmatij{k}{\lone}{\coarseIdxtwo}{\coarseIdxone} \, \robert{\constrQ{\SDFbasis}{k+1}{\lone}{\L}{\coarseIdxtwo}}$
\end{proposition}

\begin{proposition}
    \label{cor:derivative_support}
    $\support \big( \constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone} \big) = \support \big( \robert{\extd} \constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone} \big) = \bigcup\limits_{\simplex{k+1}{\lone}{\coarseIdxtwo} \in \DSt(\simplex{k}{\lone}{\coarseIdxone})} \mathrm{supp} \big( \constrQ{\SDFbasis}{k+1}{\lone}{\L}{\coarseIdxtwo} \big).$
\end{proposition}

The derivative stencil is key for ensuring that every active $k$-form basis function has its derivative stencil present in the $(k+1)$-form space and that the resulting spaces constitute a differential complex. The following lemma makes the stencil concrete for the Wang schemes~\cite{Wang.2006,Wang.2008} used in this work.

\begin{lemma}[{\cite{Piel.2026}}]
    \label{lemma:derivative_stencil}
    Let $\freeQ{v}{}{\lone}{\coarseIdxone} \in \V_{\lone}$ and $\freeQ{e}{}{\lone}{\coarseIdxtwo} \in \E_{\lone}$ be a vertex and an edge on level $\lone$. For the Wang subdivision schemes~\cite{Wang.2006,Wang.2008} (whose $0$-form scheme coincides with Loop subdivision~\cite{Loop.1987}), the derivative stencils are given by
    \begin{equation}
        \DSt\big( \freeQ{v}{}{\lone}{\coarseIdxone} \big) = \EVl{\lone}\big( \freeQ{v}{}{\lone}{\coarseIdxone} \big) \quad \text{and} \quad \DSt\big( \freeQ{e}{}{\lone}{\coarseIdxtwo} \big) = \FEl{\lone}\big( \freeQ{e}{}{\lone}{\coarseIdxtwo} \big),
    \end{equation}
    or more generally, $\DSt\big(\simplex{k}{\lone}{\coarseIdxone}\big) = \K^{k+1}\K^k_{\lone}\big(\simplex{k}{\lone}{\coarseIdxone}\big)$ for any $\simplex{k}{\lone}{\coarseIdxone} \in \K^k_{\lone}$.
\end{lemma}
\begin{proof}
    See~\cite{Piel.2026}.
\end{proof}

\cite{Piel.2026} go on to show that the derivative of every active basis function can be represented by active basis functions of the next space in the complex. To obtain this result, they need to show that every simplex in the derivative stencil has a refinement stencil in the active DoFs. In this work, we only use the derivative stencil to motivate the choice of active DoFs in Subsection~\ref{subsec:selecting_active_dofs}.

\todo{\guidance{Reframe (narrative point~2), mirroring the refinement-stencil reframe above. State the correspondence rather than the deferral: \emph{the derivative stencil is the visual test for closure of the complex --- the spaces close into a de Rham complex precisely when every active DoF's derivative stencil is itself active}. Together with the refinement-stencil condition, these are the two conditions the subdivision partition enforces.}}

\begin{figure}
\begin{center}
% Scale is tied to HEIGHT (not width): fixing the height means trimming the
% horizontal extent (tightening the column gaps / divider below) makes the
% figure narrower without enlarging the images. Adjust the height to taste.
\resizebox{!}{3.2cm}{%
\begin{tikzpicture}[
    every node/.style={inner sep=0pt, outer sep=0pt},
    arr/.style={->, >=Stealth, line width=1.5pt, shorten >=2pt, shorten <=2pt}
]

% ============================================================
%  COLUMN X POSITIONS (single row: simplex -> deriv stencil, twice)
% ============================================================
\def\xA{0cm}
\def\xB{4.3cm}
\def\xC{8.0cm}
\def\xD{11.5cm}

\def\yOne{0cm}

% ============================================================
%  0-FORM GROUP
% ============================================================
\node (A) at (\xA, \yOne)
    {\includegraphics[height=3cm]{images/stencils/0_form_simplex.pdf}};
\node[xshift=2pt, yshift=10pt, font=\small, text=black] at (A.center) {$\simplex{0}{\l}{\coarseIdxone}$};

\node (B) at (\xB, \yOne)
    {\includegraphics[height=3cm]{images/stencils/0_form_deriv_stencil.pdf}};

\draw[arr] (A.east) -- (B.west) node[midway, above=6pt, font=\small] {$\DSt\big( \simplex{0}{\l}{\coarseIdxone} \big)$};

% ============================================================
%  DIVIDER (thin, no taller than the images: height 3cm -> +/-1.5cm)
% ============================================================
\draw[line width=0.1pt] (6.35cm, 1.2cm) -- (6.35cm, -1.2cm);

% ============================================================
%  1-FORM GROUP
% ============================================================
\node (C) at (\xC, \yOne)
    {\includegraphics[height=3cm, trim=10pt 0pt 230pt 0pt, clip]{images/stencils/1_form_simplex.pdf}};
\node[xshift=-0pt, yshift=10pt, font=\small, text=black] at (C.center) {$\simplex{1}{\l}{\coarseIdxone}$};

\node (D) at (\xD, \yOne)
    {\includegraphics[height=3cm, trim=10pt 0pt 230pt 0pt, clip]{images/stencils/1_form_deriv_stencil.pdf}};

\draw[arr] ([xshift=-0pt]C.east) -- ([xshift=-1pt]D.west) node[midway, above=6pt, font=\small] {$\DSt\big( \simplex{1}{\l}{\coarseIdxone} \big)$};

\end{tikzpicture}%
}
\end{center}
\vspace{-3mm}
\caption{Derivate stencils of $0$- and $1$-form basis functions, arranged in a single row: each simplex maps under $\DSt$ to its derivative stencil, with the $0$-form and $1$-form groups separated by a divider.}
\end{figure}

\FloatBarrier

\subsection{Selecting active DoFs using subdivision partitions}
\label{subsec:selecting_active_dofs}

Section~\ref{subsec:computational_representations} established that constructing the hierarchical subdivision matrix $\Amat{k}{\activeDoFs}{\L}$ reduces to choosing, at each level $\l$, an index set $\activeDoFSet{k}{\l} \subset \K^k_\l$ of active simplices. Each such choice selects the corresponding columns of $\Amat{k}{\l}{\L}$, assembling $\Amat{k}{\activeDoFs}{\L}$ block by block as in~\eqref{eq:hierarchical_matrix_blocks}. This section addresses the question left open there: which choices of $\activeDoFSet{k}{\l}$ are admissible? Collecting the active simplices across levels,
\begin{equation}
    \activeDoFSet{k}{} \coloneq \bigcup_{\l=\lzero}^{\L} \activeDoFSet{k}{\l},
\end{equation}
the corresponding hierarchical space is
\begin{equation}
    \activeSDFspaces{k}{} \coloneq \mathrm{span}\bigl( \constrVec{\SDFbases}{k}{\activeDoFs}{\L} \bigr),
\end{equation}
where $\constrVec{\SDFbases}{k}{\activeDoFs}{\L}$ is the active basis function vector from~\eqref{eq:hierarchical_basis_vector}.

Not every choice yields a well-behaved hierarchical space $\hierSDFspace{k}{\C}{\L}$. The selected columns of $\Amat{k}{\activeDoFs}{\L}$ must be linearly independent, must together represent forms that cover the entire domain, and the spaces for successive form degrees must close the de Rham complex. The remainder of this section characterises the admissible choices via the notion of a \emph{subdivision partition}.

\guidance{The two admissibility requirements have a purely visual reading in terms of the refinement and derivative stencils introduced above. A selection is linearly \emph{independent} when every active basis function's refinement stencil still contains an inactive DoF; the spaces \emph{close into a complex} when every active function's derivative stencil is itself active. The subdivision partition defined below is the geometric bookkeeping that enforces both conditions at once, so that admissibility can be checked by inspecting patch interfaces rather than the abstract stencil machinery. This is what the stencils were for.}

\begin{figure}[tb]
\begin{center}
\begin{tikzpicture}[
    every node/.style={inner sep=0pt, outer sep=0pt},
    arr/.style={->, >=Stealth, line width=1.5pt, shorten >=2pt, shorten <=2pt}
]

% ============================================================
%  COLUMN X POSITIONS
% ============================================================
\def\xA{0cm}
\def\xB{3.9cm}
\def\xC{7.8cm}

% ============================================================
%  ROW Y POSITIONS
% ============================================================
\def\yOne{0cm}
\def\yTwo{-2.4cm}
\def\yThree{-4.8cm}

% ============================================================
%  ROW 1
% ============================================================
\node (A1) at (\xA, \yOne)
    {\includegraphics[width=0.22\textwidth]{images/dofmeshes/correct/dofs_k_0_lvl_l0.pdf}};

\node (B1) at (\xB, \yOne)
    {\includegraphics[width=0.22\textwidth, trim=10pt 0pt 10pt 0pt, clip]{images/dofmeshes/correct/dofs_k_1_lvl_l0.pdf}};

\node (C1) at (\xC, \yOne)
    {\includegraphics[width=0.22\textwidth]{images/dofmeshes/correct/dofs_k_2_lvl_l0.pdf}};

% ============================================================
%  ROW 2
% ============================================================
\node (A2) at (\xA, \yTwo)
    {\includegraphics[width=0.22\textwidth]{images/dofmeshes/correct/dofs_k_0_lvl_l1.pdf}};

\node (B2) at (\xB, \yTwo)
    {\includegraphics[width=0.22\textwidth, trim=10pt 0pt 10pt 0pt, clip]{images/dofmeshes/correct/dofs_k_1_lvl_l1.pdf}};

\node (C2) at (\xC, \yTwo)
    {\includegraphics[width=0.22\textwidth]{images/dofmeshes/correct/dofs_k_2_lvl_l1.pdf}};

% ============================================================
%  ROW 3
% ============================================================
\node (A3) at (\xA, \yThree)
    {\includegraphics[width=0.22\textwidth]{images/dofmeshes/correct/dofs_k_0_lvl_l2.pdf}};

\node (B3) at (\xB, \yThree)
    {\includegraphics[width=0.22\textwidth, trim=10pt 0pt 10pt 0pt, clip]{images/dofmeshes/correct/dofs_k_1_lvl_l2.pdf}};

\node (C3) at (\xC, \yThree)
    {\includegraphics[width=0.22\textwidth]{images/dofmeshes/correct/dofs_k_2_lvl_l2.pdf}};

% ============================================================
%  COLUMN LABELS (above row 1)
% ============================================================
\node[above=8pt of A1, font=\bfseries, inner sep=2pt] (colA) {$k = 0$};
\node[above=8pt of B1, font=\bfseries, inner sep=2pt] (colB) {$k = 1$};
\node[above=8pt of C1, font=\bfseries, inner sep=2pt] (colC) {$k = 2$};

% ============================================================
%  ROW LABELS (to the left of each row)
% ============================================================
\node[left=12pt of A1, font=\bfseries, inner sep=2pt] {$\l = 0$};
\node[left=12pt of A2, font=\bfseries, inner sep=2pt] {$\l = 1$};
\node[left=12pt of A3, font=\bfseries, inner sep=2pt] {$\l = 2$};

% ============================================================
%  OVERBRACE
% ============================================================
\draw[
    decoration={calligraphic brace, amplitude=6pt, raise=4pt},
    decorate, line width=1.2pt
]
    (A1.north west|-colA.north) -- (C1.north east|-colA.north)
    node[midway, above=14pt, font=\bfseries] {Active DoFs};

% ============================================================
%  GRAPHIC ABOVE THE OVERBRACE
% ============================================================
\node (topfig) at (3.9cm, 4.0cm)
    {\includegraphics[width=0.3\textwidth]{images/dofmeshes/correct/subdiv_partition.pdf}};

\node[above=6pt of topfig, font=\bfseries] {Subdivision partition};

\end{tikzpicture}
\end{center}
\caption{Active $k$-form DoFs for the three-level subdivision partition shown at top. Columns correspond to form degree $k = 0$ (vertices, dark circles), $k = 1$ (edges, orange), and $k = 2$ (faces, green). Rows correspond to active DoFs at levels $\l = 0$, $1$, $2$ respectively. At each level, interior simplices of the patches at that level are active, together with interface simplices on the coarser side along boundaries with finer-level patches (cf.\ Definition~\ref{def:active_k_form_dofs}).}
\label{fig:active_dofs}
\end{figure}

\robert{The preceding subsections establish that every level-$\l$ basis function $\constrQ{\SDFbasis}{k}{\l}{\L}{\coarseIdxone}$ is a column of the subdivision matrix $\Amat{k}{\l}{\L}$, and that the exterior derivative commutes with these matrices via~\eqref{eq:derivative_relation_basis}. Choosing active degrees of freedom therefore amounts to selecting a subset of columns across the hierarchy $\bigl\{\Amat{k}{\l}{\L}\bigr\}_\l$, subject to linear independence, full-domain coverage, and closure of the de Rham complex.

A geometrically natural unit for organising this selection is a \emph{mesh patch}: a connected submesh of $\T_\l$ whose simplices are all activated together. Tiling the domain with patches at (possibly) different levels turns the column-selection problem into a geometric tiling problem, with the constraints checkable locally at patch boundaries. Crucially, the subdivision of a patch's basis functions is already encoded in $\Amat{k}{\l}{\L}$: restricting to the columns indexed by $\K^k(\calP_\l)$ gives exactly the level-$\L$ representations of those basis functions, with no additional patch-level operator required.

A \emph{patch} $\calP_\l \subset \T_\l$ is a non-empty, edge-connected collection of faces of $\T_\l$, with boundary $\partial\calP_\l$ the set of edges adjacent to exactly one face of $\calP_\l$. We write $\calP_{\l \to \L}$ for the collection of level-$\L$ faces obtained by repeatedly splitting the faces of $\calP_\l$.

Two patches $\calP_{\l_i}$ and $\calP_{\l_j}$ are \emph{non-overlapping} if their images in $\T_\L$ share at most edges: $\I_{(\calP_{\l_i}, \calP_{\l_j}) \to \L} \coloneqq \calP_{\l_i \to \L} \cap \calP_{\l_j \to \L} \subset \E_\L(\T_\L)$. A \emph{covering} $\C(\T_\L) = \bigcup_{\l_i,n}\calP^n_{\l_i}$ is a set of pairwise non-overlapping patches whose images tile $\T_\L$, i.e.\ $\T_\L = \bigcup_{\l_i,n}\calP^n_{\l_i \to \L}$. We write $\C_{\l_i}$ for the level-$\l_i$ patches and $\C_{>\l_i}$, $\C_{\geq\l_i}$, $\C_{<\l_i}$, $\C_{\leq\l_i}$ for the corresponding level subsets.}

\begin{definition}[interior vertex at level $\l$]
    \label{def:interior_vertex}
    We call a vertex $\vertex{\l}{\coarseIdxtwo} \in \V(\C)$ \emph{interior at level $\l$} if the partition star $\partitionStar{\C}(\vertclass{\coarseIdxtwo})$ of its equivalence class $\vertclass{\coarseIdxtwo}$ does not contain any face on a lever fine that $\l$, i.e.,
    \begin{equation}
        \label{eq:interior_vertex}
        \partitionStar(\vertclass{\coarseIdxtwo}) \cap \F(\C_{>\l}) = \varnothing.
    \end{equation}
\end{definition}

\begin{remark}
    Note that being an interior vertex is a weaker condition than being \emph{patch interior}, i.e.,
    \begin{equation}
        \label{eq:patch_interior_vertex}
        \partitionStar(\vertclass{\coarseIdxtwo}) = \F\V \big( \vertex{\l}{\coarseIdxtwo} \big) \subset \F(\C_{\l}).
    \end{equation}
\end{remark}

\begin{definition}[admissible face]
    We call a face $\face{\l}{\coarseIdxone} \in \F(\C)$ \emph{admissible} if at least one of its vertices $\vertex{\l}{\coarseIdxtwo} \in \V\F\big( \face{\l}{\coarseIdxone} \big)$ is interior at level $\l$. Otherwise, the face is called \emph{inadmissible}.
\end{definition}

\begin{definition}[pinch vertex]
    Let $\l$ be the finest level of $\partitionStar(\vertclass{\coarseIdxtwo})$ and compute the number $\mathrm{N}_\coarseIdxtwo$
    \begin{equation}
        \mathrm{N}_\coarseIdxtwo \coloneq \Big\vert \big\{ \edge{\l}{\coarseIdxone} \in \E\V(\vertex{\l}{\coarseIdxtwo}) \;\; : \;\; \F\E(\edge{\l}{\coarseIdxone}) \cap \C_\l \neq \varnothing \quad \text{and} \quad \F\E(\edge{\l}{\coarseIdxone}) \setminus \C_\l \neq \varnothing \big\} \Big\vert.
    \end{equation}
    of edges whose adjacent faces split into an active and an inactive one on level $\l$. For an interior vertex of $\T_\l$, $\mathrm{N}_\coarseIdxtwo$ will always be even. \robert{(What about boundary vertices? We also need to be careful with the words "interior" and "boundary" here, define properly.)} The \emph{number of disconnected arcs} $\gamma_\coarseIdxtwo$ of the vertex class $\vertclass{\coarseIdxtwo}$ is given by
    \begin{equation}
        \gamma_\coarseIdxtwo = \begin{cases}
            1, \quad & \mathrm{N}_\coarseIdxtwo = 0 \\
            \tfrac{1}{2} \mathrm{N}_\coarseIdxtwo, \quad & \text{otherwise}.
        \end{cases}
    \end{equation}
    If $\gamma_\coarseIdxtwo \geq 2$, the vertex class $\vertclass{\coarseIdxtwo}$ is a called \emph{pinch} vertex.
\end{definition}

\robert{The pinch predicate is the one genuinely new ingredient: unlike interior-ness, it is a
\emph{connectivity} property of the star, read off from the cut spokes rather than expressible
through the quadrisection operators and $\partitionStar$ alone.}

\robert{
\begin{definition}[Subdivision Partition]
    \label{def:subdiv_partition}
    A covering $\C(\T_\L)$ is a \emph{subdivision partition} if every patch satisfies a minimum-size condition: every face of every patch, after one step of refinement, must contain at least one vertex whose entire face-ring lies strictly in the interior of the refined patch (i.e.\ not on any interface with a neighbouring patch). This rules out interior linear dependence of basis functions; see~\cite{Piel.2026} for the precise statement.
\end{definition}
}

\robert{The key design choices in Definition~\ref{def:active_k_form_dofs} are illustrated in Fig.~\ref{fig:active_dofs}. The need for interface DoFs is easiest to see in the $k=1$ column. At level $\l=0$ (top row, orange edges), only the edges interior to the level-$0$ patches are active; the boundary edges shared with the finer level-$1$ region are absent. At level $\l=1$ (middle row), those same boundary edges appear as active 1-form DoFs. Without them, the exterior derivative $\extd\omega^0$ of a level-$0$ 0-form basis function near the patch boundary would map to an edge with no associated active 1-form DoF, breaking the de Rham complex. More generally, when two patches at levels $\l_i < \l_j$ share a boundary, interface DoFs are added at \emph{every} intermediate level $\l_i < \l_t \leq \l_j$. This prevents basis functions from very different levels meeting abruptly across the interface, which would otherwise create an ill-conditioned system. We do not have rigorous estimates that quantify this stiffening effect, and it remains a subject for further investigation.

The interface DoFs are attributed exclusively to the \emph{finer} side of the patch boundary, not the coarser side. Including coarser-side DoFs would introduce linear dependence: any coarser-level basis function restricted to the interface is already expressible as a linear combination of the finer-level basis functions there (cf.\ the refinement stencil condition in Section~\ref{sec:construction_of_hierarchical_subdiv_spaces}). Restricting to the finer side therefore maintains linear independence while still closing the de Rham complex, as made precise in the theorems at the end of this section.}

\begin{definition}[Active $k$-form degrees of freedom]
    \label{def:active_k_form_dofs}
    Let $\C\big(\T_{L}\big) = \bigcup_{\l_i = \l_0}^L \; \bigcup_{n = 0}^{N_{\l_i}} \; \calP^n_{\l_i}$ be a subdivision partition of $\T_L$. The active $k$-form degrees of freedom $\D^k\big(\C\big)$ are defined as
    \begin{equation}
        \label{eq:active_k_form_dofs}
        \D^k \big(\C\big) \coloneqq \underbrace{\bigcup_{\calP^n_{\l_i} \in \C}}_{(i)} \Bigg[ \underbrace{\K^k \big( \calP^n_{\l_i} \big)}_{(ii)} \cup \underbrace{\bigcup_{\calP^m_{\l_j} \in \C_{> \l_i}}}_{(iii)} \; \underbrace{\bigcup_{\l_t = \l_i +1}^{\l_j}}_{(iv)} \bigg[ \underbrace{\K^k\V_{\l_t} \Big( \V_{\l_t} \big( \I_{(\calP^n_{\l_i \to \l_t}, \calP^m_{\l_j})} \big) \Big)}_{(v)} \cap \underbrace{\K^k_{\l_t} \big( \calP^n_{\l_i \to \l_t} \big)}_{(vi)} \bigg] \Bigg].
    \end{equation}
\end{definition}

Unpacking this definition: for every patch (\textit{i}) $\calP^n_{\l_i}$, we first add all of its $k$-simplices (\textit{ii}). Further, for all relatively finer patches (\textit{iii}) $\calP^m_{\l_j}$ with $\l_j > \l_i$ and all intermediate levels (\textit{iv}) $\l_t$ with $\l_i < \l_t \leq \l_j$, all interface simplices (\textit{v}) are added if they lie on the coarser side (\textit{vi}) of the interface, i.e.\ can be generated by refining patch $\calP^n_{\l_i}$ to level $\l_t$.

\begin{definition}[Adaptive subdivision differential form spaces]
    \label{def:adaptive_function_spaces}
    Fix an initial mesh $\T_{\l}$ and maximum subdivision level $L$. Let the subdivision partition $\C\big(\T_L\big)$ induce the active $k$-form degrees of freedom $\D^k \big( \C \big)$. The \textbf{active basis functions} of the space $\hierSDFspace{k}{\C}{\L}$ are given by
    \begin{equation}
        \Phi_{\D^k (\C)} \coloneqq \bigcup_{\sigma^k_{\l_i, p} \in \D^k} \Big\{ \phi^k_{\l_i \to L, p} \Big\}
    \end{equation}
    and thus
    \begin{equation}
        \hierSDFspace{k}{\C}{\L} \coloneqq \mathrm{span}\big( \Phi_{\D^k (\C)} \big).
    \end{equation}
\end{definition}

\noindent Note that the basis functions are increasingly dense for higher levels $\l_i$ because the resolution of the patches $\calP_{\l_i}$ and thus $\D^k_{\l_i}$ scales with roughly $4^{\l_i - \l}$. Two essential properties must be established for the spaces $\hierSDFspace{k}{\C}{\L}$: linear independence of the basis functions and the de Rham complex. The proofs of both properties are given in~\cite{Piel.2026}; the main results are restated below.

\begin{remark}
    The numerical examples in Section~5 use subdivision partitions where only adjacent levels share an interface. In that case, the active DoF formula~\eqref{eq:active_k_form_dofs} simplifies: only a single layer of interface simplices needs to be added at the finer of the two adjacent levels.
\end{remark}

Finally, we are going to re-state the two main theorems of \cite{Piel.2026} regarding linear independence and the exactness of the complex.

\begin{theorem}
    Let $\C$ be a subdivision partition of the mesh hierarchy $\T_\l$ for $\l \in \{ \lzero, \hdots, \L\}$. The basis functions of the hierarchical spaces $\hierSDFspace{k}{\C}{\L}$ from Definition~\ref{def:adaptive_function_spaces} are linearly independent.
\end{theorem}

\begin{proof}
    The necessary properties of a subdivision partition were chosen so that linear independence was guaranteed, see \cite{Piel.2026} for the entire proof.
\end{proof}

\begin{theorem}
    Let $\C$ be an subdivision partition. The hierarchical spaces $\hierSDFspace{k}{\C}{\L}$ constitute the differential complex
    \begin{equation}
    \begin{tikzcd}
        \mathfrak{R}_\C: \qquad 0 \arrow[r] & \mathbb{R} \arrow[r, "\subset"] & \hierSDFspace{0}{\C}{\L} \arrow[r, "\extd"] & \hierSDFspace{1}{\C}{\L} \arrow[r, "\extd"] & \hierSDFspace{2}{\C}{\L} \arrow[r] & 0.
    \end{tikzcd}
    \end{equation}
    The hierarchical subdivision $k$-form spaces with vanishing trace form the differential complex
    \begin{equation}
    \begin{tikzcd}
        \mathring{\mathfrak{R}}_\C: \qquad 0 \arrow[r] & \hierSDFspace{0}{\C}{\L} \arrow[r, "\extd"] & \hierSDFspace{1}{\C}{\L} \arrow[r, "\extd"] & \hierSDFspace{2}{\C}{\L} \arrow[r, "\int"] & \mathbb{R} \arrow[r] & 0.
    \end{tikzcd}
    \end{equation}
    If $\C$ is an \emph{exact} subdivision partition of a \emph{simply-connected} domain, the complexes $\mathfrak{R}_\C$ and $\mathring{\mathfrak{R}}_\C$ are exact.
\end{theorem}

\begin{proof}
    See \cite{Piel.2026}.
\end{proof}

\subsection{Derived objects on a subdivision partition}
\label{subsec:partition_star}

\robert{The estimation, marking, and refinement steps of the adaptive loop below repeatedly refer to three objects derived from a subdivision partition $\C$: the equivalence class of a vertex across levels, the \emph{ancestor} of a fine face, and the \emph{partition star} of a vertex. As they recur throughout the remainder of the paper, we collect their definitions here.}

We first capture the persistence of vertices across the hierarchy by defining an equivalence class $\vertclass{\coarseIdxtwo}$:
\begin{equation}
    \label{eq:def_vertex_class}
    \vertclass{\coarseIdxtwo} \coloneq \big\{ \vertex{\l}{\coarseIdxtwo}, \; \vertex{\l \to \l+1}{\coarseIdxtwo}, \; \dots, \; \vertex{\l \to \L}{\coarseIdxtwo} \big\}, \quad \text{where} \quad \l = \min \big\{ \, \lone \in \{\lzero, \, \dots, \, \L\} \; \colon \; \coarseIdxtwo \leq \vert \V_\lone \vert \, \big\}
\end{equation}
and $\vertex{\l \to \lone}{\coarseIdxtwo} = \Aop{}{\l}{\lone} \vertex{\l}{\coarseIdxtwo}$ denotes the refined vertices. We chose the $\widehat{\cdot}$ notation because we think of the equivalence relation as lifting the vertex across all finer levels. The active vertices in any equivalence class are given by $\vertclass{\coarseIdxtwo} \cap \V(\C)$. Since every equivalence class contains at least one active vertex, the number of vertex equivalence classes of $\C$ is at most the number of active $0$-form DoFs, $\vert \activeDoFSet{0}{\C} \vert$.

Further, we define the \emph{ancestor} $\ancestor{\face{\lone}{\fineIdxone}}$ by the relation
\begin{equation}
    \label{eq:def_ancestor}
    \face{\l}{\coarseIdxone} = \ancestor{\face{\lone}{\fineIdxone}} \quad \leftrightarrow \quad \face{\lone}{\fineIdxone} \in \Aop{}{\l}{\lone} \big( \face{\l}{\coarseIdxone} \big),
\end{equation}
\robert{that is, $\ancestor{\face{\lone}{\fineIdxone}}$ is the unique active face of $\C$ covering the finer face $\face{\lone}{\fineIdxone}$: the face itself if it is active, otherwise its active coarser ancestor.}

\robert{This allows us to define the \emph{partition star} $\partitionStar(\vertclass{\coarseIdxtwo})$ of a vertex equivalence class $\vertclass{\coarseIdxtwo}$ by}
\begin{equation}
    \label{eq:def_partition_star}
    \robert{\partitionStar(\vertclass{\coarseIdxtwo}) \coloneq \big\{ \, \ancestor{\face{\L}{\fineIdxone}} \; : \; \face{\L}{\fineIdxone} \in \FVl{\L} \big( \vertex{\l \to \L}{\coarseIdxtwo} \big) \, \big\}.}
\end{equation}
\robert{The partition star operator $\partitionStar(\vertclass{\coarseIdxtwo})$ collects \emph{every} active face of the subdivision partition $\C$ incident to the vertex, starting from the coarsest possible level: for each face $\face{\L}{\fineIdxone}$ in the vertex's finest-level one-ring $\FVl{\L}(\vertex{\l \to \L}{\coarseIdxtwo})$ it takes the covering active ancestor $\ancestor{\face{\L}{\fineIdxone}}$. This includes not only the faces of which the vertex is an active corner, but also the coarse face lying across an interface edge through the vertex. We name it the \emph{partition} star because the covering ancestors are cells of the partition $\C$ and hence tile the neighbourhood of the vertex without overlap, unlike faces taken across different levels of the hierarchy, which do overlap.}

\robert{Finally, we write
\begin{equation}
    \label{eq:def_partition_star_slice}
    \partitionStarl{\l}(\vertclass{\coarseIdxtwo}) \coloneq \partitionStar(\vertclass{\coarseIdxtwo}) \cap \F(\C_\l)
\end{equation}
for the \emph{level-$\l$ slice} of the partition star, the active faces of the star that lie on level $\l$. Because the covering ancestors tile the vertex neighbourhood without overlap, these slices partition $\partitionStar(\vertclass{\coarseIdxtwo})$ across the levels it occupies.}

\robert{The estimation, marking, refinement, and padding steps below all refine a subdivision partition through \emph{two} operators, which we define here once and refer back to. The \emph{flat quadrisection} $\quadriF{\C}(\simplexsubset{2}{\l})$ refines a \emph{single-level} set of faces $\simplexsubset{2}{\l} \subseteq \F(\C_\l)$ by replacing each face with its four children,
\begin{equation}
    \label{eq:quadrisection_operator}
    \quadriF{\C}(\simplexsubset{2}{\l}) \coloneq \C_{<\l} \; \cup \; \big\{ \C_{\l} \setminus \simplexsubset{2}{\l} \big\} \; \cup \; \big\{ \C_{\l + 1} \cup \Aop{}{\l}{\l + 1} \big(\simplexsubset{2}{\l}\big) \big\} \; \cup \; \C_{>\l + 1};
\end{equation}
that is, it removes the faces $\simplexsubset{2}{\l}$ from level $\l$ and adds their children on level $\l+1$, leaving all other levels untouched.}

\robert{The \emph{graded quadrisection} $\quadriV{\C}{t}(\partitionStar(\vertclass{\coarseIdxtwo}))$ homogenises a whole partition star \emph{up to} a target level $t$ by nested flat quadrisection. With $\sourcelevel(\vertclass{\coarseIdxtwo}) = \min\{\l : \partitionStarl{\l}(\vertclass{\coarseIdxtwo}) \neq \varnothing\}$ the coarsest level the star occupies, it is built by:
\begin{enumerate}
    \item Initialization: $\C^{\sourcelevel} = \C$;
    \item for $\l = \sourcelevel, \, \dots, \, t-1$, flat-refine the level-$\l$ slice of the star \emph{recomputed on the current partition} $\C^\l$,
    \begin{equation}
        \label{eq:graded_quadrisection}
        \C^{\l+1} = \quadriF{\C^\l}\big( \partitionStarCl{\C^\l}{\l}(\vertclass{\coarseIdxtwo}) \big);
    \end{equation}
    \item set $\quadriV{\C}{t}(\partitionStar(\vertclass{\coarseIdxtwo})) \coloneq \C^{t}$.
\end{enumerate}
Recomputing the slice on $\C^\l$ at each step is what drags a coarse interface face up through \emph{every} intermediate level up to $t$. The two operators are distinguished by their superscript --- $\quadriF{}$ refines a set of faces flatly by one level, while $\quadriV{}{\cdot}$ refines a star gradedly toward its vertex --- and only the graded operator carries a target level.}


\section{Implementation and adaptive loop}

\subsection{Assembling the hierarchical subdivision matrix}

The implementation of the active subdivision spaces $\hierSDFspace{k}{\C}{\L}$ proceeds in two stages. 
Given a subdivision partition $\C$, we first identify the active degrees of
freedom $\D^k$ for each form degree $k \in \{0,1,2\}$ 
using Algorithm~\ref{alg:buildactivedofs} in the appendix. Algorithm~\ref{alg:buildsubdivmatrix} then assembles, for each form
degree $k$, the sparse matrix $\Amat{k}{\activeDoFs}{\L}$ whose columns are
the level-$\L$ coefficient expansions of the active basis functions $\Phi_{\D^k (\C)}$, see Equations~\eqref{eq:hierarchical_form_compact} and~\eqref{eq:hierarchical_matrix_blocks}. Galerkin projection of all
bilinear forms to the active space then reduces to standard sparse
matrix-matrix products, without any re-assembly of integrals, see for example Equation~\eqref{eq:FE_matrix_assembly}.

The active DoFs split can be split into two contributions at each level $\l$.
\emph{Patch} DoFs are the $k$-simplices of every active face $\face{\l}{\coarseIdxone} \in \C$. \emph{Interface} DoFs are additional basis functions at the
boundary between patches on different levels. This grouping of $\D^k(\C)$ by levels is achieved by the following lemma.

\begin{lemma}[Splitting active DoFs by level, cf.~\cite{companion_paper} for proof.]
    \label{lemma:active_k_form_dofs_per_level}
    The active degrees of freedom $\D^k(\C)$ from Definition~\ref{def:active_k_form_dofs} can equivalently be described by
    \begin{equation}
        \D^k \big( \C \big) = \bigcup_{\l = \lzero}^\L \; \D^k_{\l} \big( \C \big)
    \end{equation}
    with
    \begin{equation}
        \label{eq:active_k_form_dofs_per_level}
        \D^k_{\l} \big( \C \big) = \K^k_{\l} \big(\C_{\l}\big) \cup \bigcup\limits_{\calP^n_{\lone} \in \C_{<\l}} \; \bigcup\limits_{\calP^m_{\ltwo} \in \C_{\geq \l}} \; \Big[ \K^k\V_{\l} \Big( \V_{\l} \big(\I_{(\calP^n_{\lone \to \l}, \, \calP^m_{\ltwo})} \big) \Big) \cap \K^k_{\l} \big(\calP^n_{\lone \to \l}\big) \Big]
    \end{equation}
    for $\l = \lzero, \, \dots, \, \L$.
\end{lemma}

Lemma~\ref{lemma:active_k_form_dofs_per_level}
is the basis of the active-DoF assembly, given in full as
Algorithm~\ref{alg:buildactivedofs} in Appendix~\ref{app:active_dof_pseudocode}.
The algorithm is indexed by
level $\l = 0, \, \dots, \, \L$, mirroring $\D^k(\C) = \bigcup_{\l_t} \D^k_{\l_t}(\C)$,
and its two stages realise the two terms of Equation~\eqref{eq:active_k_form_dofs_per_level}. By contrast, the
patch-pair indexing of Definition~\ref{def:active_k_form_dofs} would translate
into a doubly-nested loop over patch pairs, which is not what we implement.
The native DoFs are read off directly from the active faces at each level,
while the interface DoFs are produced by a frontier sweep that maintains the
set of interior edges separating resolved (coarse-side) faces from
unresolved (finer) regions.

Once the active DoFs $\D^k$ have been identified, the adaptive subdivision matrix
$\Amat{k}{\activeDoFs}{\L}$ is assembled column by column. According to Equation~\eqref{eq:hierarchical_matrix_blocks}, Algorithm~\ref{alg:buildsubdivmatrix} extracts the columns of $\Amat{k}{\l}{\L}$ corresponding to the active DoFs $\activeDoFSet{k}{\l}$ on level $\l$.

\robert{\begin{algorithm}[tb]
\caption{$\textsc{BuildSubdivMatrix}(\D, k)$: adaptive subdivision matrix}
\label{alg:buildsubdivmatrix}
\KwIn{active DoFs $\D$, form degree $k$, uniform subdivision matrices $\Amat{k}{\l}{\L}$}
\KwOut{adaptive subdivision matrix $\Amat{k}{\activeDoFs}{\L}$}
\BlankLine
$\Amat{k}{\activeDoFs}{\L} \leftarrow (\vert \K^k_\L \vert \times \vert \D^k \vert)$ sparse zero matrix\;
$\mathrm{col} \leftarrow 0$\;
\For{$\l = 0, \, \dots, \, \L$}{
    \For{each simplex $\simplex{k}{\l}{\coarseIdxone} \in \D^k_\l$}{
        $\Amat{k}{\activeDoFs}{\L}[\,:\,,\mathrm{col}] \leftarrow \Amat{k}{\l}{\L}[\,:\,, \coarseIdxone]$\;
        $\mathrm{col} \leftarrow \mathrm{col} + 1$\;
    }
}
\Return $\Amat{k}{\activeDoFs}{\L}$\;
\end{algorithm}}

\robert{Once the necessary adaptive subdivision matrices are assembled, the
bilinear forms of the spaces $\hierSDFspace{k}{\C}{\L}$ are obtained by \emph{unrefinement} of the assembled operators on the FEEC spaces $\FEECspace{k}{\L}$, see~\cite{Piel.2026} for more details. For example, the matrices of the mixed formulation of the vector Laplacian from Section~\ref{subsec:vector_laplace} can be unrefined by
\begin{equation}
    \label{eq:FE_matrix_assembly}
    \mathbf{M}_\C = (\Amat{0}{\activeDoFs}{\L})^T \; \mathbf{M}_\L \; \Amat{0}{\activeDoFs}{\L},
    \quad
    \mathbf{C}_\C = (\Amat{1}{\activeDoFs}{\L})^T \; \mathbf{C}_\L \; \Amat{1}{\activeDoFs}{\L},
    \quad
    \mathbf{B}_\C = (\Amat{0}{\activeDoFs}{\L})^T \; \mathbf{B}_\L \; \Amat{1}{\activeDoFs}{\L},
\end{equation}
where $\mathbf{M}_\L$, $\mathbf{C}_\L$, $\mathbf{B}_\L$ are the mass, curl--curl,
and gradient-coupling matrices over $\FEECspace{k}{\L}$.}

\todo{Do we want this here? I want to give an idea as to where the subdiv matrices enter the algorithms concretely.}

\FloatBarrier

\subsection{The adaptive loop}

\guidance{The matrix representation stays fixed across the loop: every step re-selects columns of $\{\Amat{k}{\l}{\L}\}_\l$ and reassembles the generic Galerkin system of Section~\ref{subsec:computational_representations}. What the loop actually changes --- and must keep admissible --- is the subdivision partition, i.e.\ the column index set. The \textsc{Pad} step is the mechanism that repairs admissibility after marking.}

This section describes the adaptive loop used in the numerical experiments. The loop consists of the steps
\begin{equation}
\begin{tikzcd}
    \textsc{Solve} \arrow[r] & \textsc{Estimate} \arrow[r] & \textsc{Mark} \arrow[r] & \textsc{Refine} \arrow[r] & \textsc{Pad} \arrow[llll, rounded corners=0pt, to path={(\tikztostart.south) -- ([yshift=-1.em]\tikztostart.south) -- ([yshift=-1.em]\tikztotarget.south) -- (\tikztotarget.south)}]
\end{tikzcd}
\end{equation}

The following sections will cover the individual steps of this loop, and focus particularly on the non-standard choices we made during the implementation. The $\textsc{Solve}$ step involves solving a linear system. For example, for the vector Laplace problem in Section~\ref{subsec:vector_laplace}, we use Eigen's (\robert{cite}) sparse MINRES solver with a block-diagonal approximate (mass-lumped) Schur complex preconditioner (\robert{maybe keep some information from below here and delete what's below.}). For the $\textsc{Estimate}$ step, we use a standard $\Hcurl$ estimator to compute residuals per face in the subdivision partition. These residuals are then re-distributed to the active vertices in the partition. The $\textsc{Estimate}$ step is covered in more detail in Section~\ref{subsec:estimate}. We employ greedy Dörfler marking for the vertex residuals to refine vertex stars. This typically leads to inexact subdivision partitions, and thus needs a $\textsc{Pad}$ step to recover its exactness, see Section~\ref{subsec:refinement_and_padding}.

\subsubsection{Solve}
\label{sec:preconditioner}


\robert{We solve the discrete system on the hierarchical space $\hierSDFspace{k}{\C}{\L}$ with an iterative solver. For the mixed problems considered here the block system
\begin{equation}
    \label{eq:mixed_block_system}
    K =
    \begin{pmatrix}
        -\mathbf{M}_0 & \mathbf{B}_0 \\
        \mathbf{B}_0^\top & \mathbf{C}_0
    \end{pmatrix},
\end{equation}
assembled from the Galerkin-restricted blocks $\mathbf{M}_0$, $\mathbf{C}_0$, $\mathbf{B}_0$ of Equation~\eqref{eq:FE_matrix_assembly}, is symmetric indefinite, so we use MINRES with a block-diagonal SPD preconditioner
\begin{equation}
    \mathbf{P} =
    \begin{pmatrix}
        \mathbf{M}_0 & 0 \\
        0 & \mathbf{S}
    \end{pmatrix},
    \qquad
    \mathbf{S} = \mathbf{C}_0 + \mathbf{B}_0^\top\, \mathrm{diag}(\mathbf{M}_0 \mathbf{1})^{-1}\, \mathbf{B}_0,
\end{equation}
where $\mathbf{S}$ approximates the Schur complement of the $(1,1)$ block by replacing $\mathbf{M}_0^{-1}$ with a row-sum lumped inverse ($\mathbf{1}$ the all-ones vector of length $\dim \D^0$). Row-sum lumping bounds $\mathbf{M}_0$ from above with a mesh-independent constant for piecewise-linear triangles, giving a stable Schur approximation on the irregular meshes produced by adaptive refinement. Both blocks are factorised once per adaptive step by sparse Cholesky (LDLT) and discarded afterwards, since the active spaces are non-nested between steps; each MINRES iteration then costs one triangular back-solve per block.}

\todo{
    I am not sure we need this info, it does not really make sense to include and does not help me to convey the message this paper is about.
}

\subsubsection{Estimation}
\label{subsec:estimate}

We compute error indicators $\eta_{\l,\coarseIdxone}$ per face $\face{\l}{\coarseIdxone} \in \C$ of the subdivision partition. The residual is evaluated on the finest level $\L$ and then aggregated to the coarse parent faces:
\begin{equation}
    \label{eq:project_estimator_on_L_to_partition}
    \eta^2_{\l,\coarseIdxone} = \sum\limits_{\face{\L}{\fineIdxone} \; \in \; \Aop{}{\l}{\L}(\face{\l}{\coarseIdxone})} \eta^2_{\L,\fineIdxone},
\end{equation}
where $f_h$ is obtained by applying the discrete PDE operator to $u_h$. Note that the interface DoFs are excluded from the indicator computation. In the following, we will refer to $\eta^2$ and $\rho^2$ as the \emph{total} and \emph{refinable} residuals, respectively, defined as
\begin{equation}
    \label{def:total_and_refinable_residuals}
    \eta^2 = \sum\limits_{\l=\lzero}^{\L} \; \eta^2_\l \qquad \text{and} \qquad \rho^2 = \sum\limits_{\l=\lzero}^{\L-1} \; \eta^2_\l = \eta^2 - \eta^2_{\L} \qquad \text{with} \qquad \eta^2_{\l} = \sum\limits_{\face{\l}{\coarseIdxone} \; \in \; \F(\C_\l)} \eta^2_{\l,\coarseIdxone}.
\end{equation}
The refinable residual discards the contributions of faces $\face{\L}{\coarseIdxone}$ on level $\L$ of the subdivision partition $\C$. We measure this refinable residual because faces cannot be refined beyond level $\L$ without global mesh refinement to extend the mesh hierarchy. For this reason, the residual accumulated on level $\L$ can no longer be decreased. In fact, we find that
\begin{equation}
    \label{eq:saturation_rate}
    \rho^2 / \eta^2 = 1 - \eta^2_\L / \eta^2 \eqqcolon \xi
\end{equation}
This \emph{saturation rate} $\xi \in [0,1]$ provides a good analytic tool and a concrete stopping criterion for the adaptive loop.

\begin{example}[Vector Laplace estimator]
For the mixed vector Laplace problem with $(\sigma_h, u_h) \in \hierSDFspace{0}{\C}{\L} \times \hierSDFspace{1}{\C}{\L}$ from Section~\ref{subsec:vector_laplace}, the basis functions are piecewise linear approximations of the subdivision limit functions. Hence $\extd (\iota u_h)$ is piecewise constant in $\FEECspace{2}{\L}$ and $\boldsymbol{\delta}\extd (\iota u_h)$ vanishes elementwise, while its contribution appears in the curl jumps across edges. In $\FEECspace{2}{\L}$, the indicator takes the form
\begin{equation}
\begin{aligned}
    \eta_{\L,\fineIdxone}^2 = \quad &\sum\limits_{\face{\L}{\coarseIdxone} \,\in\, \F_\L} \!\! \robert{h_{\face{\L}{\fineIdxone}}^2} \big( \|f - \extd \sigma_h\|_{L^2(\face{\L}{\fineIdxone})}^2 + \|\sigma_h + \boldsymbol{\delta} u_h\|_{L^2(\face{\L}{\fineIdxone})}^2 \big) \\
    + &\sum\limits_{\edge{\L}{\fineIdxtwo} \, \in \, \E_\L} \robert{h_e} \big\|[\![\mathrm{curl}(u_h)]\!]\big\|_{L^2(\edge{\L}{\fineIdxtwo})}^2.
\end{aligned}
\end{equation}
The first sum contains the elementwise residuals of both equations, and the second sum contains the curl jump across interior edges $e$ of the fine mesh $\T_\L$. The per-face indicator $\eta_{\L,\fineIdxone}^2$ on level $\L$ can be translated back to the subdivision partition $\C$ using Equation~\ref{eq:project_estimator_on_L_to_partition}.
\end{example}

\noindent We now have estimators $\eta_{\l,\coarseIdxone}$ per face of the subdivision partition $\C$. However, the atomic units of refinement in our scheme are vertex stars because they give rise to clean analysis. To enable vertex-based refinement for the adaptive algorithm, we need to sum up the face estimators $\eta_{\l,\coarseIdxone}$ to vertex estimators. Recall from Section~\ref{subsec:partition_star} the vertex equivalence classes $\vertclass{\coarseIdxtwo}$ from Equation~\eqref{eq:def_vertex_class} and their partition stars $\partitionStar(\vertclass{\coarseIdxtwo})$ from Equation~\eqref{eq:def_partition_star}, the active faces of $\C$ incident to the vertex. The vertex-based residual $\nu_\coarseIdxtwo$ our code uses is then defined by
\begin{equation}
    \label{eq:vertex_residual}
    \nu^2_\coarseIdxtwo = \tfrac{1}{a_\coarseIdxtwo} \; \sum\limits_{\face{\l}{\coarseIdxone} \; \in \; \partitionStar(\vertclass{\coarseIdxtwo}) \setminus \F_\L} \eta^2_{\l,\coarseIdxone}, \qquad \mathrm{where} \qquad a_\coarseIdxtwo = \sum\limits_{\face{\l}{\coarseIdxone} \; \in \; \partitionStar(\vertclass{\coarseIdxtwo}) \setminus \F_\L} \mathrm{area}(\face{\l}{\coarseIdxone}).
\end{equation}
Dividing by the cumulative area of the faces rescales the integral face estimator to a density per vertex. This rescaling makes the marking roughly level- and valence-agnostic. Otherwise, the contribution of coarse faces would be weighted higher and their adjacent vertices would be over-represented during the Dörfler marking step. Note also that we do not take faces on level $\L$ into account since they cannot be refined further, so their error is partically fixed.

Because the partition star includes the coarse face across any interface edge through the vertex, $\nu_\coarseIdxtwo$ accounts for exactly the faces that refining the vertex's star would homogenise during the \textsc{Refine} step, see Section~\ref{subsec:refine}. For this reason, $\nu_\coarseIdxtwo$ measures exactly the error that selecting the vertex would actually resolve in the sense of Dörfler marking, see Section~\ref{subsec:mark}.

\begin{figure}%[htbp]
    \centering
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/pipeline/pipeline_fig1a.pdf}
        \caption{Per-face residual $\eta_f$.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/pipeline/pipeline_fig1b.pdf}
        \caption{Per-vertex residual $\nu_v$.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/pipeline/pipeline_fig1c.pdf}
        \caption{After refinement.}
    \end{subfigure}
    \caption{\robert{Marking stage of the worked example
    (Eq.~\eqref{eq:worked_example_eta}). (a) the coarse mesh coloured by the
    per-face residual $\eta_f$; (b) the per-vertex error density $\nu_\coarseIdxtwo$, the
    ranking key the marker sorts by, evaluated at each vertex's coarsest active
    level; (c) the partition after refining the marked stars, before any padding,
    with the selected vertices overlaid on the density scale of~(b).}}
    \label{fig:pipeline_marking}
\end{figure}

\subsubsection{Marking}
\label{subsec:mark}

\todo{\guidance{Dissolve the worked example here (narrative point~3). When Section~\ref{subsec:worked_example} is folded in, attach Fig.~\ref{fig:pipeline_marking} (residual $\to$ density $\to$ post-mark) as the running illustration of this paragraph, using the closed-form $\eta$ of Eq.~\eqref{eq:worked_example_eta} as the concrete input.}}


Our code marks vertices rather than faces. This enables us to refine single vertex stars, the central combinatorial object of the admissibility argument in~\cite{companion_paper}. We rank the vertices by the vertex estimator $\nu_\coarseIdxtwo$, see Equation~\eqref{eq:vertex_residual} from Section~\ref{subsec:estimate}. For this purpose, we enumerate the $\mathrm{V}$ vertex equivalence classes $\vertclass{\coarseIdxtwo}$ of $\C$ in order of decreasing residual, so that $\nu_{\coarseIdxtwo_1} \geq \nu_{\coarseIdxtwo_2} \geq \ldots \geq \nu_{\coarseIdxtwo_{\mathrm{V}-1}} \geq \nu_{\coarseIdxtwo_\mathrm{V}}$.

The Dörfler marking algorithm greedily selects the first $q$ vertex equivalence classes with the largest estimators such that the \emph{marked refinable residual}
\begin{equation}
    \label{eq:marked_refinable_residual}
    \rho^2_{\R_\theta} \; \coloneqq \sum\limits_{\face{\l}{\coarseIdxone} \; \in \; \R_\theta} \eta^2_{\l, \coarseIdxone}, \qquad \text{where} \qquad \R_\theta = \bigcup\limits_{p=1}^q \partitionStar(\vertclass{\coarseIdxtwo_p}) \setminus \F(\C_\L),
\end{equation}
captures at least a fraction $\theta$ of the refinable residual $\rho$ of Equation~\eqref{def:total_and_refinable_residuals}, i.e.
\begin{equation}
    \label{eq:doerfler_selection}
    \rho_{\R_\theta} \; \geq \; \theta \, \rho
\end{equation}
where $\theta \in (0, 1]$ is the Dörfler parameter. The set $\R_\theta \subset \F(\C_{<\L})$ collects all the faces that are refined during the \textsc{Refine} step, see Section~\ref{subsec:refine}; faces on level $\L$ are excluded, so $\rho_{\R_\theta}$ is a restriction of the refinable residual $\rho$ rather than of the total residual $\eta$.

\subsubsection{Refinement}
\label{subsec:refine}

The \textsc{Refine} step refines the marked faces $\face{\l}{\coarseIdxone} \in \R_\theta$ of the partition stars of the vertex equivalence classes $\vertclass{\coarseIdxtwo_1}, \, \dots, \, \vertclass{\coarseIdxtwo_q}$ from Equation~\eqref{eq:marked_refinable_residual}. First, we compute source levels $\sourcelevel(\vertclass{\coarseIdxtwo_p})$ and target levels $\targetlevel(\vertclass{\coarseIdxtwo_p})$ for every vertex class by
\begin{equation}
\label{eq:source_and_target_level_of_vertex_class}
\begin{aligned}
    \sourcelevel_{\coarseIdxtwo_p} = \sourcelevel(\vertclass{\coarseIdxtwo_p}) &= \min \big\{ \l \in \{\lzero, \, \dots, \, \L\}: \; \partitionStar(\vertclass{\coarseIdxtwo_p}) \cap \F(\C_\l) \neq \varnothing \big\}, \\
    \targetlevel_{\coarseIdxtwo_p} = \targetlevel(\vertclass{\coarseIdxtwo_p}) &= \min \big\{ \max \big\{ \l \in \{\lzero, \, \dots, \, \L\}: \; \partitionStar(\vertclass{\coarseIdxtwo_p}) \cap \F(\C_\l) \neq \varnothing \big\} + 1, \;\; \L \big\}.
\end{aligned}
\end{equation}

\todo{Do we need the source level? Shall we make it a generic thing in section 3.4?}

These source and target levels must be computed on the fixed input subdivision partition $\C$ before any refinement takes place, to avoid a cascade of ever increasing target levels. The \textsc{Refine} step then homogenises each marked star to one level beyond its finest: it applies the graded quadrisection $\quadriV{\C}{\targetlevel_{\coarseIdxtwo_p}}\big(\partitionStar(\vertclass{\coarseIdxtwo_p})\big)$ from Equation~\eqref{eq:graded_quadrisection} with target level $\targetlevel_{\coarseIdxtwo_p}$.

The result of the \textsc{Refine} step is the subdivision partition $\C^q$ given by
\begin{enumerate}
    \item Initialization: subdivision partition $\C^0 = \C$,
    \item Refine partition stars iteratively for $p \in \{1, \, \dots, \, q\}$: $\C^{p} = \quadriV{\C^{p-1}}{\targetlevel_{\coarseIdxtwo_p}}\big( \partitionStar(\vertclass{\coarseIdxtwo_p})\big)$.
\end{enumerate}
\robert{Because the source and target levels $\sourcelevel_{\coarseIdxtwo_p}, \targetlevel_{\coarseIdxtwo_p}$ are frozen on the input partition $\C$ and each quadrisection only ever homogenises a star upward to its target, the order in which the $q$ stars are processed does not affect the resulting partition $\C^q$. We may therefore apply them in any order; our code processes the coarsest stars first.}

\begin{figure}
\begin{center}
% Scale is tied to HEIGHT (not width): fixing the height and trimming the
% horizontal extent (tightening the column gaps / divider below) makes the
% figure narrower without enlarging the images. Adjust the height to taste.
\resizebox{!}{3.0cm}{%
\begin{tikzpicture}[
    every node/.style={inner sep=0pt, outer sep=0pt},
    arr/.style={->, >=Stealth, line width=1.5pt, shorten >=2pt, shorten <=2pt}
]

% ============================================================
%  COLUMN X POSITIONS (single row: input -> refined, twice)
% ============================================================
\def\xA{0cm}
\def\xB{3.8cm}
\def\xC{8.2cm}
\def\xD{12.0cm}

\def\yOne{0cm}

% ============================================================
%  CENTRE VERTEX GROUP
% ============================================================
\node (A) at (\xA, \yOne)
    {\includegraphics[height=3cm]{images/refine_interface/centre_input.pdf}};

\node (B) at (\xB, \yOne)
    {\includegraphics[height=3cm]{images/refine_interface/centre_refined.pdf}};

% ============================================================
%  DIVIDER (thin, no taller than the images: height 3cm -> +/-1.5cm)
% ============================================================
\draw[line width=0.1pt] (6.0cm, 1.3cm) -- (6.0cm, -1.3cm);

% ============================================================
%  NEIGHBOUR-NEIGHBOUR VERTEX GROUP
% ============================================================
\node (C) at (\xC, \yOne)
    {\includegraphics[height=3cm]{images/refine_interface/neighbour-neighbour_input.pdf}};

\node (D) at (\xD, \yOne)
    {\includegraphics[height=3cm]{images/refine_interface/neighbour-neighbour_refined.pdf}};

\end{tikzpicture}%
}
\end{center}
\vspace{-3mm}
\caption{\robert{Marked-star refinement on a valence-$6$ level-$0$/level-$2$ interface, for two
positions of the marked vertex (highlighted in the input panels), arranged in a single
row: each input maps under $\textsc{Refine}$ to the refined partition, with the
centre-vertex and fine-side-vertex groups separated by a divider.}}
\label{fig:refine_interface}
\end{figure}

\subsubsection{Padding}
\label{subsec:padding}

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/pipeline/pipeline_fig2a.pdf}
        \caption{Before padding.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/pipeline/pipeline_fig2b.pdf}
        \caption{resolved vertex pinches.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/pipeline/pipeline_fig2c.pdf}
        \caption{inadmissible-faces pad.}
    \end{subfigure}
    \caption{\robert{Padding stage of the worked example. (a) repeats
    Fig.~\ref{fig:pipeline_marking}(c), the partition before padding; (b) after
    $\textsc{ResolveVertexPinches}$, the centre pinch is gone; (c) after the
    inadmissible-faces pad, the one remaining inadmissible face is refined, giving the
    final admissible partition (grading does not fire here).}}
    \label{fig:pipeline_padding}
\end{figure}

\todo{\guidance{Dissolve the worked example here (narrative point~3). Attach Fig.~\ref{fig:pipeline_padding} (pre-pad $\to$ after pinch $\to$ after inadmissible-faces) as the illustration of this paragraph, and \emph{end the padding exposition with the open hand-off} placed below.}}

The \textsc{Refine} step in the previous section yields a partition that might no longer be admissible. The \textsc{Pad} step restores the exactness of the subdivision partition by applying additional refinement around vertex pinches and inadmissible faces, see Definition~\ref{def:exact_subdiv_partition}. The goal is to keep the padded faces to a minimum to avoid inflating the number of DoFs in non-marked regions. The only exception is some grading that we apply around vertices whose partition star covers too many levels. This grading does not change the restored exactness but improves the conditioning of the resulting matrix-vector system. This section covers all three kinds of padding one by one.

The \textsc{Pad} needs no machinery beyond the partition star $\partitionStar$ and the two quadrisection operators $\quadriF{\C}$, $\quadriV{\C}{\cdot}$ of Section~\ref{subsec:partition_star} since every padding substep is a quadrisection of either a partition-star of a vertex class or of faces that violate an admissibility or grading condition.

\paragraph{Resolving vertex pinches.}

\robert{For each pinched vertex $\vertclass{\coarseIdxtwo}$ we homogenise its star to its finest level by applying $\quadriV{\C}{\L_{\coarseIdxtwo}}\big(\partitionStar(\vertclass{\coarseIdxtwo})\big)$, lifting the coarse arcs to $\L_{\coarseIdxtwo}$ and merging them into a single disc. This resolves the cohomology defect: a coarse region pinched at a vertex would otherwise carry a spurious harmonic.}

\robert{\begin{algorithm}[H]
\caption{$\textsc{ResolveVertexPinches}(P)$ (condensed; full version in Alg.~\ref{alg:pinch_full})}
\label{alg:pinch}
\Repeat{a pass refines nothing}{
    detect all pinched vertices on the fixed partition $P$: flag $\vertclass{\coarseIdxtwo}$ if it has $\geq 4$ cut spokes, i.e.\ $\geq 2$ arcs, on the level-$\L_{\coarseIdxtwo}$ one-ring (Eq.~\eqref{eq:arc_count})\;
    \lFor{each flagged $\vertclass{\coarseIdxtwo}$}{$P \leftarrow \quadriV{P}{\L_{\coarseIdxtwo}}\big(\partitionStar(\vertclass{\coarseIdxtwo})\big)$}
}
\Return $P$\;
\end{algorithm}}

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[b]{0.22\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/exactness/v8_quarters_lvl2_input.pdf}
        \caption{Pinch input.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.22\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/exactness/v8_quarters_lvl2_padded.pdf}
        \caption{Pinch padded.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.22\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/exactness/v8_quarters_lvl2_mixedcoarse_input.pdf}
        \caption{Mixed-coarse input.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.22\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/exactness/v8_quarters_lvl2_mixedcoarse_padded.pdf}
        \caption{Mixed-coarse padded.}
    \end{subfigure}
    \caption{\robert{Pinch resolution on a valence-$8$ centre vertex. Left pair: a
    two-level pinch, whose two coarse arcs give $\dim\mathcal{H}^1 = 2$
    (Table~\ref{tab:atomic-cohomology}, ``Valence-$8$ pinch, level~2''); pinch
    resolution grades the coarse arcs to the finest level and removes the defect.
    Right pair: the two coarse patches sit at \emph{different} levels ($0$ and $1$),
    which is why the pinch is detected at each vertex's finest level with contiguous
    coarse runs merged across levels.}}
    \label{fig:pinch_examples}
\end{figure}

\paragraph{Resolving inadmissible faces.}

\robert{For each active face $\face{\l}{\coarseIdxone}$ with no corner interior at level $\l$ we quadrisect the face, applying $\quadriF{\C}\big(\{\face{\l}{\coarseIdxone}\}\big)$. This enforces condition~I of Definition~\ref{def:active_k_form_dofs} --- every active face must have a corner interior at its level --- and so restores admissibility at coarse/fine interfaces that are too tight.}

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[b]{0.28\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/exactness/v6_3rings_horseshoe_lvl2_input.pdf}
        \caption{Input ($0\leftrightarrow2$ jump).}
    \end{subfigure}\qquad
    \begin{subfigure}[b]{0.28\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/exactness/v6_3rings_horseshoe_lvl2_padded.pdf}
        \caption{Graded (coarse arc raised to level~$1$).}
    \end{subfigure}
    \caption{\robert{Grading versus inadmissible-faces padding. An already-exact horseshoe
    (open arc, no spurious harmonic) with a two-level jump: grading only raises the
    coarse arc by one level to satisfy $\mathrm{maxSpan}$, it does not refine it to
    the finest level, cleanly separating the conditioning role of grading from the
    admissibility role of inadmissible-faces padding.}}
    \label{fig:grading_example}
\end{figure}

\paragraph{Grading partition stars.}

\robert{For each vertex $\vertclass{\coarseIdxtwo}$ whose active faces span more than $\mathrm{maxSpan}$ levels we quadrisect its coarsest star slice, applying $\quadriF{\C}\big(\partitionStarl{\sourcelevel_{\coarseIdxtwo}}(\vertclass{\coarseIdxtwo})\big)$, where $\sourcelevel_{\coarseIdxtwo}$ is the coarsest active level of the star. Unlike the first two rules this is a purely numerical repair (excessive level jumps): it does not change the restored exactness, but bounds the level span around a vertex and so improves the conditioning of the matrix-vector system.}

\robert{The pinch rule uses the \emph{graded} quadrisection $\quadriV{}{\cdot}$ on a whole star, at
target $\L_{\coarseIdxtwo}$ --- one level below the \textsc{Refine} target; the inadmissible-faces and grading rules use the \emph{flat} quadrisection $\quadriF{}$, on a single frontier face and on the
coarsest slice $\partitionStarl{\sourcelevel_{\coarseIdxtwo}}$ of a star respectively. Every rule only
ever refines and adds the fewest faces that locally repair its violation, so each pass
is monotone and $\textsc{Pad}$ terminates: no face is refined beyond level $\L$. The
rules interact --- homogenising a star or refining a frontier face can expose a fresh
violation of another rule --- so we alternate pinch resolution
($\textsc{ResolveVertexPinches}$) with inadmissible-faces-plus-grading ($\textsc{PadCondI}$)
until a full pass changes nothing. A counter $\mathrm{padding\_cycles}$ records the
productive passes, which was $1$ in every atomic test and every adaptive step we
observed; empirically the fixed point is insensitive to the order in which violations
are processed, though we do not prove it. Within $\textsc{PadCondI}$ we apply
inadmissible-faces padding first, to a fixed point, and run grading last on the span violations that
survive the now-admissible partition, which avoids over-refining, since inadmissible-faces
padding usually already refines the coarse frontier faces that would otherwise trip grading.
Pinch resolution subsumes an earlier boundary-vertex repair: a lone coarse boundary
\emph{notch} is a single coarse arc (\emph{fine\,--\,coarse\,--\,fine}), not a pinch, so
it is left to inadmissible-faces padding and grading.}

\robert{\begin{algorithm}[H]
\caption{$\textsc{Pad}(P)$: admissibility padding (condensed; full sub-routines in Alg.~\ref{alg:pinch_full}, \ref{alg:condI_full})}
\label{alg:padding}
\KwIn{partition $P$}
\KwOut{admissible partition $P$}
\Repeat{a full pass refines nothing}{
    $P \leftarrow \textsc{ResolveVertexPinches}(P)$ \tcp{repair pinched coarse regions}
    $P \leftarrow \textsc{PadCondI}(P)$ \tcp{inadmissible faces $+$ grading}
}
\Return $P$\;
\end{algorithm}}

\robert{\begin{algorithm}[H]
\caption{$\textsc{PadCondI}(P)$ (condensed; full version in Alg.~\ref{alg:condI_full})}
\label{alg:condI}
\tcp{Phase 1 --- admissibility (condition I)}
\Repeat{no face is refined this pass}{
    \lForEach{active $\face{\l}{\coarseIdxone}$ with no corner interior at level $\l$}{$P \leftarrow \quadriF{P}\big(\{\face{\l}{\coarseIdxone}\}\big)$}
}
\tcp{Phase 2 --- grading}
\Repeat{no vertex spans $> \mathrm{maxSpan}$ levels}{
    \lForEach{$\vertclass{\coarseIdxtwo}$ whose active faces span $> \mathrm{maxSpan}$ levels}{$P \leftarrow \quadriF{P}\big(\partitionStarl{\sourcelevel_{\coarseIdxtwo}}(\vertclass{\coarseIdxtwo})\big)$}
    re-run Phase 1\;
}
\Return $P$\;
\end{algorithm}}

\FloatBarrier

\section{Numerical Experiment}

\subsection{Exactness and padding on atomic partitions}
\label{subsec:exactness_atomic}

\guidance{This subsection is the verification promised at the end of the \textsc{Pad} discussion: it establishes that pinch resolution and inadmissible-faces padding \emph{reliably} restore the correct cohomology across the admissibility boundary, not just on the single constructed input of the (dissolved) worked example.}

\robert{Before applying the adaptive loop to a PDE, we validate the admissibility
construction directly on a suite of atomic test cases and the two failure modes it
is designed to prevent. Each case is a small concentric-ring mesh around a single
interior vertex of chosen valence --- a topological \emph{disk}, so that
\emph{every} discrete harmonic that appears is spurious. For each partition we
compute the de Rham cohomology of the active space (from the ranks of the discrete
$\extd$) before and after $\textsc{Pad}$; Table~\ref{tab:atomic-cohomology}
collects the results under both natural and vanishing-trace (Dirichlet) boundary
conditions.}

\robert{A non-admissible partition can fail in two qualitatively different ways.
In a \emph{hard failure} the kernel/image of $\extd$ on the active space genuinely
changes, producing a spurious harmonic --- a discrete $k$-form that is closed but
not exact with no topological counterpart --- which corrupts the Hodge
decomposition and, for Maxwell, injects the spurious zero modes of
Section~\ref{subsec:worked_example}. In a \emph{soft failure} the active basis is
merely linearly \emph{dependent}: its column space is still an exact subcomplex, so
the cohomology is correct, but the naive DoF count overstates the space dimension
and the mass matrix is singular. In Table~\ref{tab:atomic-cohomology} a hard
failure shows up as a nonzero harmonic $\dim\mathcal{H}^1$ on a disk, while a soft
failure shows up as a rank deficit $n_k - \dim V^k > 0$; both distort the Euler
characteristic $\chi$ away from the disk value $\chi = 1$, and both are removed by
padding.}

\robert{The controlled examples delineate the admissibility boundary precisely
(Fig.~\ref{fig:loop_horseshoe}, \ref{fig:control_boundary}):}
\begin{itemize}
    \item \robert{\textbf{A coarse loop injects a spurious $\mathcal{H}^1$.} A
    one-band-wide coarse annulus (the ``$3$-ring, inner$+$outer refined'' row) is
    \emph{both} a hard failure ($\dim\mathcal{H}^1 = 1$) and a soft failure
    ($n_1 = 360$, $\operatorname{rank} = 342$, deficit $18$): the single richest
    case. Padding refines the middle band and restores an exact, independent basis.}
    \item \robert{\textbf{Break the loop and the defect vanishes.} Replacing the
    closed annulus by an open arc (``horseshoe'') makes the coarse region a disk
    again: no spurious harmonic, confirming that the defect is topological (the
    closed loop), not a mesh-size artifact.}
    \item \robert{\textbf{It is specifically a one-band effect.} A
    \emph{two}-band-wide coarse annulus (the ``$4$-ring, middle refined'' negative
    control) is already admissible --- padding is a no-op --- so the defect is a
    one-band-strip phenomenon, not a property of any coarse annulus.}
    \item \robert{\textbf{Boundary notches are a Dirichlet-only, additive defect.}
    Refining the stars of boundary ``corner'' vertices leaves coarse notches; under
    natural BCs these are harmless slits, but under Dirichlet each is a relative
    loop, and independent notches \emph{add} (six symmetric notches give exactly six
    Dirichlet harmonics).}
\end{itemize}
\robert{The two-level \emph{pinch} and its \emph{doubling law}
($\dim\mathcal{H}^1 = 2$, $\chi = -1$ before padding, both restored after) are the
``Valence-$8$ pinch, level~2'' row of Table~\ref{tab:atomic-cohomology}, already
illustrated in Fig.~\ref{fig:pinch_examples}. Further atomic cases are collected in
Appendix~\ref{app:figure_gallery}.}

\begin{table}[htbp]
  \centering
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{@{}ll l ccc ccc ccc c@{}}
    \toprule
    & & & \multicolumn{3}{c}{active DoFs $n_k$} & \multicolumn{3}{c}{$\dim V^k=\operatorname{rank}A_k$} & \multicolumn{3}{c}{harmonic $\dim \mathcal{H}^k$} & \\
    \cmidrule(lr){4-6}\cmidrule(lr){7-9}\cmidrule(lr){10-12}
    case & BC & state & $0$ & $1$ & $2$ & $0$ & $1$ & $2$ & $0$ & $1$ & $2$ & $\chi$ \\
    \midrule
    \multirow{4}{*}{Valence-6 pinch, level 1} & \multirow{2}{*}{natural} & input & 60 & 156 & 96 & 60 & 156 & 96 & 1 & 1 & 0 & 0 \\
     &  & padded & 61 & 156 & 96 & 61 & 156 & 96 & 1 & 0 & 0 & 1 \\
    \cmidrule(l){2-13}
     & \multirow{2}{*}{Dirichlet} & input & 36 & 132 & 96 & 36 & 132 & 96 & 0 & 1 & 1 & 0 \\
     &  & padded & 37 & 132 & 96 & 37 & 132 & 96 & 0 & 0 & 1 & 1 \\
    \midrule
    \multirow{4}{*}{Valence-8 pinch, level 1} & \multirow{2}{*}{natural} & input & 66 & 174 & 108 & 66 & 174 & 108 & 1 & 1 & 0 & 0 \\
     &  & padded & 73 & 192 & 120 & 73 & 192 & 120 & 1 & 0 & 0 & 1 \\
    \cmidrule(l){2-13}
     & \multirow{2}{*}{Dirichlet} & input & 38 & 146 & 108 & 38 & 146 & 108 & 0 & 1 & 1 & 0 \\
     &  & padded & 45 & 164 & 120 & 45 & 164 & 120 & 0 & 0 & 1 & 1 \\
    \midrule
    \multirow{4}{*}{Valence-8 pinch, level 2} & \multirow{2}{*}{natural} & input & 195 & 556 & 360 & 195 & 556 & 360 & 1 & 2 & 0 & -1 \\
     &  & padded & 209 & 592 & 384 & 209 & 592 & 384 & 1 & 0 & 0 & 1 \\
    \cmidrule(l){2-13}
     & \multirow{2}{*}{Dirichlet} & input & 147 & 508 & 360 & 147 & 508 & 360 & 0 & 2 & 1 & -1 \\
     &  & padded & 161 & 544 & 384 & 161 & 544 & 384 & 0 & 0 & 1 & 1 \\
    \midrule
    \multirow{4}{*}{3-ring, inner+outer refined} & \multirow{2}{*}{natural} & input & 127 & 360 & 234 & 126 & 342 & 216 & 1 & 1 & 0 & 1 \\
     &  & padded & 127 & 342 & 216 & 127 & 342 & 216 & 1 & 0 & 0 & 1 \\
    \cmidrule(l){2-13}
     & \multirow{2}{*}{Dirichlet} & input & 91 & 324 & 234 & 90 & 306 & 216 & 0 & 1 & 1 & 1 \\
     &  & padded & 91 & 306 & 216 & 91 & 306 & 216 & 0 & 0 & 1 & 1 \\
    \midrule
    \multirow{4}{*}{4-ring, middle refined} & \multirow{2}{*}{natural} & input & 169 & 504 & 336 & 169 & 504 & 336 & 1 & 0 & 0 & 1 \\
     &  & padded & 169 & 504 & 336 & 169 & 504 & 336 & 1 & 0 & 0 & 1 \\
    \cmidrule(l){2-13}
     & \multirow{2}{*}{Dirichlet} & input & 145 & 480 & 336 & 145 & 480 & 336 & 0 & 0 & 1 & 1 \\
     &  & padded & 145 & 480 & 336 & 145 & 480 & 336 & 0 & 0 & 1 & 1 \\
    \midrule
    \multirow{4}{*}{Nested centre, level 3} & \multirow{2}{*}{natural} & input & 73 & 240 & 168 & 73 & 240 & 168 & 1 & 0 & 0 & 1 \\
     &  & padded & 73 & 240 & 168 & 73 & 240 & 168 & 1 & 0 & 0 & 1 \\
    \cmidrule(l){2-13}
     & \multirow{2}{*}{Dirichlet} & input & 61 & 228 & 168 & 61 & 228 & 168 & 0 & 0 & 1 & 1 \\
     &  & padded & 61 & 228 & 168 & 61 & 228 & 168 & 0 & 0 & 1 & 1 \\
    \bottomrule
  \end{tabular}
  \caption{\robert{De Rham cohomology of the atomic subdivision-partition test cases, before
  (\emph{input}) and after (\emph{padded}) pinch resolution and admissibility
  padding, under natural and Dirichlet boundary conditions. $n_k$ is the number of
  active $k$-form DoFs; $\dim V^k=\operatorname{rank}A_k$ is the true dimension of
  the active space (a deficit $n_k-\dim V^k>0$ signals a linearly dependent basis);
  $\dim\mathcal{H}^k$ is the rank-corrected harmonic dimension (Betti number). The
  disk has Euler characteristic $\chi=1$ under both BCs; the pinched inputs deviate,
  the padded spaces recover it.}}
  \label{tab:atomic-cohomology}
\end{table}

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[b]{0.22\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/exactness/v6_3rings_inner_outer_refined_input.pdf}
        \caption{Loop input.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.22\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/exactness/v6_3rings_inner_outer_refined_padded.pdf}
        \caption{Loop padded.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.22\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/exactness/v6_3rings_horseshoe_input.pdf}
        \caption{Horseshoe input.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.22\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/exactness/v6_3rings_horseshoe_padded.pdf}
        \caption{Horseshoe padded.}
    \end{subfigure}
    \caption{\robert{Loop versus horseshoe. Left pair: a one-band coarse annulus
    carries a spurious $\dim\mathcal{H}^1 = 1$ and a rank deficit $18$
    (Table~\ref{tab:atomic-cohomology}, ``$3$-ring, inner$+$outer refined''); padding
    refines the middle band to restore an exact, independent basis. Right pair:
    breaking the loop into an open arc makes the coarse region a disk again, so
    there is no defect --- the failure is topological, not a mesh-size artifact.}}
    \label{fig:loop_horseshoe}
\end{figure}

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[b]{0.22\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/exactness/v6_4rings_middle_refined_input.pdf}
        \caption{Two-band input.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.22\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/exactness/v6_4rings_middle_refined_padded.pdf}
        \caption{Two-band padded (no-op).}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.22\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/exactness/v6_boundary_allcorners_lvl1_input.pdf}
        \caption{Boundary notches input.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.22\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/exactness/v6_boundary_allcorners_lvl1_padded.pdf}
        \caption{Boundary notches padded.}
    \end{subfigure}
    \caption{\robert{Negative control and boundary notches. Left pair: a two-band
    coarse annulus is already admissible, so padding is a no-op
    (Table~\ref{tab:atomic-cohomology}, ``$4$-ring, middle refined''), showing the
    loop defect is a one-band-strip effect. Right pair: coarse notches at boundary
    corners are harmless under natural BCs but each becomes a relative loop under
    Dirichlet BCs; independent notches add, and padding removes them.}}
    \label{fig:control_boundary}
\end{figure}

\FloatBarrier

\subsection{Maxwell Eigenvalue problem}
\label{subsec:maxwell}

\guidance{Thesis of this subsection, stated up front so it does not read as a Maxwell-flavoured repeat of Section~\ref{subsec:exactness_atomic}: Section~\ref{subsec:exactness_atomic} established that padding restores the correct cohomology; this section shows the practical consequence for an actual eigensolver. Because a spurious harmonic is closed, its Rayleigh quotient is exactly zero, so the admissibility defect lives entirely at $\lambda = 0$ and leaves the physical band uncontaminated --- in sharp contrast to the interleaved pollution of non-compatible elements. That confinement-to-the-kernel is the new point here, not implied by the cohomology counts of Section~\ref{subsec:exactness_atomic}.}

\robert{The Maxwell cavity (curl--curl) eigenvalue problem is a classical test case for
structure-preserving discretisations. On the domain $\M = [0,\pi]^2$ its weak form
is: find $\lambda \in \mathbb{R}$ and $u \in \HocurlM{\M}$, $u \neq 0$, such that
\begin{equation}
  (\extd u, \extd v)_\M = \lambda (u, v)_\M \qquad \forall v \in \HocurlM{\M},
\end{equation}
with analytic spectrum $\lambda = i^2 + j^2$, $(i,j) \in \mathbb{Z}^2_{\geq 0}\setminus\{0\}$,
i.e.\ $1, 1, 2, 4, 4, 5, \ldots$. We impose homogeneous Dirichlet (vanishing-trace)
boundary conditions, so the relevant invariant is the \emph{relative} cohomology
(mod $\partial\M$). Since the square is contractible, its relative first Betti
number is zero, and \textbf{every harmonic $1$-form appearing in the
discretisation is therefore spurious}. Applying Loop subdivision alters the domain
slightly, so we do not compare the discrete spectrum directly with the analytic
$i^2+j^2$; instead we compare three subdivision partitions that use the same
approximation of $\M$:
\begin{itemize}
    \item \textbf{inexact} --- a mixed-level partition whose interface violates admissibility (Fig.~\ref{fig:Maxwell_inexact_DoFmesh});
    \item \textbf{exact (padded)} --- the \emph{same} partition after $\textsc{Pad}$ (Fig.~\ref{fig:Maxwell_exact_DoFmesh});
    \item \textbf{FEEC (uniform)} --- the finest FEEC space $\FEECspaceZero{1}{\L}$, refining the whole domain to level $\L$, which is the verifiably spurious-mode-free reference~\cite{Arnold.FEEC}.
\end{itemize}}

\robert{Table~\ref{tab:maxwell_spectrum} and Figure~\ref{fig:Maxwell_eigenvalues} report the
result. The physical ($\lambda > 0$) band is \emph{identical} across the three
partitions (to plotted precision) and tracks the analytic values; the only defect
of the inexact partition is $\dim\mathcal{H}^1 = 2$ spurious harmonics, which
padding removes ($\dim\mathcal{H}^1 \to 0$). The reason the pollution is confined
to $\lambda = 0$ follows from the exact-sequence structure. A non-admissible
interface makes the active space fail to be a subcomplex, and the concrete symptom
is a discrete $1$-form that is \emph{closed but not exact} --- a spurious harmonic.
Being closed, $\extd u_h = 0$, so its Rayleigh quotient
$(\extd u_h, \extd u_h)/(u_h, u_h) = 0$: it can appear \emph{only} at $\lambda = 0$
and never in the physical band. This contrasts sharply with classical spectral
pollution for non-compatible elements, where spurious eigenvalues interleave with
the physical band; here the admissibility failure is confined to the kernel of
$\extd$, so the physical spectrum stays clean.}

\robert{The defect must be read off the rank-based $\dim\mathcal{H}^1$ (from the ranks of
the discrete $\extd$), \emph{not} the raw count of near-zero eigenvalues. The
physical kernel $\extd(\hierSDFspace{0}{\C}{\L})$ of gradients is large and
mesh-dependent, and padding changes the gradient DoF count, so the raw zero-count
is not comparable between partitions: here it is $125$ (inexact) versus $90$
(exact), which taken alone would misleadingly suggest padding \emph{removed} modes,
even as the cohomologically meaningful $\dim\mathcal{H}^1$ falls from $2$ to $0$.}

\begin{table}[htbp]
    \centering
    \small
    \begin{tabular}{@{}l ccc c c@{}}
        \toprule
        partition & \multicolumn{3}{c}{physical band $\lambda_1, \ldots, \lambda_6$ recovered} & raw zero-modes ($\lambda \leq \tfrac12$) & $\dim\mathcal{H}^1$ \\
        \midrule
        inexact         & \multicolumn{3}{c}{$1.003,\ 1.008,\ 2.017,\ 4.006,\ 4.028,\ 5.031$} & $125$ & $\mathbf{2}$ \\
        exact (padded)  & \multicolumn{3}{c}{$1.003,\ 1.008,\ 2.017,\ 4.006,\ 4.028,\ 5.031$} & $90$  & $\mathbf{0}$ \\
        FEEC (uniform)  & \multicolumn{3}{c}{$1.002,\ 1.002,\ 2.010,\ 4.000,\ 4.019,\ 5.024$} & $43$  & --- \\
        \bottomrule
    \end{tabular}
    \caption{\robert{Maxwell cavity spectrum on the inexact, exact (padded), and FEEC
    (uniform) partitions. The physical band coincides across all three and tracks
    the analytic $i^2+j^2$; the inexact partition's sole defect is
    $\dim\mathcal{H}^1 = 2$ spurious harmonics, removed by padding. The raw
    zero-mode count is dominated by legitimate gradient modes and is not comparable
    across partitions, so the rank-based $\dim\mathcal{H}^1$ is the clean
    diagnostic.}}
    \label{tab:maxwell_spectrum}
\end{table}

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[b]{0.23\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/maxwell/maxwell_partition_exact.pdf}
        \caption{Exact (padded) partition.}
        \label{fig:Maxwell_exact_DoFmesh}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.23\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/maxwell/maxwell_partition_inexact.pdf}
        \caption{Inexact partition.}
        \label{fig:Maxwell_inexact_DoFmesh}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.5\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/maxwell/maxwell_spectrum.pdf}
        \caption{Physical eigenvalue band.}
        \label{fig:Maxwell_eigenvalues}
    \end{subfigure}
    \caption{\robert{Maxwell eigenvalue problem: the exact (padded) and inexact subdivision
    partitions differ only at a single coarse/fine interface (left, centre); the
    physical ($\lambda > 0$) curl--curl band by mode index for the inexact
    ($\times$), padded ($+$), and FEEC ($\circ$) discretisations overlaps and tracks
    $i^2+j^2$ (right). The $\dim\mathcal{H}^1$ spurious harmonics carried by the
    inexact partition live at $\lambda = 0$, off the physical axis, and are
    annotated rather than plotted.}}
    \label{fig:maxwell_overview}
\end{figure}

\FloatBarrier

\subsection{Vector Laplace problem}
\label{subsec:vector_laplace}

The $1$-form Laplace problem is given by
\begin{equation}
  (\extd \boldsymbol{\delta} + \boldsymbol{\delta} \extd) u = f
\end{equation}
on a domain $\M$. Since the operator $\boldsymbol{\delta}$ is not well-defined for a (subdivision) $1$-form, we introduce the auxiliary variable $\boldsymbol{\delta} \sigma = u$ and use the FEEC-typical mixed weak formulation
\begin{align}
  (\sigma, \tau)_\M - (u, \extd \tau)_\M &= 0 \qquad \forall \tau \in H^1(\M), \\
  (\extd \sigma, v)_\M + (\extd u, \extd v)_\M &= (f, v)_\M \qquad \forall v \in \HocurlM{\M}.
\end{align}

This section studies the \emph{circular layer} test case from \cite{Cabanas.2026} as an $L$-sweep that exercises the full \textsc{Solve}--\textsc{Estimate}--\textsc{Mark}--\textsc{Refine}--\textsc{Pad} loop. Its analytical solution is given by
\begin{equation}
    \label{eq:circular_layer_test_case}
    \Vec{u}_{\mathrm{CL}}(x) = \tanh \big(\alpha (\vvert{\vvert{x}}^2 - R^2) \big) \; \Vec{b}(x) \quad \text{with} \quad \Vec{b}(x) = \begin{bmatrix}
        \sin(\pi x_1) \\
        \sin(\pi x_2)
    \end{bmatrix}
\end{equation}
and $\alpha = 100$ and $R = 0.3$. The vector part $\Vec{b}(x)$ provides a background field while the $\tanh$ term adds a sharp, radially symmetric transition between $+\Vec{b}$ and $-\Vec{b}$ localised on the ring $\vvert{\vvert{x}} = R$. The right-hand side $f_{\mathrm{CL}}$ corresponding to $\Vec{u}_{\mathrm{CL}}$ can be computed using the (strong) vector Laplace equation. The exact solution is thus smooth almost everywhere and steep only on a thin annulus --- the canonical setting for adaptivity. We impose \emph{natural} boundary conditions (no trace on $u$), in contrast to the Dirichlet Maxwell case of Section~\ref{subsec:maxwell}.

\robert{Because $\Vec{u}_{\mathrm{CL}}$ is known in closed form, every solve reports two quantities: the computable residual estimator $\eta$ of Section~\ref{subsec:estimate}, which \emph{drives marking}; and the true error $\Ltwoerror \coloneqq \vvert{u_h - \Vec{u}_{\mathrm{CL}}}_{L^2(\M)}$, computed against $\Vec{u}_{\mathrm{CL}}$ interpolated into a degree-$3$ vector-CG space on the finest mesh, which \emph{measures accuracy} and is never used for marking. Marking by an estimator and measuring by the truth is what makes the error-versus-DoF comparison below meaningful.}

\paragraph{Experimental design.} \robert{For each maximum refinement depth $L \in \{3, 4, 5\}$ we run the adaptive loop (per-vertex Dörfler marking with $\theta = 0.67$, followed by \textsc{Pad}) to estimator saturation, and, as a reference, a single \emph{uniform} solve at level $L$. The saturation criterion is discussed below; it stops the L=3 run at step $5$, L=4 at step $7$, and L=5 at step $9$. Table~\ref{tab:cl_headline} collects the headline numbers at the final, saturated adaptive step. The adaptive scheme reaches the accuracy of the uniform level-$L$ solve at a small fraction of its $1$-form DoFs $n_1$, and the saving \emph{grows with depth} --- $3.3\times$, $5.0\times$, $6.2\times$ at $L = 3, 4, 5$ --- because the layer needs fine resolution only locally, so the deeper the refinement allowed, the more uniform refinement wastes on the smooth bulk. Figure~\ref{fig:cl_solution} shows the mechanism directly: the partition concentrates refinement on the circular layer while leaving the interior and exterior coarse.}

\begin{table}[htbp]
    \centering
    \small
    \begin{tabular}{@{}r c rr rr cc@{}}
        \toprule
        & & \multicolumn{2}{c}{adaptive (saturated)} & \multicolumn{2}{c}{uniform level $L$} & & \\
        \cmidrule(lr){3-4}\cmidrule(lr){5-6}
        $L$ & step & $n_1$ & $\Ltwoerror$ & $n_1$ & $\Ltwoerror$ & DoF saving & $L^2$ gap \\
        \midrule
        3 & 5 & $3\,230$  & $9.07\cdot10^{-4}$ & $10\,688$  & $8.34\cdot10^{-4}$ & $\mathbf{3.3\times}$ & $1.09\times$ \\
        4 & 7 & $8\,520$  & $5.16\cdot10^{-4}$ & $42\,496$  & $3.70\cdot10^{-4}$ & $\mathbf{5.0\times}$ & $1.39\times$ \\
        5 & 9 & $27\,319$ & $4.03\cdot10^{-4}$ & $169\,472$ & $2.00\cdot10^{-4}$ & $\mathbf{6.2\times}$ & $2.02\times$ \\
        \bottomrule
    \end{tabular}
    \caption{\robert{The $L$-sweep at the saturated adaptive step. The adaptive scheme matches the uniform level-$L$ accuracy at $3.3\text{--}6.2\times$ fewer $1$-form DoFs $n_1$, with the saving growing with $L$. The adaptive $L^2$ error floors slightly above the uniform value at the same $L$ (the ``$L^2$ gap''), a consequence of marking in the energy norm while measuring in $L^2$; this is decomposed in Table~\ref{tab:cl_decomp}. The DoF count is not flat across $L$ because saturation is deliberately conservative (see the stopping criterion below); a looser stop sharpens the saving to $3.5\times / 5.8\times / 9.9\times$.}}
    \label{tab:cl_headline}
\end{table}

\begin{figure}[htbp]
    \centering
    \begin{minipage}[c]{0.80\textwidth}
        \centering
        \begin{subfigure}[b]{0.31\linewidth}
            \includegraphics[width=\textwidth]{images/laplace_case/u_L5_step0.pdf}
            \caption{Step $0$.}
        \end{subfigure}\hfill
        \begin{subfigure}[b]{0.31\linewidth}
            \includegraphics[width=\textwidth]{images/laplace_case/u_L5_step2.pdf}
            \caption{Step $2$.}
        \end{subfigure}\hfill
        \begin{subfigure}[b]{0.31\linewidth}
            \includegraphics[width=\textwidth]{images/laplace_case/u_L5_step4.pdf}
            \caption{Step $4$.}
        \end{subfigure}
        \\[6pt]
        \begin{subfigure}[b]{0.31\linewidth}
            \includegraphics[width=\textwidth]{images/laplace_case/u_L5_step6.pdf}
            \caption{Step $6$.}
        \end{subfigure}\hfill
        \begin{subfigure}[b]{0.31\linewidth}
            \includegraphics[width=\textwidth]{images/laplace_case/u_L5_step8.pdf}
            \caption{Step $8$.}
        \end{subfigure}\hfill
        \begin{subfigure}[b]{0.31\linewidth}
            \includegraphics[width=\textwidth]{images/laplace_case/u_L5_step9.pdf}
            \caption{Step $9$.}
        \end{subfigure}
    \end{minipage}\hfill
    \begin{minipage}[c]{0.14\textwidth}
        \centering
        \includegraphics[height=5.2cm]{images/laplace_case/u_colorbar_L5.pdf}
    \end{minipage}
    \caption{\robert{Adaptive sweep at $L = 5$: the magnitude $\vvert{\Vec{u}^n_{\mathrm{CL}}}$ on the finest mesh (viridis) with the subdivision partition $\C^n_{\mathrm{CL}}$ overlaid as a thin grey wireframe, at selected steps $n$. Step by step the loop concentrates refinement tightly on the circular layer where $\vvert{\Vec{u}_{\mathrm{CL}}}$ is steep, leaving the smooth interior and exterior coarse. The colour range is fixed from the final, most-converged step (shared colorbar, right), so that under-resolved early steps do not blow out the scale.}}
    \label{fig:cl_solution}
\end{figure}

\paragraph{The DoF saving grows with $L$.} \robert{Figure~\ref{fig:cl_convergence} (left) plots the true $L^2$ error against $n_1$ on log--log axes. The three adaptive runs sit on a single convergence envelope, while each uniform reference (hollow square, with a dotted floor at its error) lies far to the right at the same accuracy: the horizontal gap \emph{is} the DoF saving, $3.3\times \to 5.0\times \to 6.2\times$, growing with $L$. We compare in the true $L^2$ error rather than in $\eta$ because $\eta$ is not comparable across $L$ (it carries the finest cell size $h_\L = h_0/2^L$ in its scaling, see below), whereas the $L^2$ error is a genuine norm with no finest-mesh normalisation --- it is identical at step $0$ for every $L$, as it must be.}

\robert{The adaptive $L^2$ error floors slightly \emph{above} the uniform value at the same $L$, and this ``$L^2$ gap'' widens with $L$ (Table~\ref{tab:cl_headline}, last column). This is not a solver defect but the exact signature of marking in the energy norm while measuring in $L^2$, and it decomposes cleanly. Split the domain into the \emph{ring} annulus, where the $\tanh$ layer lives, and the smooth \emph{bulk}. Their supports are disjoint, so the squared $L^2$ error adds in quadrature, $\vvert{u_h - u}^2 = \int_{\mathrm{ring}} \vvert{e}^2 + \int_{\mathrm{bulk}} \vvert{e}^2 = R^2 + B^2$. Both schemes drive the ring to level $L$, so they share the same ring error $R$; uniform also resolves the bulk ($B \approx 0$), so $\vvert{u_h - u}_{\mathrm{unif}} \approx R$, while adaptive leaves the bulk coarse, so $\vvert{u_h - u}_{\mathrm{adap}} = \sqrt{R^2 + B^2}$. Backing the bulk term $B = \sqrt{\text{adaptive}^2 - \text{uniform}^2}$ out of the measured errors (Table~\ref{tab:cl_decomp}) shows that $B$ is essentially \emph{constant} at $\approx 3.5\cdot10^{-4}$ across $L$ --- energy-based marking never sees enough error \emph{density} in the smooth bulk to refine it, so the bulk floor is $L$-independent --- while the uniform (ring) error $R$ halves per level. The gap is therefore $\sqrt{R^2 + B^2}/R = \sqrt{1 + (B/R)^2}$ with $B$ fixed and $R$ shrinking, so it must \emph{diverge} with $L$; the value $2.02$ at $L = 5$ is merely $R$ passing through $\approx 0.57\,B$. A goal-oriented estimator targeting the $L^2$ functional would refine the bulk and close the gap; the energy-optimal estimator used here does not, so this is a \emph{DoF/memory} claim at matched \emph{estimator} accuracy, with the $L^2$ gap explained rather than hidden.}

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/laplace_case/fig_l2_vs_dofs.pdf}
        \caption{Error versus DoFs.}
        \label{fig:cl_l2_vs_dofs}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/laplace_case/fig_estimator_saturation.pdf}
        \caption{Estimator saturation.}
        \label{fig:cl_saturation_fig}
    \end{subfigure}
    \caption{\robert{(a) True $L^2$ error against active $1$-form DoFs $n_1$ for the adaptive runs (solid, one envelope) and uniform references (hollow squares) at $L = 3, 4, 5$; the horizontal gap at matched accuracy is the DoF saving. (b) Total estimator $\eta$ (dashed) and its refineable part (solid, the contribution from faces below the finest level $L$) against $n_1$; the refineable part collapsing below the total is the signature of hitting the $L$-ceiling.}}
    \label{fig:cl_convergence}
\end{figure}

\begin{table}[htbp]
    \centering
    \small
    \begin{tabular}{@{}r rrr rr@{}}
        \toprule
        $L$ & uniform $R$ & adaptive $\sqrt{R^2+B^2}$ & recovered $B$ & $B/R$ & gap \\
        \midrule
        3 & $8.34\cdot10^{-4}$ & $9.07\cdot10^{-4}$ & $\mathbf{3.57\cdot10^{-4}}$ & $0.43$ & $1.09$ \\
        4 & $3.70\cdot10^{-4}$ & $5.16\cdot10^{-4}$ & $\mathbf{3.60\cdot10^{-4}}$ & $0.97$ & $1.39$ \\
        5 & $2.00\cdot10^{-4}$ & $4.03\cdot10^{-4}$ & $\mathbf{3.51\cdot10^{-4}}$ & $1.76$ & $2.02$ \\
        \bottomrule
    \end{tabular}
    \caption{\robert{Ring/bulk decomposition of the $L^2$ gap. The recovered bulk error $B = \sqrt{\text{adaptive}^2 - \text{uniform}^2}$ is constant to $\sim 1.5\%$ across $L$ (the $L$-independent bulk floor), while the ring error $R$ halves per level; the gap $\sqrt{1 + (B/R)^2}$ therefore grows without bound. That $B$ is constant is strong evidence the ring/bulk split, not an interface or subdivision-basis artifact, is the real mechanism.}}
    \label{tab:cl_decomp}
\end{table}

\paragraph{Estimator saturation and a stopping criterion.} \robert{Figure~\ref{fig:cl_convergence} (right) plots, for each $L$, the total estimator $\eta$ against its refineable part --- the contribution from faces not yet at the finest level $L$. As the sweep exhausts the depth-$L$ ceiling, the refineable part collapses below the total: the remaining error sits at level $L$ and cannot be refined further. We stop the loop on the dimensionless \emph{refineable energy fraction}
\begin{equation}
    \rho = \frac{\text{refineableEtaSq}}{\text{totalEtaSq}}, \qquad \text{stop when } \rho < \tau,\ \tau = 0.05,
\end{equation}
where the numerator is the estimated error energy on faces below level $L$ --- precisely the energy Dörfler can still act on. This is the right metric for three reasons: it is exactly the quantity the plot tracks; it is computable (no exact solution needed, unlike the $L^2$ error); and it is dimensionless, so the same $\tau$ works across $L$ --- unlike the absolute $\eta$, whose $L$-dependent floor makes no single tolerance portable. The $L = 3$ run illustrates it: $\rho$ holds at $1.0$ for steps $0$--$2$ (nothing at level $L$ yet), then drops $0.471 \to 0.116 \to 0.028$ and stops at step $5$.}

\robert{But $\tau = 0.05$ is deliberately conservative. Once the refineable part has decoupled from the total ($\rho$ first falls below $1$), the true $L^2$ error is already essentially converged; the remaining steps buy almost nothing. Reading the stopping step off the same trajectories for a range of tolerances (Table~\ref{tab:cl_tol}, all computable from one run --- no re-solve), the $L^2$ error at $\tau = 0.3$ is within $1.5\%$ of the fully-saturated value for every $L$. The strict tail hurts most exactly where it is most expensive: for $L = 5$, tightening $\tau$ from $0.3$ to $0.05$ adds $+10\,259$ DoFs ($+60\%$) and $\approx 5.4$ hours of solve time for a $1.4\%$ $L^2$ gain, because the marginal faces it refines carry vanishing error. A tolerance $\tau \approx 0.2\text{--}0.3$ --- stopping once the refineable part has clearly decoupled rather than nearly vanished --- is the better operating point, sharpening the DoF saving to $3.5\times / 5.8\times / 9.9\times$. The absolute vertical offset between the $L$ curves in Figure~\ref{fig:cl_convergence} (right) is largely normalisation ($\eta \sim h_\L$), not accuracy, so each panel is read \emph{within} its $L$; cross-$L$ accuracy is the job of Figure~\ref{fig:cl_convergence} (left).}

\begin{table}[htbp]
    \centering
    \small
    \begin{tabular}{@{}r ccc@{}}
        \toprule
        $L$ & $\tau = 0.3$ & $\tau = 0.2$ & $\tau = 0.05$ (as run) \\
        \midrule
        3 & step $4$, $n_1\,3\,080$, $\mathbf{3.5\times}$ & step $4$, $3\,080$, $3.5\times$ & step $5$, $3\,230$, $3.3\times$ \\
        4 & step $6$, $n_1\,7\,329$, $\mathbf{5.8\times}$ & step $6$, $7\,329$, $5.8\times$ & step $7$, $8\,520$, $5.0\times$ \\
        5 & step $7$, $n_1\,17\,060$, $\mathbf{9.9\times}$ & step $8$, $22\,942$, $7.4\times$ & step $9$, $27\,319$, $6.2\times$ \\
        \bottomrule
    \end{tabular}
    \caption{\robert{Stopping step, DoF count, and DoF saving as the saturation tolerance $\tau$ is varied (read from the as-run trajectories, no re-solve). The looser $\tau \approx 0.2\text{--}0.3$ recovers nearly all the accuracy --- within $1.5\%$ of the saturated $L^2$ error --- at a markedly better DoF saving, avoiding the runaway final solves of the strict tail.}}
    \label{tab:cl_tol}
\end{table}

\paragraph{Padding overhead is lower-dimensional.} \robert{The admissibility padding is a frontier overhead, not an area cost. Table~\ref{tab:padding} lists the active $1$-form DoFs, Dörfler-marked faces, and padded faces per step. Its worst-case size does grow with $L$ (max padded per step $11 \to 22 \to 120$ at $L = 3, 4, 5$) because the padding lives on the \emph{interface} around the refined region, whose length scales like the finest-mesh circumference ($\sim 2^L$), not on its area. Measured against what it pads it stays small: even at $L = 5$ the $120$ padded faces sit against $1\,177$ marked faces ($\approx 10\%$) and $27\,319$ $1$-form DoFs ($< 0.5\%$), and padding converges in a single cycle throughout the whole sweep (pinch resolution and inadmissible-faces padding and grading reach a fixed point in one pass). Padding is thus an $O(\text{interface})$ cost --- one dimension below the $O(\text{area})$ cost of the refinement it supports --- with a fixed one-pass overhead; it is a table, not a figure, because the overhead never approaches the leading DoF cost.}

\begin{table}[t]
  \centering
  \caption{\robert{Padding overhead over the adaptive sweep. For each refinement depth $L$ and step, $n_1$ is the number of active 1-form DoFs, `marked' the number of D\"orfler-marked faces, and `padded' the faces added by admissibility padding. Padding stays $O(1)$ while $n_1$ grows by orders of magnitude. (Padding always converged in a single cycle.)}}
  \label{tab:padding}
  \begin{tabular}{r r r r r}
    \toprule
    $L$ & step & $n_1$ & marked & padded \\
    \midrule
    3 & 0 & 181 & 34 & 2 \\
    3 & 1 & 483 & 99 & 4 \\
    3 & 2 & 1302 & 158 & 11 \\
    3 & 3 & 2585 & 74 & 6 \\
    3 & 4 & 3080 & 28 & 4 \\
    3 & 5 & 3230 & 0 & 0 \\
    \midrule
    4 & 0 & 181 & 32 & 2 \\
    4 & 1 & 474 & 100 & 4 \\
    4 & 2 & 1285 & 149 & 8 \\
    4 & 3 & 2442 & 180 & 5 \\
    4 & 4 & 3871 & 329 & 3 \\
    4 & 5 & 6158 & 215 & 7 \\
    4 & 6 & 7329 & 239 & 22 \\
    4 & 7 & 8520 & 0 & 0 \\
    \midrule
    5 & 0 & 181 & 34 & 2 \\
    5 & 1 & 483 & 85 & 7 \\
    5 & 2 & 1189 & 164 & 15 \\
    5 & 3 & 2493 & 216 & 5 \\
    5 & 4 & 4176 & 260 & 6 \\
    5 & 5 & 6197 & 638 & 30 \\
    5 & 6 & 10713 & 1095 & 40 \\
    5 & 7 & 17060 & 1177 & 101 \\
    5 & 8 & 22942 & 872 & 120 \\
    5 & 9 & 27319 & 0 & 0 \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Patch versus interface DoFs.} \robert{Figure~\ref{fig:cl_patch_interface} splits, per $L$ and step, each subdivision level's $1$-form DoFs into their \emph{patch} (native) part --- outlined in black --- and their \emph{interface} part, left unboxed. Interface DoFs are the overhead of making the active basis conforming across the coarse/fine frontier (the frontier loop is also the seat of the loop-monodromy spurious harmonic addressed by padding). The figure shows this overhead stays a modest share of the patch DoFs and that refinement progressively shifts DoFs onto finer levels as the sweep proceeds.}

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[b]{0.32\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/laplace_case/fig_patch_interface_L3.pdf}
        \caption{$L = 3$.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.32\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/laplace_case/fig_patch_interface_L4.pdf}
        \caption{$L = 4$.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.32\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/laplace_case/fig_patch_interface_L5.pdf}
        \caption{$L = 5$.}
    \end{subfigure}
    \caption{\robert{Active $1$-form DoFs over the adaptive steps, stacked by subdivision level. Each level's bar is split into a patch (native, black outline) and an interface (unboxed) part. The interface overhead of conformity stays modest and the DoF mass shifts onto finer levels as the sweep progresses.}}
    \label{fig:cl_patch_interface}
\end{figure}

\paragraph{Solve cost does not follow the DoF saving.} \robert{The one honest cost is the linear solver. Figure~\ref{fig:cl_minres} reports MINRES iterations (bars) and pure solve time (twin axis) over the sweep. Wall-clock \emph{grows} over the sweep and is dramatically worse than uniform despite far fewer DoFs: adaptive $L = 5$ takes $\approx 3.2$ hours and $668$ iterations at $27\,319$ DoFs, while uniform $L = 5$ takes $46$ seconds and $21$ iterations at $169\,472$ DoFs --- a $6\times$ DoF saving turned into a $250\times$ slowdown. The measured time is the MINRES \texttt{solve} call only (not dofset construction, assembly, or the preconditioner factorisation), so it is genuine solve cost, from two compounding causes: the block-diagonal preconditioner degrades on irregular adaptive meshes, and each iteration gets more expensive because the Galerkin-projected operators \emph{densify} --- the subdivision basis functions are globally supported, so the projection fills far more nonzeros per row than a nodal matrix of the same DoF count, whereas the uniform operators stay sparse and well-conditioned ($\approx 20$ iterations). The adaptivity win here is thus in DoFs and memory, not wall-clock, with the current solver; the preconditioner is the bottleneck and a subdivision-compatible multigrid variant is left for future work (which reinforces the case for the looser $\tau$ above).}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.75\textwidth]{images/laplace_case/fig_minres_cost_all.pdf}
    \caption{\robert{MINRES iteration count over the adaptive sweep, grouped across $L = 3, 4, 5$. The iteration count climbs as the block-diagonal preconditioner degrades on irregular adaptive meshes and the globally supported subdivision basis densifies the Galerkin-projected operators; the uniform references (not shown) converge in $\approx 20$ iterations. The DoF saving is a memory saving, not (yet) a wall-clock one.}}
    \label{fig:cl_minres}
\end{figure}

\robert{In summary, on a manufactured problem with a sharp circular layer and a known exact solution, adaptive subdivision refinement reaches the accuracy of a uniform level-$L$ solve at a small fraction of its DoFs, and the saving grows with depth ($3.3\times, 5.0\times, 6.2\times$ to saturation; $3.5\times, 5.8\times, 9.9\times$ at a looser stop). The refineable/total estimator split identifies when a depth $L$ is exhausted and yields a portable, computable stopping criterion; the admissibility padding costs only $O(\text{interface})$ extra faces per step; and the conformity interface DoFs are a modest, bounded overhead. The one honest cost is the solver: because the projected operators densify under the globally supported basis and the block preconditioner weakens on irregular meshes, the DoF saving is a memory saving rather than, yet, a wall-clock one.}

\section{Conclusion}

\robert{We have presented a complete matrix-level framework for hierarchical subdivision $k$-form spaces and their use in an adaptive loop for structure-preserving PDE discretisations. The central observation is that any active subdivision basis function is a column of an accumulated subdivision matrix $\Smat{k}{\l}{\L}$; selecting columns according to a subdivision partition produces a hierarchical space $\hierSDFspace{k}{\C}{\L}$ whose linear independence and de Rham exactness are guaranteed by the partition condition proved in the companion paper~\cite{Piel.2026}. Once the hierarchical subdivision matrix $\Amat{k}{\activeDoFs}{\L}$ is assembled, all bilinear forms are obtained by Galerkin restriction of the embedding-level operators without recomputing any integrals, and the active-space linear system reduces to standard sparse linear algebra on the finest mesh $\T_\L$.

The adaptive loop adds a subdivision-specific \textsc{Pad} step to the standard \textsc{Solve}--\textsc{Estimate}--\textsc{Mark}--\textsc{Refine} cycle. Padding (Algorithm~\ref{alg:padding}) alternates pinch resolution with inadmissible-faces padding and grading to a fixed point: pinch resolution repairs a pinched coarse region that would otherwise carry a spurious harmonic (a closed but non-exact discrete $1$-form, breaking de Rham exactness), while inadmissible-faces padding and grading enforce the interior-vertex (Good-vertex) requirement and bound the level jump at coarse/fine interfaces. Both rules only ever refine, are local, and are inexpensive relative to the linear solve.

The numerical experiments confirm the two key structural properties of the framework. For the Maxwell eigenvalue problem, exact subdivision partitions reproduce the reference FEEC spectrum without spurious modes; inexact ones introduce them. This serves as a direct diagnostic for the correctness of the active-DoF construction and the de Rham complex structure. For the mixed vector Laplace equation, the adaptive loop concentrates refinement at the location of steep gradients and achieves convergence behaviour consistent with the hierarchical approximation theory.

Several directions remain open. The precise correspondence between the subdivision PAD step and the $L$-chain completion used in hierarchical B-spline AFEM~\cite{Cabanas.2026} has not been established; clarifying this connection would relate the two frameworks and potentially enable convergence proofs to be transferred. On the solver side, the block-diagonal LDLT preconditioner used here is effective for the problem sizes considered, but a multigrid preconditioner that respects the globally supported subdivision basis would reduce the per-step cost for larger problems; this requires a prolongation operator compatible with $\Amat{k}{\activeDoFs}{\L}$ and is left for future work. Finally, a promising direction is to adopt the overlapping patch framework of Bressan~\cite{Bressan.2024} in place of the non-overlapping subdivision partition used here. Overlapping patches can provide a richer set of admissible active-DoF configurations and may admit a more flexible PAD step, at the cost of requiring a partition-of-unity treatment to maintain linear independence; adapting the matrix-level assembly and the de Rham complex to that setting is an open problem.}

\bibliography{references}

\appendix

\section{Active-DoF assembly pseudo-code}
\label{app:active_dof_pseudocode}

\robert{This appendix gives the full active-DoF assembly routine
$\textsc{BuildActiveDoFs}$ whose role in Section~\ref{sec:construction_of_hierarchical_subdiv_spaces}
is described around Lemma~\ref{lemma:active_k_form_dofs_per_level}.
Algorithm~\ref{alg:buildactivedofs} produces the interface DoFs by a frontier sweep
(steps~(a)--(d)) that maintains the
set of interior edges separating resolved (coarse-side) faces from
unresolved (finer) regions. Each level the frontier is split into child
edges and interface vertices, the vertices emit the DoFs they introduce on
the following level. The ordering of operations within each level is fixed:
old frontier edges are consumed first, new ones are spawned, DoFs are
emitted from the previous level's vertices, and finally the frontier is
split for the next level.}

\begin{algorithm}[H]
\color{orange}
\caption{$\textsc{BuildActiveDoFs}(\C)$: active degrees of freedom from a subdivision partition}
\label{alg:buildactivedofs}
\BlankLine
\tcp{1.\ Native DoFs}
$\D^2 \leftarrow \C$\;
\For{$\l = 0, \, \ldots, \, \L$}{
    $\D^0[\l] \leftarrow \emptyset$\;
    $\D^1[\l] \leftarrow \emptyset$\;
    \For{each $f \in \C[\l]$}{
        $\D^0[\l] \leftarrow \D^0[\l] \cup \mathrm{verts}(f)$\;
        $\D^1[\l] \leftarrow \D^1[\l] \cup \mathrm{edges}(f)$\;
    }
}
\BlankLine
\tcp{2.\ Interface DoFs (frontier sweep)}
$\mathrm{frontier} \leftarrow \emptyset$\;
$\mathrm{pendingVerts} \leftarrow \emptyset$\;
\For{$\l = 0, \, \ldots, \, \L$}{
    \tcp{(a) consume frontier edges whose fine side is now resolved at level $\l$}
    $\mathrm{frontier} \leftarrow \{ e \in \mathrm{frontier} : \text{fine-side faces of } e \notin P[\l] \}$\;
    \tcp{(b) spawn new frontier edges: interior edges of $\mathcal{F}_\l$ with exactly one adjacent face in $P[\l]$}
    $\mathrm{frontier} \leftarrow \mathrm{frontier} \cup \textsc{FindInterfaceEdges}(\l, \D)$\;
    \tcp{(c) emit DoFs from interface vertices spawned at level $\l - 1$}
    \For{each $v \in \mathrm{pendingVerts}$}{
        $\D^0[\l] \leftarrow \D^0[\l] \cup \{v\}$\;
        $\D^1[\l] \leftarrow \D^1[\l] \cup \mathrm{activeEdges}(v)$\;
        $\D^2[\l] \leftarrow \D^2[\l] \cup \mathrm{activeFaces}(v)$\;
    }
    $\mathrm{pendingVerts} \leftarrow \emptyset$\;
    \tcp{(d) split frontier edges into child edges and interface vertices for level $\l + 1$}
    \If{$\l < \L$}{
        $\mathrm{newFrontier} \leftarrow \emptyset$;\quad $\mathrm{newVerts} \leftarrow \emptyset$\;
        \For{each $e \in \mathrm{frontier}$}{
            $(e_1, e_2, v_{\mathrm{mid}}) \leftarrow \textsc{SplitEdge}(e)$ at level $\l + 1$\;
            $\mathrm{newFrontier} \leftarrow \mathrm{newFrontier} \cup \{e_1, e_2\}$;\quad
            $\mathrm{newVerts} \leftarrow \mathrm{newVerts} \cup \{v_{\mathrm{mid}}\}$\;
        }
        \For{each endpoint $v$ of edges in $\mathrm{frontier}$, taken once per $v$}{
            \lIf{$v$ borders a refined region}{$\mathrm{newVerts} \leftarrow \mathrm{newVerts} \cup \{v\}$}
        }
        $\mathrm{frontier} \leftarrow \mathrm{newFrontier}$;\quad
        $\mathrm{pendingVerts} \leftarrow \mathrm{newVerts}$\;
    }
}
\Return $\D$\;
\end{algorithm}

\section{Full marking and padding pseudo-code}
\label{app:full_pseudocode}

\robert{This appendix gives the full versions of the marking and padding routines
whose condensed forms appear in Section~\ref{subsec:worked_example}
(Alg.~\ref{alg:Dörfler}, \ref{alg:refine}, \ref{alg:padding}, \ref{alg:pinch}, \ref{alg:condI}).}

\robert{\begin{algorithm}[H]
\caption{$\textsc{DörflerMarkPerVertex}(P, \eta, \theta)$: full per-vertex-star marking}
\label{alg:Dörfler_full}
\KwIn{partition $P$, per-face indicator $\eta_f$ for active $f$, $\theta \in (0,1]$}

$\mathrm{selected} \leftarrow [\,]$;\quad $\mathrm{acc} \leftarrow 0$;\quad $\mathrm{marked}[\l] \leftarrow \emptyset$\;
\While{$\mathrm{acc} < \theta E$ \textbf{and} $\mathrm{PQ}$ nonempty}{
    $(\_, \l, v) \leftarrow \mathrm{PQ}.\mathrm{pop}()$;\quad $\mathrm{selected}.\mathrm{append}(\l, v)$\;
    \For{each face $f \in \mathrm{star}(v)$ with $f \in P[\l]$, $f \notin \mathrm{marked}[\l]$}{
        $\mathrm{marked}[\l] \leftarrow \mathrm{marked}[\l] \cup \{f\}$;\quad $\mathrm{acc} \mathrel{+}= \eta_f^2$\;
    }
}
\tcp{APPLICATION --- refine each selected star}
\lFor{$(\l,v) \in \mathrm{selected}$}{$T[v] \leftarrow \min(\text{finest active level of } \mathrm{star}_P(v) + 1,\ \L)$}
$P' \leftarrow$ copy of $P$\;
\For{$(\l,v) \in \mathrm{selected}$ sorted ascending by $(\l, v)$}{
    \For{each face $f$ of $v$'s one-ring at level $T[v]$}{
        \lIf{$f$ already active at level $\geq T[v]$}{\textbf{continue}}
        grade $f$'s covering active ancestor up to level $T[v]$ (one level at a time)\;
    }
}
\Return $\textsc{Pad}(P')$\;
\end{algorithm}}

\newpage

\robert{\begin{algorithm}[H]
\caption{$\textsc{ResolveVertexPinches}(P)$: full pinch resolution}
\label{alg:pinch_full}
\Repeat{a pass refines nothing}{
    \tcp{Phase 1 --- detect (no mutation)}
    $\mathrm{pinches} \leftarrow \emptyset$\;
    \For{each vertex $v$}{
        $\L_v \leftarrow v$'s finest active level\;
        \lIf{any $\L_v$-ring face is covered only by a finer face}{skip $v$}
        classify each face of $v$'s $\L_v$ one-ring: \textsc{fine} $=$ active at $\L_v$; \textsc{coarse} $=$ covered by an active ancestor at level $< \L_v$\;
        $\mathrm{cut} \leftarrow$ number of $\L_v$-spokes with exactly one \textsc{fine} incident face (for boundary $v$, close the fan by treating the exterior as one \textsc{coarse} gap)\;
        \lIf{$\mathrm{cut} \geq 4$}{$\mathrm{pinches} \leftarrow \mathrm{pinches} \cup \{(\L_v, v)\}$ \tcp*{$\geq 2$ arcs, Eq.~\eqref{eq:arc_count}}}
    }
    \tcp{Phase 2 --- refine}
    \For{$(\L_v, v) \in \mathrm{pinches}$}{
        \For{each \textsc{coarse} face $f$ of $v$'s $\L_v$-ring}{
            grade $f$'s covering active ancestor up to level $\L_v$ (one level at a time along the chain; deferred, erase-guarded)\;
        }
    }
}
\Return $P$\;
\end{algorithm}}

\newpage

\robert{\begin{algorithm}[H]
\caption{$\textsc{PadPartitionPerVertexGraded}(P)$: full inadmissible-faces padding with grading}
\label{alg:condI_full}
\tcp{$\mathrm{covered}(g,\l)$: $g$ or a coarser ancestor of $g$ is active}
\tcp{$\mathrm{Good}(w,\l)$: every face in $w$'s one-ring is $\mathrm{covered}(\cdot,\l)$}
\tcp{Phase 1 --- admissibility (condition I) to a fixed point}
\Repeat{no face is refined this pass}{
    $\mathrm{toRefine} \leftarrow \emptyset$\;
    \For{each level $\l$, each active level-$\l$ face $f$}{
        \lIf{no vertex of $f$ is $\mathrm{Good}(\cdot,\l)$}{$\mathrm{toRefine} \leftarrow \mathrm{toRefine} \cup \{f\}$}
    }
    apply $\mathrm{toRefine}$: each face $\to$ its $4$ children one level deeper\;
}
\tcp{Phase 2 --- grading: span $\leq \mathrm{maxSpan}$ levels per vertex}
\Repeat{$\mathrm{toRefine} = \emptyset$}{
    $\mathrm{toRefine} \leftarrow \emptyset$\;
    \For{each vertex $v$}{
        $\mathrm{lo}, \mathrm{hi} \leftarrow \min, \max$ active level among $v$'s active faces\;
        \lIf{$\mathrm{hi} - \mathrm{lo} > \mathrm{maxSpan}$}{$\mathrm{toRefine} \leftarrow \mathrm{toRefine} \cup \{v\text{'s active faces at level } \mathrm{lo}\}$}
    }
    \If{$\mathrm{toRefine} \neq \emptyset$}{
        apply $\mathrm{toRefine}$;\quad re-run Phase 1\;
    }
}
\Return $P$\;
\end{algorithm}}

\section{Atomic-test figure gallery}
\label{app:figure_gallery}

\robert{This gallery collects the remaining atomic subdivision-partition test cases
that are not reproduced in the main text (Section~\ref{subsec:exactness_atomic}).
Each case is shown as an input/padded pair; the corresponding cohomology of the
richer cases is quantified in Table~\ref{tab:atomic-cohomology}. The
\texttt{v10} trio (Fig.~\ref{fig:gallery_v10}) stress-tests the coarser-gap guard
in the pinch-arc detector, and is included for completeness as supplementary
material.}

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v10_last3_200_input.pdf}\caption*{200 in}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v10_last3_200_padded.pdf}\caption*{200 pad}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v10_last3_211_input.pdf}\caption*{211 in}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v10_last3_211_padded.pdf}\caption*{211 pad}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v10_mixed_level_arcs_input.pdf}\caption*{mixed in}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v10_mixed_level_arcs_padded.pdf}\caption*{mixed pad}\end{subfigure}
    \caption{\robert{Valence-$10$ detector-guard cases: \texttt{v10\_last3\_200} (resolves), \texttt{v10\_last3\_211} (deliberately slips through the guard), \texttt{v10\_mixed\_level\_arcs} (resolves).}}
    \label{fig:gallery_v10}
\end{figure}

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_3rings_middle_refined_input.pdf}\caption*{3r-mid in}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_3rings_middle_refined_padded.pdf}\caption*{3r-mid pad}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_4rings_inner_outer_refined_input.pdf}\caption*{4r-io in}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_4rings_inner_outer_refined_padded.pdf}\caption*{4r-io pad}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_4rings_third_coarse_input.pdf}\caption*{4r-3rd in}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_4rings_third_coarse_padded.pdf}\caption*{4r-3rd pad}\end{subfigure}
    \caption{\robert{Further ring cases: $3$-ring middle-refined, $4$-ring inner$+$outer refined, and $4$-ring third-coarse partitions.}}
    \label{fig:gallery_rings}
\end{figure}

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_boundary_lvl1_input.pdf}\caption*{bd-1 in}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_boundary_lvl1_padded.pdf}\caption*{bd-1 pad}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_boundary_lvl2_input.pdf}\caption*{bd-2 in}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_boundary_lvl2_padded.pdf}\caption*{bd-2 pad}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_leftright_lvl1_input.pdf}\caption*{lr-1 in}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_leftright_lvl1_padded.pdf}\caption*{lr-1 pad}\end{subfigure}
    \\[6pt]
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_leftright_lvl2_input.pdf}\caption*{lr-2 in}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_leftright_lvl2_padded.pdf}\caption*{lr-2 pad}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_refine_center_input.pdf}\caption*{ctr in}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_refine_center_padded.pdf}\caption*{ctr pad}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_nested_center_lvl3_input.pdf}\caption*{nest in}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v6_nested_center_lvl3_padded.pdf}\caption*{nest pad}\end{subfigure}
    \caption{\robert{Boundary, left/right, single-centre, and nested-centre partitions (valence $6$).}}
    \label{fig:gallery_boundary_misc}
\end{figure}

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v8_quarters_lvl1_input.pdf}\caption*{v8-1 in}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v8_quarters_lvl1_padded.pdf}\caption*{v8-1 pad}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v9_cff_lvl1_input.pdf}\caption*{v9-1 in}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v9_cff_lvl1_padded.pdf}\caption*{v9-1 pad}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v9_cff_lvl2_input.pdf}\caption*{v9-2 in}\end{subfigure}\hfill
    \begin{subfigure}[b]{0.16\textwidth}\centering\includegraphics[width=\textwidth]{images/exactness/v9_cff_lvl2_padded.pdf}\caption*{v9-2 pad}\end{subfigure}
    \caption{\robert{Valence-$8$ single-level quarters and valence-$9$ mixed-arc (\texttt{cff}) partitions.}}
    \label{fig:gallery_v8v9}
\end{figure}

\FloatBarrier

\end{document}
\documentclass[a4paper, 12pt]{article}
\usepackage[margin=2.5cm]{geometry}
%\usepackage{geometry}[2cm,2cm,2cm,2cm]
\usepackage[utf8]{inputenc}
\usepackage[nosumlimits]{amsmath}
\usepackage{amssymb,stmaryrd,amsthm,MnSymbol}
\usepackage{amsfonts,amssymb,amsopn}
 \usepackage[colorlinks=true,
            linkcolor=red,
            filecolor=red,
            citecolor=blue,
            urlcolor=blue,]{hyperref}
\usepackage[dvipsnames]{xcolor}
\usepackage{bbold}
\usepackage{phoenician}

\usepackage{comment}
\usepackage{graphicx}
\usepackage{mathtools}
\usepackage{multicol}
\usepackage{tikz, tikz-cd}
\usetikzlibrary{arrows.meta, decorations.pathreplacing, calligraphy, positioning}
\usepackage{adjustbox}

\usepackage{aligned-overset}
\usepackage{enumerate}
\usepackage[ruled]{algorithm2e}
\usepackage{wrapfig}
\usepackage{placeins}

\usepackage{nicematrix}

\usepackage{caption, subcaption}
% for basis function plots
\newlength{\twosubht}
\newsavebox{\twosubbox}

\usepackage{natbib}
\let\OLDthebibliography\thebibliography
\renewcommand\thebibliography[1]{
  \OLDthebibliography{#1}
  \setlength{\parskip}{1pt}
  \setlength{\itemsep}{0pt plus 0.3ex}
}
\bibliographystyle{elsarticle-harv}

\usepackage{multirow}

%opening
\title{Hierarchical Subdivision $k$-Form Spaces on the Matrix Level}
\author{Robert Piel\footnote{University of Surrey, School of Mathematics and Physics, UK (r.piel@surrey.ac.uk), ORCID: \url{https://orcid.org/0009-0001-8681-2493}} \ and
Werner Bauer\footnote{University of Surrey, School of Mathematics and Physics, UK (w.bauer@surrey.ac.uk), ORCID: \url{https://orcid.org/0000-0002-5040-4287}}
}


\newcommand{\todo}[1]{\vspace{5 mm}\par \noindent
	\framebox{\begin{minipage}[c]{0.95 \textwidth}
			\tt #1 \end{minipage}}\vspace{5 mm}\par}
\newcommand{\werner}[1]{{\color{magenta}WB says: #1}}
\newcommand{\robert}[1]{{\color{orange} #1}}
\newcommand{\colin}[1]{{\color{blue}CC says: #1}}
\newcommand{\incl}[1]{{\color{red}#1}}
\newcommand{\checkit}[1]{{\color{red}#1}}
\newcommand{\RobertChanged}[1]{{\color{brown}#1}}
%\renewcommand{\checkit}[1]{{\color{red}#1}}
\newcommand{\tbd}[1]{{\color{red}#1}}
\newcommand{\highlight}[1]{{\color{cyan} #1}}

% colors
\newcommand{\blue}{\color{blue}}
\newcommand{\red}{\color{red}}
\newcommand{\green}{\color{ForestGreen}}

% highlighting
\newcommand{\hlcyan}[1]{{\sethlcolor{cyan}\hl{#1}}}
\newcommand{\hlorange}[1]{{\sethlcolor{orange}\hl{#1}}}

% handling simplices
\newcommand{\T}{\mathcal{T}}
\newcommand{\K}{\mathcal{K}}
\newcommand{\V}{\mathcal{V}}
\newcommand{\E}{\mathcal{E}}
\newcommand{\F}{\mathcal{F}}

\newcommand{\simplexname}{\sigma}
\newcommand{\simplex}[3]{\freeQ{\simplexname}{#1}{#2}{#3}}
\newcommand{\simplexnametwo}{\kappa}
\newcommand{\simplextwo}[3]{\freeQ{\simplexnametwo}{#1}{#2}{#3}}
\newcommand{\simplexsubset}[2]{\freeVec{\Delta}{#1}{#2}}

\newcommand{\vertex}[2]{\freeQ{v}{}{#1}{#2}}
\newcommand{\vertexpos}[2]{\freeQ{\Vec{v}}{}{#1}{#2}}
\newcommand{\edge}[2]{\freeQ{e}{}{#1}{#2}}
\newcommand{\face}[2]{\freeQ{f}{}{#1}{#2}}

% adjacency relations
\newcommand{\KmKnl}[3]{\K^{#1}\K^{#2}_{#3}}
\newcommand{\VFl}[1]{\V\F_{#1}}
\newcommand{\EFl}[1]{\E\F_{#1}}
\newcommand{\FFl}[1]{\F\F_{#1}}
\newcommand{\VEl}[1]{\V\E_{#1}}
\newcommand{\EEl}[1]{\E\E_{#1}}
\newcommand{\FEl}[1]{\F\E_{#1}}
\newcommand{\VVl}[1]{\V\V_{#1}}
\newcommand{\EVl}[1]{\E\V_{#1}}
\newcommand{\FVl}[1]{\F\V_{#1}}

\newcommand{\KKltoL}[3]{\K^{#1}\K^{#1}_{#2 \to #3}}
\newcommand{\VVltoL}[2]{\V\V_{#1 \to #2}}
\newcommand{\EEltoL}[2]{\E\E_{#1 \to #2}}
\newcommand{\FFltoL}[2]{\F\F_{#1 \to #2}}

% indices for components
\newcommand{\coarseIdxone}{{ i}}
\newcommand{\coarseIdxtwo}{{ m}}
\newcommand{\fineIdxone}{{ j}}
\newcommand{\fineIdxtwo}{{ n}}

% indices for levels
\renewcommand{\l}{{\green\ell}}
\renewcommand{\L}{{\green L}}

\newcommand{\lzero}{{\green 0}}
\newcommand{\lone}{{\green {\l'}  }}
\newcommand{\ltwo}{{\green {\l''} }}
\newcommand{\lthree}{{\green {\l'''} }}

% numbers
\newcommand{\nc}{{\color{Magenta} n_c}}
\newcommand{\nr}{{\color{Magenta} n_r}}
\newcommand{\ns}{{\color{Magenta} n_s}}

% refinement stencils
\newcommand{\RSt}{\mathrm{R}}
\newcommand{\cRSt}{\RSt_{c}}
\newcommand{\R}{\mathcal{R}}

% derivative stencil
\newcommand{\DSt}{\mathrm{D}}

% macros that determine the general format
% constrained quantity
\newcommand{\constrQ}[5]{ ( {#1}^{#2}_{#3 \vert #4} )_{#5}} % e.g. \constr_vec{c}{k}{\ell}{L}{i} renders as (c^k_{l|L})_i
% constrained vector (no index i)
\newcommand{\constrVec}[4]{ {#1}^{#2}_{#3 \vert #4} }
% free quantity
\newcommand{\freeQ}[4]{ ( {#1}^{#2}_{#3} )_{#4}}
% free vector (no index i)
\newcommand{\freeVec}[3]{ {#1}^{#2}_{#3}}

% subdiv operators
\newcommand{\Sop}[3]{\mathcal{S}^{#1}_{{#2} \to {#3}}}
\newcommand{\Smat}[3]{\mathbf{\mathrm{S}}^{#1}_{#2 \to #3}}
\newcommand{\Smatij}[5]{\left(\mathbf{\mathrm{S}}^{#1}_{#2 \to #3}\right)_{#4 #5}}

% accumulated subdiv matrix
\newcommand{\Aop}[3]{\mathcal{A}^{#1}_{{#2} \to {#3}}}
\newcommand{\Amat}[3]{\mathbf{\mathrm{A}}^{#1}_{#2 \to #3}}
\newcommand{\Amatij}[5]{\left(\mathbf{\mathrm{A}}^{#1}_{#2 \to #3}\right)_{#4 #5}}

\newcommand{\primalAop}[4]{ {\mathcal{A}^{#1}_{{#2} \to {#3}|{#4} }}  }
\newcommand{\dualAop}[4]{ {\mathcal{B}^{#1}_{{#2}|\,{#3} \rightarrow {#4}} }}

% Whit and Loop, and their Aops
\newcommand{\Whit}{\mathrm{Whit}}
\newcommand{\Loop}{\mathrm{Loop}}
\newcommand{\AopWhit}[2]{\Aop{\Whit}{#1}{#2}}
\newcommand{\AopLoop}[2]{\Aop{\Loop}{#1}{#2}}

% derivative
\newcommand{\extd}{\mathbf{d}}
\newcommand{\auxextd}{\overset{\sim}{\vphantom{a}\smash{\mathbf{d}}}} % auxiliary derivative
\newcommand{\Dmat}[2]{\mathbf{D}^{#1}_{#2}} % derivative matrix
\newcommand{\Dmatij}[4]{\big(\mathbf{D}^{#1}_{#2}\big)_{#3 #4}} % derivative matrix with components

\newcommand{\dd}{\mathrm{d}_d} % discrete exterior derivative
\newcommand{\ddk}[1]{\mathrm{d}_d^{#1}}
\newcommand{\ddkl}[2]{\mathrm{d}_{d,#2}^{#1}}

% FEEC spaces
\newcommand{\FEECspace}[2]{\Lambda^{#1}_{#2}}
\newcommand{\FEECbasis}{\psi}
\newcommand{\FEECbases}{\Psi}
\newcommand{\FEECspaceZero}[2]{\mathring{\Lambda}^{#1}_{#2}}

% subdiv spaces
\newcommand{\SDFspace}[3]{\mathcal{S}\Lambda^{#1}_{#2 \vert #3}}
\newcommand{\SDFbasis}{\phi}
\newcommand{\SDFbases}{\Phi}
\newcommand{\SDFbasesZero}{\mathring{\SDFbases}}
\newcommand{\SDFspaceZero}[3]{\mathring{\mathcal{S}}\Lambda^{#1}_{#2 \vert #3}}

% operators for BCs
\newcommand{\trace}{\mathrm{tr}}
\newcommand{\Vint}{\mathring{\V}}
\newcommand{\Eint}{\mathring{\E}}
\newcommand{\Fint}{\mathring{\F}}
\newcommand{\Kint}{\mathring{\K}^k}
\newcommand{\BCAop}[3]{\mathring{\mathcal{A}} {}^{#1}_{#2 \to #3}}
\newcommand{\BCAmat}[3]{\mathring{\mathrm{A}} {}^{#1}_{#2 \to #3}}
\newcommand{\BCBEop}[4]{\mathring{\mathcal{B}} {}^{#1}_{#2 \vert #3 \to #4}}

% hierarchical spaces
\newcommand{\C}{\heth}
\newcommand{\I}{\mathcal{I}}
\newcommand{\II}{\mathbb{I}}
\newcommand{\calP}{\mathcal{P}}
\newcommand{\heth}{\textphnc{H}}

\newcommand{\activeDoFs}{\mathbb{A}}
\newcommand{\inactiveDoFs}{\mathbb{I}}
\newcommand{\D}{\activeDoFs}
\newcommand{\activeDoFSet}[2]{\activeDoFs^{#1}_{#2}}
\newcommand{\activeSDFspaces}[2]{\mathcal{S}\activeDoFSet{#1}{#2}}

\newcommand{\hierSDFspace}[3]{\mathcal{S}\activeDoFs^{#1}_{#2 \vert #3}}
\newcommand{\inactiveSDFspace}[3]{\mathcal{S}\inactiveDoFs^{#1}_{#2 \vert #3}}
\newcommand{\coarseInactiveSDFspace}[3]{\mathcal{S}\inactiveDoFs^{#1, \mathrm{c}}_{#2 \vert #3}}
\newcommand{\fineInactiveSDFspace}[3]{\mathcal{S}\inactiveDoFs^{#1, \mathrm{f}}_{#2 \vert #3}}

% general maths
\renewcommand{\powerset}{\mathbb{P}}
\newcommand{\inclusion}{\iota}
\newcommand{\auxincl}{\overset{\sim}{\vphantom{a}\smash{\iota}}} % auxiliary derivative
\newcommand{\support}{\mathrm{supp}}
\newcommand{\pfrac}[2]{\frac{\partial #1}{\partial #2}}
\newcommand{\M}{\mathcal{M}}
\newcommand{\vvert}[1]{\left\vert #1 \right\vert}
\newcommand{\dimlk}[2]{\big\vert \K^{#2}_{#1} \big\vert}
\newcommand{\sumdimlk}[2]{\vert \K^{#2}_{#1} \vert}
\newcommand{\closure}[1]{\overset{\circ}{#1} {}}
\renewcommand{\complement}{\mathsf{c}}

% widecheck and widehat
\DeclareFontFamily{U}{mathx}{}
\DeclareFontShape{U}{mathx}{m}{n}{<-> mathx10}{}
\DeclareSymbolFont{mathx}{U}{mathx}{m}{n}
\DeclareMathAccent{\widehat}{0}{mathx}{"70}
\DeclareMathAccent{\widecheck}{0}{mathx}{"71}

% macros for specific spaces
\newcommand{\CG}{\mathrm{CG}}
\newcommand{\NED}{\mathrm{NED}}
\newcommand{\DG}{\mathrm{DG}}
\newcommand{\Hcurl}{H(\text{curl})}
\newcommand{\HcurlM}[1]{H\left(\text{curl}, #1\right)}
\newcommand{\HocurlM}[1]{\mathring{H}\left(\text{curl}, #1\right)}

%%% to be removed %%%
\newcommand{\rmA}{A}

%% maths environments
\newtheorem{theorem}{Theorem}[section]
\newtheorem{definition}[theorem]{Definition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{example}[theorem]{Example}
\newtheorem{remark}[theorem]{Remark}
\newtheorem{assumption}[theorem]{Assumption}
\newtheorem{numberedproof}[theorem]{Proof}


%% adjust equation numbering to IMA style
\numberwithin{equation}{section}

\begin{document}

\maketitle

\vspace{-2.0em}

\begin{abstract}
We provide the algorithmic and computational complement to the theoretical framework of~\cite{Piel.2026} for hierarchical subdivision $k$-form spaces on triangular meshes. The central observation is that every basis function is represented by a column of the accumulated subdivision matrix. Selecting active degrees of freedom results in collecting their corresponding columns into the hierarchical subdivision matrix. This sparse matrix reduces all downstream operations like matrix assembly or residual evaluation to standard sparse linear algebra on the finest mesh $\T_\L$ without recomputing any integrals. We give explicit algorithms for constructing the active degrees of freedom from a subdivision partition, including a frontier-sweep procedure for the interface basis functions at level boundaries.

For the adaptive solution of PDEs, we describe a \textsc{Solve}--\textsc{Estimate}--\textsc{Mark}--\textsc{Pad}--\textsc{Refine} loop in which the non-standard \textsc{Pad} step enforces the subdivision partition condition after Dörfler marking, and a topological repair step corrects isolated boundary vertices that would otherwise break de Rham exactness. Numerical experiments on the Maxwell eigenvalue problem confirm that exact subdivision partitions reproduce the reference spectrum without spurious modes and adaptive experiments on the mixed vector Laplace equation demonstrate out adaptive loop.
\end{abstract}


\tableofcontents

\section{Introduction}

The finite element exterior calculus (FEEC)~\cite{Arnold.FEEC} provides a systematic framework for constructing stable, structure-preserving discretisations of partial differential equations (PDEs) involving differential forms. Its central structure is the discrete de Rham complex: a sequence of finite-dimensional function spaces connected by the exterior derivative $\extd$. Exactness of this complex is the key algebraic property that prevents spurious modes in Maxwell eigenvalue problems, guarantees inf-sup stability in mixed formulations, and underpins the commuting diagram that makes error analysis tractable. Classical realisations of the FEEC use the lowest-order Whitney forms, also knownn as the continuous Galerkin ($\mathrm{CG}_1$), Raviart--Thomas ($\mathrm{RT}_0$), and discontinuous ($\mathrm{DG}_0$) spaces, which are globally conforming but only piecewise polynomial and, at best, $\mathcal{C}^0$.

Subdivision algorithms, originally developed for geometric modelling, produce limit functions that are globally smooth on arbitrary triangular meshes. For example, Loop's scheme~\citep{Loop.1987} generates globally $\mathcal{C}^1$ scalar limit functions. Wang~\citep{Wang.2006,Wang.2008} introduce subdivision schemes for $1$- and $2$-forms that commute with the discrete exterior derivative from Discrete Exterior Calculus (DEC, \cite{Hirani.2003,Desbrun.2006}). These subdivision schemes give rise to subdivision $k$-form spaces in the DEC sense \citep{Goes.2015}. \cite{Piel.2026} translate their work to finite elements and introduce a framework for subdivision-based finite elements that form discrete de Rham complexes on meshes with uniform refinement.

Such uniform subdivision spaces assign the same resolution everywhere: every basis function has a support whose size is determined by the coarse mesh $\T_\lzero$. This is adequate for globally smooth solutions but wasteful when the PDE solution is rough only in a small part of the domain. In isogeometric analysis, the analogous limitation of globally refined B-spline spaces has been addressed through hierarchical B-spline spaces~\citep{Evans.2020}, which mix basis functions from different refinement levels to obtain spatially varying resolution while retaining linear independence. Extending this idea to spaces of differential forms is non-trivial: activating basis functions at different levels can break the exactness of the de Rham complex unless the selection is governed by a compatible combinatorial rule.
The analogous construction for B-spline de Rham complexes has been studied in~\cite{Buffa2011,Evans.2020,Shepherd.2024,Dijkstra.2025}, where admissibility conditions on the hierarchical mesh ensure exactness of the complex.
A central mechanism in these works is a \emph{completion step} that, after marking basis functions for refinement, enforces a compatibility condition on the active set: in~\cite{Cabanas.2026} this takes the form of \emph{$L$-chain completion}, which forces all basis functions in a connecting $L$-chain to be refined simultaneously whenever a conflicting pair is detected.
\checkit{The subdivision \textsc{Pad} step described in Section~4 serves an analogous role (restoring the subdivision partition condition after marking), but the precise correspondence between $L$-chain completion and the partition condition, and whether one implies the other, remains to be investigated.}

This paper provides the matrix-level description and algorithms for hierarchical subdivision $k$-form spaces that preserve the de Rham complex, serving as the practical and computational complement to~\cite{Piel.2026}. The key computational objects are the subdivision matrices $\Amat{k}{\l}{\L}$, which represent level-$\l$ basis functions in the Whitney basis of the fine assembly mesh $\T_\L$. A hierarchical space is formed by selecting one column of these matrices per active basis function, according to a \emph{subdivision partition} of $\T_\L$, a combinatorial condition on the patch decomposition of the domain. This condition, characterised in~\cite{Piel.2026}, simultaneously guarantees linear independence and exactness of the complex. Once the hierarchical subdivision matrix $\Amat{k}{\activeDoFs}{\L}$ is assembled, all downstream operations (exterior derivative, mass and stiffness matrices, residual evaluation) reduce to standard sparse linear algebra on $\T_\L$ without recomputing any integrals.

\paragraph*{Contributions.} This paper makes the following contributions:
\begin{itemize}
    \item A matrix-level construction of hierarchical subdivision $k$-form spaces and a practical algorithm for assembling the active degrees of freedom and the hierarchical subdivision matrix (Sections~3 and~4).
    \item An adaptive refinement loop \textsc{Solve}--\textsc{Estimate}--\textsc{Mark}--\textsc{Pad}--\textsc{Refine}, including a non-standard padding step that restores the subdivision partition condition after marking (Section~4).
    \item Numerical verification via the Maxwell eigenvalue problem, confirming that the exact de Rham complex is preserved, and adaptive convergence experiments for the vector Laplace equation with multi-scale solutions (Section~5).
\end{itemize}

\section{Properties of the subdivision-induced basis functions}

This section collects the most important properties of the uniform subdivision $k$-form spaces introduced in~\cite{Piel.2026}. We focus on the aspects that enable the construction of hierarchical subdivision $k$-form spaces in a similar way as~\cite{Evans.2020} did for B-splines.

\robert{Starting from an initial coarse triangular mesh $\T_\l$ at level $\l = \lzero$, the \emph{subdivision algorithm} produces a sequence of increasingly fine meshes $\T_{\lzero+1}, \ldots, \T_\L$ by repeatedly applying two steps: a \emph{topological refinement} that splits every triangle into four, and a \emph{geometric averaging} step that repositions vertices as weighted averages of neighbouring coarse vertices. We use Loop's subdivision scheme~\cite{Loop.1987} throughout. The resulting \emph{mesh hierarchy} $\{\T_\l\}_{\l=\lzero}^{\L}$ ranges from the coarsest level $\lzero$ to the finest, the \emph{assembly mesh} $\T_\L$. At each level $\l$, we denote the sets of vertices, edges, and faces by $\K^0_\l = \V_\l$, $\K^1_\l = \E_\l$, and $\K^2_\l = \F_\l$, with cardinalities $|\K^k_\l|$.

On each mesh $\T_\l$, the lowest-order Whitney $k$-form spaces are the standard FEEC spaces
\begin{equation}
    \FEECspace{0}{\l} = \mathrm{CG}_1(\T_\l), \qquad \FEECspace{1}{\l} = \mathrm{RT}_0(\T_\l), \qquad \FEECspace{2}{\l} = \mathrm{DG}_0(\T_\l),
\end{equation}
with basis functions $\freeQ{\FEECbasis}{k}{\l}{\coarseIdxone}$ indexed by the $k$-simplices $\simplex{k}{\l}{\coarseIdxone} \in \K^k_\l$.

The subdivision algorithm induces a linear \emph{subdivision operator} $\Sop{k}{\l}{\l+1}: \FEECspace{k}{\l} \to \FEECspace{k}{\l+1}$ between Whitney spaces on consecutive levels. Composing these one-step operators gives the \emph{accumulated subdivision operator}
\begin{equation}
    \Aop{k}{\l}{\L} \coloneqq \Sop{k}{\L-1}{\L} \circ \cdots \circ \Sop{k}{\l}{\l+1},
\end{equation}
whose coordinate representation with respect to the Whitney bases of $\FEECspace{k}{\l}$ and $\FEECspace{k}{\L}$ is the \emph{subdivision matrix} $\Amat{k}{\l}{\L}$ of size $|\K^k_\L| \times |\K^k_\l|$. Column $\coarseIdxone$ of $\Amat{k}{\l}{\L}$ is the coordinate vector of the image of $\freeQ{\FEECbasis}{k}{\l}{\coarseIdxone}$ in the fine Whitney basis of $\FEECspace{k}{\L}$.

The \emph{subdivision $k$-form space} on level $\l$ is defined as the image of $\FEECspace{k}{\l}$ under $\Aop{k}{\l}{\L}$,
\begin{equation}
    \SDFspace{k}{\l}{\L}\big(\T_\l\big) \coloneqq \Aop{k}{\l}{\L}\!\big(\FEECspace{k}{\l}\big),
\end{equation}
spanned by the \emph{subdivision basis functions} $\constrQ{\SDFbasis}{k}{\l}{\L}{\coarseIdxone} \coloneqq \Aop{k}{\l}{\L}\, \freeQ{\FEECbasis}{k}{\l}{\coarseIdxone}$, i.e.\ the columns of $\Amat{k}{\l}{\L}$ expressed in the fine Whitney basis. At the finest level, $\SDFspace{k}{\L}{\L} = \FEECspace{k}{\L}$ by definition. The spaces $\SDFspace{k}{\l}{\L}$ are smooth and globally supported functions on $|\T_\L|$ that constitute a discrete de Rham complex, as detailed in~\cite{Piel.2026} and recalled below.}

\subsection{Refinement of basis functions}

\begin{proposition}[Prop.~NUM from~\cite{Piel.2026}]
\label{prop:uniform_spaces_nested}
Let $\lzero \leq \l \leq \L$. For a fixed initial meshes $\T_{\l}$ for $\l \in \{\lzero, \hdots, \L\}$, the
corresponding subdivision $k$-form spaces $\SDFspace{k}{\l}{\L}\big(\T_{\l} \big)$ are nested, i.e.
\begin{equation}
    \label{eq:nested_spaces}
    \SDFspace{k}{\l}{\L} \subset  \SDFspace{k}{\l+1}{\L} \subset \hdots \subset \SDFspace{k}{\L}{\L} = \FEECspace{k}{\L},
\end{equation}
and have the dimensions $\dimlk{\l}{k} < \dimlk{\l +1}{k} < \hdots < \dimlk{\L}{k}$.
\end{proposition}

This proposition implies the refinement relation for the subdivision basis functions $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$. The resulting properties regarding the refinability of the basis functions form the backbone of the adaptive method presented in this paper. The following discussion is focused on basis functions on intermediate levels $\lone$ with $\lzero \leq \lone \leq \L$ defined as
\begin{equation}
    \label{equ:basisrel}
    \constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone} = \Aop{k}{\lone}{\L} \freeQ{\FEECbasis}{k}{\lone}{\coarseIdxone} = \sum_{\fineIdxone = 1}^{\sumdimlk{\lone}{k}} \Amatij{k}{\lone}{\L}{\fineIdxone}{\coarseIdxone} \; \freeQ{\FEECbasis}{k}{\L}{\fineIdxone} \, ,
\end{equation}
where $\Amat{k}{\lone}{\L}$ is the subdivision matrix, cf.~\cite{Piel.2026}. Splitting the accumulated subdivision operator $\Aop{k}{\l}{\L}$ reveals the following relation between the basis functions $\constrVec{\Phi}{k}{\lone}{\L}$ and $\constrVec{\Phi}{k}{\lone + 1}{\L}$ on neighbouring levels.

\begin{lemma}[Refinement Relation, {\cite{Piel.2026}}]
    \label{lemma:hiermeshrel}
    Let $\lone$ be an arbitrary level such that $\lzero \leq \lone \leq \L$ and let $\Amat{k}{\l}{\l+1}$ be the subdivision matrix associated to $\Aop{k}{\lone}{\lone+1}$. Then, the basis functions $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$ and $\constrQ{\SDFbasis}{k}{\lone+1}{\L}{\fineIdxone}$ satisfy the following \emph{refinement relation}
    \begin{equation}
        \label{eq:refinement_relation}
        \constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone} = \sum\limits_{\fineIdxone = 1}^{\sumdimlk{\lone+1}{k}} \Amatij{k}{\lone}{\lone+1}{\fineIdxone}{\coarseIdxone} \; \constrQ{\SDFbasis}{k}{\lone+1}{\L}{\fineIdxone} \; .
    \end{equation}
\end{lemma}
\begin{proof}
    See~\cite{Piel.2026}.
\end{proof}

We will capture relationships between different basis functions across the mesh hierarchy through \textit{stencils}. Since there is a one-to-one correspondence between basis functions and simplices, we will formulate these stencils as maps between (sets of) simplices.

\robert{We use the term \emph{stencil} for a map from subsets of simplices on one level to subsets of simplices across the hierarchy; formally, a map $\powerset(\K^k_{\lone}) \to \powerset\!\bigl(\bigsqcup_{\l=\lzero}^{\L}\bigsqcup_{k=0}^{2}\K^k_\l\bigr)$.}

We choose to formulate stencils with respect to simplices instead of basis functions because that enables us to use the adjacency operator $\KmKnl{m}{n}{\lone}$ and refinement operators $\KKltoL{k}{\lone}{\ltwo}$ introduced in~\cite{Piel.2026}. The remainder of this section sets up refinement stencils based on the refinement relation~\ref{eq:refinement_relation}, while the next section lays the foundations for the derivative stencil by investigating the relation of basis functions of different form degrees.

The hierarchical refinement relation in Lemma~\ref{lemma:hiermeshrel} provides a way to re-express a basis function $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$ as a linear combination of finer basis functions. This idea of expressing coarse basis functions through finer basis functions is a core concept in this work. The following definition introduces the corresponding formalism called refinement stencils. After that, we highlight one particular known refinement stencil and then present a construction for more general refinement stencils.

\begin{definition}[Refinement Stencil]
    \label{def:deep_ref_stencil}
    Let $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$ denote the basis function of $\SDFspace{k}{\lone}{\L}$ that is associated to the simplex $\simplex{k}{\lone}{\coarseIdxone}$.
    A \robert{deep} refinement stencil is a collection of simplices $\simplex{k}{\ltwo}{\fineIdxone}$ across the finer levels $\ltwo \in \{\lone\!+\!1, \, \hdots, \L\}$ whose associated basis functions $\constrQ{\SDFbasis}{k}{\ltwo}{\L}{\fineIdxone}$ can exactly re-express $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$. More concretely, a refinement stencil $\RSt\big(\simplex{k}{\lone}{\coarseIdxone}\big)$ is a map
    \begin{equation}
        \label{equ:refstencilunion}
        \RSt : \K^k_{\lone} \to \powerset \Big(\bigcup_{\ltwo = \lone+1}^{\L} \K^k_{\ltwo} \Big), \quad  \simplex{k}{\lone}{\coarseIdxone} \mapsto \RSt \big( \simplex{k}{\lone}{\coarseIdxone} \big)
    \end{equation}
    such that there exist coefficients $\constrQ{c}{k}{\ltwo}{\lone}{\fineIdxone} \neq 0$ satisfying
    \begin{equation}
        \constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone} = \sum_{\simplex{k}{\ltwo}{\fineIdxone} \in \RSt \big( \simplex{k}{\lone}{\coarseIdxone} \big)} \constrQ{c}{k}{\robert{\ltwo}}{\robert{\lone}}{\fineIdxone} \; \constrQ{\SDFbasis}{k}{\ltwo}{\L}{\fineIdxone}.
    \end{equation}
\end{definition}

\begin{corollary}
    \label{cor:refinement_support}
    Let $\RSt\big( \simplex{k}{\lone}{\coarseIdxone} \big)$ be a refinement stencil of $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$ with $\lzero \leq \lone \leq \ltwo \leq \L$. It then holds that
    \begin{equation}
        \support \big( \constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone} \big) = \bigcup_{ \simplex{k}{\ltwo}{\fineIdxone} \in \RSt( \simplex{k}{\lone}{\coarseIdxone} )} \support\big( \constrQ{\SDFbasis}{k}{\ltwo}{\L}{\fineIdxone} \big).
    \end{equation}
\end{corollary}

The coefficients $\constrQ{c}{k}{\ltwo}{\lone}{\fineIdxone}$ above are connected to the non-zero entries of the subdivision matrices. In fact, we can compute them by tracing back the contributions of certain fine basis functions to the coarse one analogously to the refinement relation~(\ref{eq:refinement_relation}). The only difference is that the refinement stencil $\RSt \big( \simplex{k}{\lone}{\coarseIdxone} \big)$ now contains simplices across different refinement levels, i.e. $\ltwo$ is not constant for $\simplex{k}{\ltwo}{\fineIdxone}$. We are only concerned with the existence of the coefficients $\constrQ{c}{k}{\ltwo}{\lone}{\fineIdxone}$ because our goal is typically not to exactly re-express the coarse basis functions but to test wether they are in the space spanned by some finer basis functions. That is why we are going to formulate indicators for such linear dependence of coarse basis functions based on the existence of refinement stencils.

In the following, we will systematically construct refinement stencils by iterating the canonical refinement stencil defined next. According to eq.~\ref{eq:refinement_relation}, all basis functions $\constrQ{\SDFbasis}{k}{\lone+1}{\L}{\fineIdxone}$ with non-zero entries $\Amatij{k}{\lone}{\lone+1}{\fineIdxone}{\coarseIdxone} \neq 0$ are required to re-express the coarse basis function $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$. The following definition formalises this selection of basis functions.

\begin{definition}[\robert{Canonical} Refinement Stencil]
    Let $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$ denote the basis function of $\SDFspace{k}{\lone}{\L}$ that is associated to the simplex $\simplex{k}{\lone}{\coarseIdxone}$. The canonical refinement $\cRSt\big( \simplex{k}{\lone}{\coarseIdxone}\big)$ of $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$ is the map
    \begin{eqnarray}
        \cRSt: \K^k_{\lone} \to \powerset\big( \K^k_{\lone+1}\big), \qquad \simplex{k}{\lone}{\coarseIdxone} \mapsto \Big\{ \simplex{k}{\lone+1}{\fineIdxone} \in \K^k_{\lone+1} \colon \; \Amatij{k}{\lone}{\lone+1}{\fineIdxone}{\coarseIdxone} \neq 0 \Big\}.
    \end{eqnarray}
\end{definition}

As we did before, we will slightly abuse the notation and overload the canonical refinement stencil to act on sets of simplices on the same level $\lone$, i.e.
\begin{equation}
    \cRSt: \powerset\big(\K^k_{\lone}\big) \to \powerset\big( \K^k_{\lone+1}\big), \qquad \simplexsubset{k}{\lone} \mapsto \bigcup_{\simplex{k}{\lone}{\coarseIdxone} \in \simplexsubset{k}{\lone}}\cRSt\big( \simplex{k}{\lone}{\coarseIdxone} \big).
\end{equation}

\robert{We call this refinement stencil canonical because it is the only one that contains simplices solely on level $\lone\!+\!1$ and is thus uniquely determined by the refinement relation~\ref{eq:refinement_relation}. Once we allow simplices from finer levels $\ltwo > \lone\!+\!1$, uniqueness is lost. For instance, if $\cRSt(\simplex{k}{\lone}{\coarseIdxone}) = \{\simplex{k}{\lone+1}{\fineIdxone_1}, \ldots, \simplex{k}{\lone+1}{\fineIdxone_n}\}$, then for each $m = 1, \ldots, n$,
\begin{equation}
    \label{eq:example_ref_stencil}
    \RSt_m = \cRSt\big(\simplex{k}{\lone+1}{\fineIdxone_m}\big) \cup \big\{\simplex{k}{\lone+1}{\fineIdxone_i} : i \neq m\big\}
\end{equation}
is a valid refinement stencil of $\simplex{k}{\lone}{\coarseIdxone}$ with simplices on levels $\lone\!+\!1$ and $\lone\!+\!2$, yielding $n$ distinct alternatives to $\cRSt(\simplex{k}{\lone}{\coarseIdxone})$.}


\begin{figure}
\begin{center}
\begin{tikzpicture}[
    every node/.style={inner sep=0pt, outer sep=0pt},
    arr/.style={->, >=Stealth, line width=1.5pt, shorten >=2pt, shorten <=2pt}
]

% ============================================================
%  COLUMN X POSITIONS
% ============================================================
\def\xA{0cm}
\def\xB{5cm}
\def\xC{9cm}

% ============================================================
%  ROW Y POSITIONS
% ============================================================
\def\yOne{0cm}
\def\yTwo{-4.5cm}
\def\yThree{-8cm}

% ============================================================
%  ROW 1
% ============================================================
\node (A1) at (\xA, \yOne)
    {\includegraphics[width=0.25\textwidth]{images/stencils/0_form_simplex.pdf}};
\node[xshift=2pt, yshift=10pt, font=\small, text=black] at (A1.center) {$\simplex{0}{\l}{\coarseIdxone}$};

\node (B1) at (\xB, \yOne)
    {\includegraphics[width=0.25\textwidth, trim=10pt 0pt 10pt 0pt, clip]{images/stencils/1_form_simplex.pdf}};
\node[xshift=-12pt, yshift=10pt, font=\small, text=black] at (B1.center) {$\simplex{1}{\l}{\coarseIdxone}$};

\node (C1) at (\xC, \yOne)
    {\includegraphics[width=0.25\textwidth]{images/stencils/2_form_simplex.pdf}};
\node[xshift=-12pt, yshift=8pt, font=\small, text=black] at (C1.center) {$\simplex{2}{\l}{\coarseIdxone}$};

% ============================================================
%  ROW 2
% ============================================================
\node (A2) at (\xA, \yTwo)
    {\includegraphics[width=0.25\textwidth]{images/stencils/0_form_refinement_stencil.pdf}};

\node (B2) at (\xB, \yTwo)
    {\includegraphics[width=0.25\textwidth, trim=10pt 0pt 10pt 0pt, clip]{images/stencils/1_form_refinement_stencil.pdf}};

\node (C2) at (\xC, \yTwo)
    {\includegraphics[width=0.25\textwidth]{images/stencils/2_form_refinement_stencil.pdf}};

% ============================================================
%  VERTICAL ARROWS (one per column, between each row pair)
% ============================================================
\draw[arr] (A1.south) -- (A2.north) node[midway, right, font=\small] {$\;\; \cRSt\big( \simplex{0}{\l}{\coarseIdxone} \big)$};
\draw[arr] ([xshift=-14pt]B1.south) -- ([xshift=-14pt]B2.north) node[midway, right, font=\small] {$\;\; \cRSt\big( \simplex{1}{\l}{\coarseIdxone} \big)$};
\draw[arr] ([xshift=-14pt]C1.south) -- ([xshift=-14pt]C2.north) node[midway, right, font=\small] {$\;\; \cRSt\big( \simplex{2}{\l}{\coarseIdxone} \big)$};

%\draw[arr] 
%    ([yshift=-12pt]A2.south west) -- ([yshift=-12pt]C2.south east)
%    node[midway, below=4pt, font=\small] {Choose active basis functions across all levels $\l \leq \L$ to span hierarchical spaces.};

\end{tikzpicture}
\end{center}
\vspace{-5mm}
\caption{Refinement stencils of $0$-, $1$- and $2$-form basis functions.}
\end{figure}

\subsection{Derivatives of basis functions}

The previous concepts set up the hierarchical nature of the subdivision $k$-form spaces. Ultimately, they will allow us to pick and choose our active degrees of freedom as we wish (with certain restrictions).

We now set up the differential structure of the spaces. First,~\cite{Piel.2026} shows that the uniform subdivision $k$-form spaces comprise the de Rham complex
\begin{equation}
\begin{tikzcd}
  \constrVec{\mathfrak{R}}{}{\lone}{\L}\colon \quad 0 \arrow[r] & \SDFspace{0}{\lone}{\L} \arrow[r, "\extd"] & \SDFspace{1}{\lone}{\L} \arrow[r, "\extd"] & \SDFspace{2}{\lone}{\L} \arrow[r] & 0.
\end{tikzcd}
\end{equation}
on all intermediate levels $\lone$ with $\lzero \leq \l \leq \lone \leq \L$. \robert{The coordinate expression of the action of the exterior derivative $\extd$ on the spaces $\SDFspace{k}{\lone}{\L}$ follows directly from the coarse-level oriented incidence matrix $\Dmat{k}{\lone}$~\citep{Hirani.2003,Desbrun.2006,Piel.2026}. (Throughout, we write $\extd$ directly; the companion paper~\cite{Piel.2026} employs an auxiliary operator $\auxextd$ that coincides with $\extd$ for the compatible schemes used here.)
\begin{equation}
    \label{eq:derivative_relation_basis}
    \extd \constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone} = \sum_{\coarseIdxtwo=1}^{\sumdimlk{\lone}{k+1}} \Dmatij{k}{\lone}{\coarseIdxtwo}{\coarseIdxone} \, \constrQ{\SDFbasis}{k+1}{\lone}{\L}{\coarseIdxtwo}.
\end{equation}
Since $\Dmat{k}{\lone}$ is the signed incidence matrix of $\T_\lone$, only the $(k+1)$-simplices of $\T_\lone$ adjacent to $\simplex{k}{\lone}{\coarseIdxone}$ appear with non-zero coefficients. In the context of splines, this is equivalent to forward-differencing, in particular for triangular splines like box splines~\citep{de_boor_box_1993}.}

\begin{definition}[Derivative Stencil]
    \label{def:derivative_stencil}
    Let $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$ be a basis function of $\SDFspace{k}{\lone}{\L}$ associated with the $k$-simplex $\simplex{k}{\lone}{\coarseIdxone} \in \K^k_{\lone}$. The derivative stencil $\DSt$ is a map 
    \begin{equation}
        \DSt : \K^k_{\lone} \to \powerset \big( \K^{k+1}_{\lone} \big), \qquad \simplex{k}{\lone}{\coarseIdxone} \robert{\mapsto} \big\{ \simplex{k+1}{\lone}{\coarseIdxtwo}: \; \Dmatij{k}{\lone}{\coarseIdxtwo}{\coarseIdxone} \neq 0 \big\}.
    \end{equation}
\end{definition}

Once again, we overload the derivative stencil such that it can also act on sets of simplices:
\begin{equation}
    \DSt : \powerset\Big(\K^k_{\lone}\Big) \to \powerset \big( \K^{k+1}_{\lone} \big), \qquad \simplexsubset{k}{\lone} \mapsto \bigcup_{\simplex{k}{\lone}{\coarseIdxone} \in \simplexsubset{k}{\lone}} \DSt \big( \simplex{k}{\lone}{\coarseIdxone} \big)
\end{equation}

This definition together with Eq.~\eqref{eq:derivative_relation_basis} implies the following two propositions.

\begin{proposition}
    \label{prop:d_tilde_on_phi}
    $\robert{\extd} \constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone} = \sum\limits_{\simplex{k+1}{\lone}{\coarseIdxtwo} \in \DSt(\simplex{k}{\lone}{\coarseIdxone})} \Dmatij{k}{\lone}{\coarseIdxtwo}{\coarseIdxone} \, \robert{\constrQ{\SDFbasis}{k+1}{\lone}{\L}{\coarseIdxtwo}}$
\end{proposition}

\begin{proposition}
    \label{cor:derivative_support}
    $\support \big( \constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone} \big) = \support \big( \robert{\extd} \constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone} \big) = \bigcup\limits_{\simplex{k+1}{\lone}{\coarseIdxtwo} \in \DSt(\simplex{k}{\lone}{\coarseIdxone})} \mathrm{supp} \big( \constrQ{\SDFbasis}{k+1}{\lone}{\L}{\coarseIdxtwo} \big).$
\end{proposition}

\robert{The derivative stencil is key for ensuring that every active $k$-form basis function has its derivative stencil present in the $(k+1)$-form space — a requirement for preserving the de Rham complex. The following lemma makes the stencil concrete for the Wang schemes~\cite{Wang.2006,Wang.2008} used in this work.}

\robert{\begin{lemma}[{\cite{Piel.2026}}]
    \label{lemma:derivative_stencil}
    Let $\freeQ{v}{}{\lone}{\coarseIdxone} \in \V_{\lone}$ and $\freeQ{e}{}{\lone}{\coarseIdxtwo} \in \E_{\lone}$ be a vertex and an edge on level $\lone$. For the Wang subdivision schemes~\cite{Wang.2006,Wang.2008} (whose $0$-form scheme coincides with Loop subdivision~\cite{Loop.1987}), the derivative stencils are given by
    \begin{equation}
        \DSt\big( \freeQ{v}{}{\lone}{\coarseIdxone} \big) = \EVl{\lone}\big( \freeQ{v}{}{\lone}{\coarseIdxone} \big) \quad \text{and} \quad \DSt\big( \freeQ{e}{}{\lone}{\coarseIdxtwo} \big) = \FEl{\lone}\big( \freeQ{e}{}{\lone}{\coarseIdxtwo} \big),
    \end{equation}
    or more generally, $\DSt\big(\simplex{k}{\lone}{\coarseIdxone}\big) = \K^{k+1}\K^k_{\lone}\big(\simplex{k}{\lone}{\coarseIdxone}\big)$ for any $\simplex{k}{\lone}{\coarseIdxone} \in \K^k_{\lone}$.
\end{lemma}
\begin{proof}
    See~\cite{Piel.2026}.
\end{proof}}

In the rest of this paper, we will set up adaptive function spaces by mixing basis functions on different subdivision levels and control their linear independence and the de Rham complex by using the refinement and derivative stencils.


\begin{figure}
\begin{center}
\begin{tikzpicture}[
    every node/.style={inner sep=0pt, outer sep=0pt},
    arr/.style={->, >=Stealth, line width=1.5pt, shorten >=2pt, shorten <=2pt}
]

% ============================================================
%  COLUMN X POSITIONS
% ============================================================
\def\xA{0cm}
\def\xB{5cm}
\def\xC{10cm}

% ============================================================
%  ROW Y POSITIONS
% ============================================================
\def\yOne{0cm}
\def\yTwo{-4.5cm}
\def\yThree{-8cm}

% ============================================================
%  ROW 1
% ============================================================
\node (A1) at (\xA, \yOne)
    {\includegraphics[width=0.25\textwidth]{images/stencils/0_form_simplex.pdf}};
\node[xshift=2pt, yshift=10pt, font=\small, text=black] at (A1.center) {$\simplex{0}{\l}{\coarseIdxone}$};

\node (B1) at (\xB, \yOne)
    {\includegraphics[width=0.25\textwidth, trim=10pt 0pt 10pt 0pt, clip]{images/stencils/1_form_simplex.pdf}};
\node[xshift=-12pt, yshift=10pt, font=\small, text=black] at (B1.center) {$\simplex{1}{\l}{\coarseIdxone}$};

% ============================================================
%  ROW 2
% ============================================================
\node (A2) at (\xA, \yTwo)
    {\includegraphics[width=0.25\textwidth]{images/stencils/0_form_deriv_stencil.pdf}};

\node (B2) at (\xB, \yTwo)
    {\includegraphics[width=0.25\textwidth, trim=10pt 0pt 10pt 0pt, clip]{images/stencils/1_form_deriv_stencil.pdf}};

% ============================================================
%  VERTICAL ARROWS (one per column, between each row pair)
% ============================================================
\draw[arr] (A1.south) -- (A2.north) node[midway, right, font=\small] {$\;\;\DSt\big( \simplex{0}{\l}{\coarseIdxone} \big)$};
\draw[arr] ([xshift=-14pt]B1.south) -- ([xshift=-14pt]B2.north) node[midway, right, font=\small] {$\;\;\DSt\big( \simplex{1}{\l}{\coarseIdxone} \big)$};

\end{tikzpicture}
\end{center}
\vspace{-5mm}
\caption{Derivate stencils of $0$- and $1$-form basis functions.}
\end{figure}

\FloatBarrier

\section{Hierarchical subdivision $k$-form spaces}

\subsection{Active degrees of freedom}

\robert{Uniform subdivision spaces $\SDFspace{k}{\lone}{\L}$ resolve the domain at a single global scale, which is wasteful when the PDE solution contains localised features. Hierarchical spaces break this uniformity by mixing basis functions from \emph{different} levels $\lone$: as illustrated in Fig.~\ref{fig:hier_basis_funs}, a level-$0$ function (left) has broad support capturing large-scale variation, while a level-$2$ function (right) has support roughly $4^2$ times smaller and can resolve local detail. Selecting which levels to activate in which part of the domain yields an approximation space whose resolution adapts spatially to the solution.}

\begin{figure}
\begin{center}
\begin{tikzpicture}[
    every node/.style={inner sep=0pt, outer sep=0pt},
    arr/.style={->, >=Stealth, line width=1.5pt, shorten >=2pt, shorten <=2pt}
]

% ============================================================
%  COLUMN X POSITIONS
% ============================================================
\def\xA{0cm}
\def\xB{5cm}
\def\xC{10cm}

% ============================================================
%  ROW Y POSITIONS
% ============================================================
\def\yOne{0cm}
\def\yTwo{-4cm}
\def\yThree{-8cm}

% ============================================================
%  ROW 1
% ============================================================
\node (A1) at (\xA, \yOne)
    {\includegraphics[width=0.25\textwidth]{images/basis_funs/whitney_basis_fun_l0.png}};

\node (B1) at (\xB, \yOne)
    {\includegraphics[width=0.25\textwidth, trim=10pt 0pt 10pt 0pt, clip]{images/basis_funs/whitney_basis_fun_l1.png}};

\node (C1) at (\xC, \yOne)
    {\includegraphics[width=0.25\textwidth]{images/basis_funs/whitney_basis_fun_l2.png}};

% ============================================================
%  ROW 2
% ============================================================
\node (A2) at (\xA, \yTwo)
    {\includegraphics[width=0.25\textwidth]{images/basis_funs/basis_fun_l0.png}};

\node (B2) at (\xB, \yTwo)
    {\includegraphics[width=0.25\textwidth, trim=10pt 0pt 10pt 0pt, clip]{images/basis_funs/basis_fun_l1.png}};

\node (C2) at (\xC, \yTwo)
    {\includegraphics[width=0.25\textwidth]{images/basis_funs/basis_fun_l2.png}};

% ============================================================
%  VERTICAL ARROWS (one per column, between each row pair)
% ============================================================
\draw[arr] (A1.south) -- (A2.north) node[midway, right, font=\small] {$\;\;\Aop{0}{0}{\L}$};
\draw[arr] (B1.south) -- (B2.north) node[midway, right, font=\small] {$\;\;\Aop{0}{1}{\L}$};
\draw[arr] (C1.south) -- (C2.north) node[midway, right, font=\small] {$\;\;\Aop{0}{2}{\L}$};

% ============================================================
%  COLUMN LABELS (above row 1)
% ============================================================
\node[above=8pt of A1, inner sep=2pt] (colA) {Basis function from $\l = 0$};
\node[above=8pt of B1, inner sep=2pt] (colB) {Basis function from $\l = 1$};
\node[above=8pt of C1, inner sep=2pt] (colC) {Basis function from $\l = 2$};

%\draw[arr] 
%    ([yshift=-12pt]A2.south west) -- ([yshift=-12pt]C2.south east)
%    node[midway, below=4pt, font=\small] {Choose active basis functions across all levels $\l \leq \L$ to span hierarchical spaces.};

\end{tikzpicture}
\end{center}
\vspace{-5mm}
\caption{Choose active basis functions across all levels $\l \leq \L$ to span hierarchical spaces.}
\label{fig:hier_basis_funs}
\end{figure}

On a matrix-level, any subdivision basis function can be described as a column of the subdivision matrix. Ultimately, since the subdivision matrices of all levels $\lone$ map to $\L$, we can pick and choose any column of any subdivision matrix to become an active degree of freedom. This means we can assemble a vector of degree of freedom
\begin{equation}
  \activeDoFSet{k}{} \coloneq \bigcup_{\lone = \l}^{\L} \activeDoFSet{k}{\lone} \quad \text{with} \quad \activeDoFSet{k}{\lone} \subset \K^k_\lone.
\end{equation}
This means that we can obtain a set of active degrees of freedom by simply choosing sets of simplices and their associated subdivision basis functions across the hierarchy. This gives rise to the hierarchical subdivision $k$-form spaces $\activeSDFspaces{k}{}$ defined as
\begin{equation}
  \activeSDFspaces{k}{} \coloneq \mathrm{span}\big( \SDFbases^k_\activeDoFs \big) \quad \text{with} \quad \SDFbases^k_\activeDoFs \coloneq \bigcup_{\simplex{k}{\lone}{\coarseIdxone} \in \activeDoFSet{k}{}} \; \constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}.
\end{equation}
The hierarchical spaces of subdivision $k$-forms have their own associated subdivision operators.

{\color{orange}
On the matrix level, the hierarchical basis is encoded in the \emph{hierarchical subdivision matrix} $\Amat{k}{\activeDoFs}{\L}$. Recall that the uniform subdivision matrix $\Amat{k}{\lone}{\L}$ has rows indexed by $\K^k_\L$ and columns indexed by $\K^k_{\lone}$; its column $\coarseIdxone$ is the coordinate vector of the constrained basis function $\constrQ{\SDFbasis}{k}{\lone}{\L}{\coarseIdxone}$ in the Whitney basis of $\FEECspace{k}{}(\T_\L)$. We form $\Amat{k}{\activeDoFs}{\L}$ by retaining, for each level $\lone$, only those columns whose index belongs to the active set $\activeDoFSet{k}{\lone} \subset \K^k_{\lone}$, and concatenating the resulting blocks horizontally across all levels. Identifying the column index of $\Amat{k}{\activeDoFs}{\L}$ with the pair $(\lone, \coarseIdxone)$ for $\simplex{k}{\lone}{\coarseIdxone} \in \activeDoFSet{k}{\lone}$, this reads
\begin{equation}
    \left(\Amat{k}{\activeDoFs}{\L}\right)_{\fineIdxone,\,(\lone,\,\coarseIdxone)}
    \;=\;
    \left(\Amat{k}{\lone}{\L}\right)_{\fineIdxone,\,\coarseIdxone},
    \qquad
    \simplex{k}{\L}{\fineIdxone} \in \K^k_\L, \quad \simplex{k}{\lone}{\coarseIdxone} \in \activeDoFSet{k}{\lone}.
\end{equation}
The resulting matrix has dimensions $\big|\K^k_\L\big| \times \big|\activeDoFSet{k}{}\big|$, with $\big|\activeDoFSet{k}{}\big| = \sum_{\lone} \big|\activeDoFSet{k}{\lone}\big|$.

Given a hierarchical form $\omega^k_\activeDoFs \in \activeSDFspaces{k}{}$ with coefficient vector $\bar{c}^k \in \mathbb{R}^{|\activeDoFSet{k}{}|}$, its image under the inclusion $\inclusion \colon \activeSDFspaces{k}{} \hookrightarrow \FEECspace{k}{}(\T_\L)$ is
\begin{equation}
    \inclusion\!\left(\omega^k_\activeDoFs\right) = \Amat{k}{\activeDoFs}{\L}\,\bar{c}^k.
\end{equation}
This representation allows all operations on hierarchical forms, such as applying the discrete exterior derivative, assembling mass or stiffness matrices, and evaluating residuals, to be carried out as standard sparse linear algebra on the fine level $\T_\L$.
}

Clearly, most of these configurations do not make sense from a numerical perspective. For instance, the spaces $\activeSDFspaces{k}{}$ might not cover the entire domain, have linearly dependent basis functions or not form differential complexes. The following section present parameterisations of the set $\activeDoFSet{k}{}$ of active degrees of freedom that give us more control over these aspects.

\robert{A natural organisational unit for the choice of active degrees of freedom is a \emph{mesh patch} $\calP_\l \subset \T_\l$: a connected submesh at level $\l$ whose closure aligns with the support of the basis functions $\constrQ{\SDFbasis}{k}{\l}{\L}{\coarseIdxone}$ associated with its interior simplices. Tiling the domain with patches at possibly different levels $\l_i$ produces a \emph{subdivision partition} $\C$; we then activate all interior DoFs of each patch, plus additional interface DoFs along patch boundaries. The precise definition is given in Definition~\ref{def:active_k_form_dofs}; here we motivate the key design choices visible in Fig.~\ref{fig:active_dofs}.

The need for interface DoFs is easiest to see in the $k=1$ column of Fig.~\ref{fig:active_dofs}. At level $\l=0$ (top row, orange edges), only the edges interior to the level-$0$ patches are active; the boundary edges shared with the finer level-$1$ region are absent. At level $\l=1$ (middle row), those same boundary edges appear as active 1-form DoFs. Without them, the exterior derivative $\extd\omega^0$ of a level-$0$ 0-form basis function near the patch boundary would map to an edge with no associated active 1-form DoF, breaking the de Rham complex. More generally, when two patches at levels $\l_i < \l_j$ share a boundary, interface DoFs are added at \emph{every} intermediate level $\l_i < \l_t \leq \l_j$. This prevents basis functions from very different levels meeting abruptly across the interface, which would otherwise create an ill-conditioned system. We do not have rigorous estimates that quantify this stiffening effect, and it remains a subject for further investigation.

The interface DoFs are attributed exclusively to the \emph{finer} side of the patch boundary, not the coarser side. Including coarser-side DoFs would introduce linear dependence: any coarser-level basis function restricted to the interface is already expressible as a linear combination of the finer-level basis functions there (cf.\ the refinement stencil condition in Section~2). Restricting to the finer side therefore maintains linear independence while still closing the de Rham complex, as made precise in the theorems at the end of this section.}

\begin{figure}[htbp]
\begin{center}
\begin{tikzpicture}[
    every node/.style={inner sep=0pt, outer sep=0pt},
    arr/.style={->, >=Stealth, line width=1.5pt, shorten >=2pt, shorten <=2pt}
]

% ============================================================
%  COLUMN X POSITIONS
% ============================================================
\def\xA{0cm}
\def\xB{3.9cm}
\def\xC{7.8cm}

% ============================================================
%  ROW Y POSITIONS
% ============================================================
\def\yOne{0cm}
\def\yTwo{-2.4cm}
\def\yThree{-4.8cm}

% ============================================================
%  ROW 1
% ============================================================
\node (A1) at (\xA, \yOne)
    {\includegraphics[width=0.22\textwidth]{images/dofmeshes/correct/dofs_k_0_lvl_l0.pdf}};

\node (B1) at (\xB, \yOne)
    {\includegraphics[width=0.22\textwidth, trim=10pt 0pt 10pt 0pt, clip]{images/dofmeshes/correct/dofs_k_1_lvl_l0.pdf}};

\node (C1) at (\xC, \yOne)
    {\includegraphics[width=0.22\textwidth]{images/dofmeshes/correct/dofs_k_2_lvl_l0.pdf}};

% ============================================================
%  ROW 2
% ============================================================
\node (A2) at (\xA, \yTwo)
    {\includegraphics[width=0.22\textwidth]{images/dofmeshes/correct/dofs_k_0_lvl_l1.pdf}};

\node (B2) at (\xB, \yTwo)
    {\includegraphics[width=0.22\textwidth, trim=10pt 0pt 10pt 0pt, clip]{images/dofmeshes/correct/dofs_k_1_lvl_l1.pdf}};

\node (C2) at (\xC, \yTwo)
    {\includegraphics[width=0.22\textwidth]{images/dofmeshes/correct/dofs_k_2_lvl_l1.pdf}};

% ============================================================
%  ROW 3
% ============================================================
\node (A3) at (\xA, \yThree)
    {\includegraphics[width=0.22\textwidth]{images/dofmeshes/correct/dofs_k_0_lvl_l2.pdf}};

\node (B3) at (\xB, \yThree)
    {\includegraphics[width=0.22\textwidth, trim=10pt 0pt 10pt 0pt, clip]{images/dofmeshes/correct/dofs_k_1_lvl_l2.pdf}};

\node (C3) at (\xC, \yThree)
    {\includegraphics[width=0.22\textwidth]{images/dofmeshes/correct/dofs_k_2_lvl_l2.pdf}};

% ============================================================
%  COLUMN LABELS (above row 1)
% ============================================================
\node[above=8pt of A1, font=\bfseries, inner sep=2pt] (colA) {$k = 0$};
\node[above=8pt of B1, font=\bfseries, inner sep=2pt] (colB) {$k = 1$};
\node[above=8pt of C1, font=\bfseries, inner sep=2pt] (colC) {$k = 2$};

% ============================================================
%  ROW LABELS (to the left of each row)
% ============================================================
\node[left=12pt of A1, font=\bfseries, inner sep=2pt] {$\l = 0$};
\node[left=12pt of A2, font=\bfseries, inner sep=2pt] {$\l = 1$};
\node[left=12pt of A3, font=\bfseries, inner sep=2pt] {$\l = 2$};

% ============================================================
%  OVERBRACE
% ============================================================
\draw[
    decoration={calligraphic brace, amplitude=6pt, raise=4pt},
    decorate, line width=1.2pt
]
    (A1.north west|-colA.north) -- (C1.north east|-colA.north)
    node[midway, above=14pt, font=\bfseries] {Active DoFs};

% ============================================================
%  GRAPHIC ABOVE THE OVERBRACE
% ============================================================
\node (topfig) at (3.9cm, 4.0cm)
    {\includegraphics[width=0.3\textwidth]{images/dofmeshes/correct/subdiv_partition.pdf}};

\node[above=6pt of topfig, font=\bfseries] {Subdivision partition};

\end{tikzpicture}
\end{center}
\caption{Active $k$-form DoFs for the three-level subdivision partition shown at top. Columns correspond to form degree $k = 0$ (vertices, dark circles), $k = 1$ (edges, orange), and $k = 2$ (faces, green). Rows correspond to active DoFs at levels $\l = 0$, $1$, $2$ respectively. At each level, interior simplices of the patches at that level are active, together with interface simplices on the coarser side along boundaries with finer-level patches (cf.\ Definition~\ref{def:active_k_form_dofs}).}
\label{fig:active_dofs}
\end{figure}

\FloatBarrier
\subsection{Selecting active degrees of freedom using subdivision partitions}

\robert{A \emph{patch} $\calP_\l \subset \T_\l$ is a non-empty, edge-connected collection of faces of $\T_\l$, with boundary $\partial\calP_\l$ consisting of those edges adjacent to exactly one face. The \emph{patch subdivision operator} $\mathcal{A}_{\calP_\l \to \L}$ maps $\calP_\l$ to the collection $\calP_{\l \to \L}$ of all faces of $\T_\L$ arising from repeatedly splitting the faces of $\calP_\l$; in coordinates, it is the restriction of $\Amat{k}{\l}{\L}$ to the rows and columns indexed by the $k$-simplices of $\calP_{\l \to \L}$ and $\calP_\l$, respectively; see~\cite{Piel.2026}.

Two patches $\calP_{\l_i}$ and $\calP_{\l_j}$ are \emph{non-overlapping} if their refinements share at most edges: $\I_{(\calP_{\l_i}, \calP_{\l_j}) \to \L} \coloneqq \calP_{\l_i \to \L} \cap \calP_{\l_j \to \L} \subset \E_\L(\T_\L)$. A \emph{covering} $\C(\T_\L) = \bigcup_{\l_i,n}\calP^n_{\l_i}$ is a set of pairwise non-overlapping patches whose refinements tile $\T_\L$, i.e.\ $\T_\L = \bigcup_{\l_i,n}\calP^n_{\l_i \to \L}$. We write $\C_{\l_i}$ for the level-$\l_i$ patches and $\C_{>\l_i}$, $\C_{\geq\l_i}$, $\C_{<\l_i}$, $\C_{\leq\l_i}$ for the corresponding level subsets.}

\robert{
\begin{definition}[Subdivision Partition]
    \label{def:subdiv_partition}
    A covering $\C(\T_\L)$ is a \emph{subdivision partition} if every patch satisfies a minimum-size condition: every face of every patch, after one step of refinement, must contain at least one vertex whose entire face-ring lies strictly in the interior of the refined patch (i.e.\ not on any interface with a neighbouring patch). This rules out interior linear dependence of basis functions; see~\cite{Piel.2026} for the precise statement.
\end{definition}
}

\robert{The following definition formalises the interface DoF construction motivated in the previous section and illustrated in Fig.~\ref{fig:active_dofs}.}

\begin{definition}[Active $k$-form degrees of freedom]
    \label{def:active_k_form_dofs}
    Let $\C\big(\T_{L}\big) = \bigcup_{\l_i = \l_0}^L \; \bigcup_{n = 0}^{N_{\l_i}} \; \calP^n_{\l_i}$ be a subdivision partition of $\T_L$. The active $k$-form degrees of freedom $\D^k\big(\C\big)$ are defined as
    \begin{equation}
        \label{eq:active_k_form_dofs}
        \D^k \big(\C\big) \coloneqq \underbrace{\bigcup_{\calP^n_{\l_i} \in \C}}_{(i)} \Bigg[ \underbrace{\K^k \big( \calP^n_{\l_i} \big)}_{(ii)} \cup \underbrace{\bigcup_{\calP^m_{\l_j} \in \C_{> \l_i}}}_{(iii)} \; \underbrace{\bigcup_{\l_t = \l_i +1}^{\l_j}}_{(iv)} \bigg[ \underbrace{\K^k\V_{\l_t} \Big( \V_{\l_t} \big( \I_{(\calP^n_{\l_i \to \l_t}, \calP^m_{\l_j})} \big) \Big)}_{(v)} \cap \underbrace{\K^k_{\l_t} \big( \calP^n_{\l_i \to \l_t} \big)}_{(vi)} \bigg] \Bigg].
    \end{equation}
\end{definition}

\robert{Unpacking this definition: for every patch (\textit{i}) $\calP^n_{\l_i}$, we first add all of its $k$-simplices (\textit{ii}). Further, for all relatively finer patches (\textit{iii}) $\calP^m_{\l_j}$ with $\l_j > \l_i$ and all intermediate levels (\textit{iv}) $\l_t$ with $\l_i < \l_t \leq \l_j$, all interface simplices (\textit{v}) are added if they lie on the coarser side (\textit{vi}) of the interface, i.e.\ can be generated by refining patch $\calP^n_{\l_i}$ to level $\l_t$.}

For future computations, it is helpful to group the active degrees of freedom by level $\l_t$ rather than by the individual patches $\calP^n_{\l_i}$. The following lemma re-formulates the active degrees of freedom in exactly that way.

\begin{lemma}
    \label{lemma:active_k_form_dofs_per_level}
    The active degrees of freedom $\D^k$ induced by the subdivision partition $\C$ can equivalently be described by
    \begin{equation}
        \D^k \big( \C \big) = \bigcup_{\l_t = \l}^L \D^k_{\l_t} \big( \C \big)
    \end{equation}
    with
    \begin{equation}
        \D^k_{\l} \big( \C \big) = \K^k_{\l} \big(\C_{\l}\big)
    \end{equation}
    for $\l_t = \l$ and
    \begin{equation}
        \label{eq:active_k_form_dofs_per_level}
        \D^k_{\l_t} \big( \C \big) = \K^k_{\l_t} \big(\C_{\l_t}\big) \cup \bigcup_{\calP^n_{\l_i} \in \C_{<\l_t}} \; \bigcup_{\calP^m_{\l_j} \in \C_{\geq \l_t}} \; \bigg[ \K^k\V_{\l_t} \Big( \V_{\l_t} \big(\I_{(\calP^n_{\l_i \to \l_t}, \calP^m_{\l_j})} \big) \Big) \cap \K^k_{\l_t} \big(\calP^n_{\l_i \to \l_t}\big) \bigg].
    \end{equation}
    for $\l_t = \l \!+\!1, \hdots, L$.
\end{lemma}

\begin{definition}[Adaptive subdivision differential form spaces]
    \label{def:adaptive_function_spaces}
    Fix an initial mesh $\T_{\l}$ and maximum subdivision level $L$. Let the subdivision partition $\C\big(\T_L\big)$ induce the active $k$-form degrees of freedom $\D^k \big( \C \big)$. The \textbf{active basis functions} of the space $\hierSDFspace{k}{\C}{\L}$ are given by
    \begin{equation}
        \Phi_{\D^k (\C)} \coloneqq \bigcup_{\sigma^k_{\l_i, p} \in \D^k} \Big\{ \phi^k_{\l_i \to L, p} \Big\}
    \end{equation}
    and thus
    \begin{equation}
        \hierSDFspace{k}{\C}{\L} \coloneqq \mathrm{span}\big( \Phi_{\D^k (\C)} \big).
    \end{equation}
\end{definition}

\noindent Note that the basis functions are increasingly dense for higher levels $\l_i$ because the resolution of the patches $\calP_{\l_i}$ and thus $\D^k_{\l_i}$ scales with roughly $4^{\l_i - \l}$. Two essential properties must be established for the spaces $\hierSDFspace{k}{\C}{\L}$: linear independence of the basis functions and the de Rham complex. The proofs of both properties are given in~\cite{Piel.2026}; the main results are restated below.

\begin{remark}
    The numerical examples in Section~5 use subdivision partitions where only adjacent levels share an interface. In that case, the active DoF formula~\eqref{eq:active_k_form_dofs} simplifies: only a single layer of interface simplices needs to be added at the finer of the two adjacent levels.
\end{remark}

Finally, we are going to re-state the two main theorems of \cite{Piel.2026} regarding linear independence and the exactness of the complex.

\begin{theorem}
    Let $\C$ be a subdivision partition of the mesh hierarchy $\T_\l$ for $\l \in \{ \lzero, \hdots, \L\}$. The basis functions of the hierarchical spaces $\hierSDFspace{k}{\C}{\L}$ from Definition~\ref{def:adaptive_function_spaces} are linearly independent.
\end{theorem}

\begin{proof}
    The necessary properties of a subdivision partition were chosen so that linear independence was guaranteed, see \cite{Piel.2026} for the entire proof.
\end{proof}

\begin{theorem}
    Let $\C$ be an subdivision partition. The hierarchical spaces $\hierSDFspace{k}{\C}{\L}$ constitute the differential complex
    \begin{equation}
    \begin{tikzcd}
        \mathfrak{R}_\C: \qquad 0 \arrow[r] & \mathbb{R} \arrow[r, "\subset"] & \hierSDFspace{0}{\C}{\L} \arrow[r, "\extd"] & \hierSDFspace{1}{\C}{\L} \arrow[r, "\extd"] & \hierSDFspace{2}{\C}{\L} \arrow[r] & 0.
    \end{tikzcd}
    \end{equation}
    The hierarchical subdivision $k$-form spaces with vanishing trace form the differential complex
    \begin{equation}
    \begin{tikzcd}
        \mathring{\mathfrak{R}}_\C: \qquad 0 \arrow[r] & \hierSDFspace{0}{\C}{\L} \arrow[r, "\extd"] & \hierSDFspace{1}{\C}{\L} \arrow[r, "\extd"] & \hierSDFspace{2}{\C}{\L} \arrow[r, "\int"] & \mathbb{R} \arrow[r] & 0.
    \end{tikzcd}
    \end{equation}
    If $\C$ is an \emph{exact} subdivision partition of a \emph{simply-connected} domain, the complexes $\mathfrak{R}_\C$ and $\mathring{\mathfrak{R}}_\C$ are exact.
\end{theorem}

\begin{proof}
    See \cite{Piel.2026}.
\end{proof}


\subsection{Implementation}

\robert{The implementation of the active subdivision space proceeds in two stages.
Given a subdivision partition $P$, we first enumerate the active degrees of
freedom $\D^k[\l]$ for each form degree $k \in \{0,1,2\}$ and level $\l$
using Algorithm~\ref{alg:buildactivedofs}. We then assemble, for each form
degree $k$, the sparse matrix $\Amat{k}{\activeDoFs}{\L}$ whose columns are
the level-$\L$ coefficient expansions of the active basis functions
(Algorithm~\ref{alg:buildsubdivmatrix}). Galerkin projection of all
bilinear forms to the active space then reduces to standard sparse
matrix--matrix products, without any re-assembly of integrals.

The active DoFs split into two contributions at each level $\l$.
\emph{Native} DoFs are the $k$-simplices of every active face $f \in
P[\l]$. \emph{Interface} DoFs are additional basis functions at the
boundary between a level-$\l$ active region and a refined region on level
$\l + 1$: these correspond to subdivision basis functions whose support
straddles the level boundary and are not captured by the native-DoF
construction alone. They are produced by a frontier sweep
(steps~(a)--(d) of Algorithm~\ref{alg:buildactivedofs}) that maintains the
set of interior edges separating resolved (coarse-side) faces from
unresolved (finer) regions. Each level the frontier is split into child
edges and interface vertices; the vertices emit the DoFs they introduce on
the following level. The ordering of operations within each level is fixed:
old frontier edges are consumed first, new ones are spawned, DoFs are
emitted from the previous level's vertices, and finally the frontier is
split for the next level.}

\robert{\begin{algorithm}[H]
\caption{$\textsc{BuildActiveDoFs}(P)$: active degrees of freedom from a subdivision partition}
\label{alg:buildactivedofs}
\tcp{1.\ Native DoFs}
$\D^0[\l] \leftarrow \emptyset$,\; $\D^1[\l] \leftarrow \emptyset$ for all $\l$;\quad $\D^2 \leftarrow P$\;
\For{$\l = 0, \ldots, \L$}{
    \For{each $f \in P[\l]$}{
        $\D^0[\l] \leftarrow \D^0[\l] \cup \mathrm{verts}(f)$;\quad $\D^1[\l] \leftarrow \D^1[\l] \cup \mathrm{edges}(f)$\;
    }
}
\tcp{2.\ Interface DoFs (frontier sweep)}
$\mathrm{frontier} \leftarrow \emptyset$;\quad $\mathrm{pendingVerts} \leftarrow \emptyset$\;
\For{$\l = 0, \ldots, \L$}{
    \tcp{(a) consume frontier edges whose fine side is now resolved at level $\l$}
    $\mathrm{frontier} \leftarrow \{ e \in \mathrm{frontier} : \text{fine-side faces of } e \notin P[\l] \}$\;
    \tcp{(b) spawn new frontier edges: interior edges of $\mathcal{F}_\l$ with exactly one adjacent face in $P[\l]$}
    $\mathrm{frontier} \leftarrow \mathrm{frontier} \cup \textsc{FindInterfaceEdges}(\l, \D)$\;
    \tcp{(c) emit DoFs from interface vertices spawned at level $\l - 1$}
    \For{each $v \in \mathrm{pendingVerts}$}{
        $\D^0[\l] \leftarrow \D^0[\l] \cup \{v\}$;\quad
        $\D^1[\l] \leftarrow \D^1[\l] \cup \mathrm{activeEdges}(v)$;\quad
        $\D^2[\l] \leftarrow \D^2[\l] \cup \mathrm{activeFaces}(v)$\;
    }
    $\mathrm{pendingVerts} \leftarrow \emptyset$\;
    \tcp{(d) split frontier edges into child edges and interface vertices for level $\l + 1$}
    \If{$\l < \L$}{
        $\mathrm{newFrontier} \leftarrow \emptyset$;\quad $\mathrm{newVerts} \leftarrow \emptyset$\;
        \For{each $e \in \mathrm{frontier}$}{
            $(e_1, e_2, v_{\mathrm{mid}}) \leftarrow \textsc{SplitEdge}(e)$ at level $\l + 1$\;
            $\mathrm{newFrontier} \leftarrow \mathrm{newFrontier} \cup \{e_1, e_2\}$;\quad
            $\mathrm{newVerts} \leftarrow \mathrm{newVerts} \cup \{v_{\mathrm{mid}}\}$\;
        }
        \For{each endpoint $v$ of edges in $\mathrm{frontier}$, taken once per $v$}{
            \lIf{$v$ borders a refined region}{$\mathrm{newVerts} \leftarrow \mathrm{newVerts} \cup \{v\}$}
        }
        $\mathrm{frontier} \leftarrow \mathrm{newFrontier}$;\quad
        $\mathrm{pendingVerts} \leftarrow \mathrm{newVerts}$\;
    }
}
\Return $\D$\;
\end{algorithm}}

\robert{\todo{Flag: The interface DoFs emitted in step~(c) include 2-form (face) contributions from the fine side of the level boundary. Native 2-form DoFs on the coarser side are already added in step~1; the interface emission adds child faces of the coarser active fan that meet the interface. Please confirm this is consistent with Definition~\ref{def:active_k_form_dofs} and the intended interpretation in the paper.}}

\robert{Once the active DoFs $\D$ are known, the active subdivision matrix
$\Amat{k}{\activeDoFs}{\L}$ is assembled column by column. Let
$\Smat{k}{\l}{\L}$ denote the accumulated subdivision matrix for $k$-forms
from level $\l$ to level $\L$, i.e.\ the product of per-level operators
$\Smat{k}{\l}{\l+1}, \ldots, \Smat{k}{\L-1}{\L}$. For an active simplex
$s \in \D^k[\l]$, the level-$\L$ expansion of the corresponding basis
function is column $s$ of $\Smat{k}{\l}{\L}$. The columns of
$\Amat{k}{\activeDoFs}{\L}$ are ordered level by level (all level-$0$
active DoFs first, then level $1$, etc.); this ordering is exploited by the
preconditioner described in Section~\ref{sec:preconditioner}.}

\robert{\begin{algorithm}[H]
\caption{$\textsc{BuildSubdivMatrix}(\D, k)$: active subdivision matrix}
\label{alg:buildsubdivmatrix}
\KwIn{active DoFs $\D$, form degree $k \in \{0,1\}$}
\KwOut{matrix $\Amat{k}{\activeDoFs}{\L} \in \mathbb{R}^{K_\L \times K_\D}$, where $K_\L = |\mathcal{K}^k_\L|$ and $K_\D = \sum_\l |\D^k[\l]|$}
$\Amat{k}{\activeDoFs}{\L} \leftarrow (K_\L \times K_\D)$ sparse zero matrix;\quad $\mathrm{col} \leftarrow 0$\;
\For{$\l = 0, \ldots, \L$}{
    $\mathbf{S} \leftarrow \Smat{k}{\l}{\L}$ \tcp{accumulated subdivision matrix from level $\l$ to $\L$}
    \For{each simplex $s \in \D^k[\l]$}{
        $\Amat{k}{\activeDoFs}{\L}[\,:\,,\mathrm{col}] \leftarrow \mathbf{S}[\,:\,,s]$;\quad $\mathrm{col} \leftarrow \mathrm{col} + 1$\;
    }
}
\Return $\Amat{k}{\activeDoFs}{\L}$\;
\end{algorithm}}

\robert{Given $\Amat{0}{\activeDoFs}{\L}$ and $\Amat{1}{\activeDoFs}{\L}$, the
active-space bilinear forms are obtained by Galerkin restriction of the
embedding-level operators, assembled once before the adaptive loop:
\begin{equation}
    \mathbf{M}_0 = (\Amat{0}{\activeDoFs}{\L})^\top \mathbf{M}\, \Amat{0}{\activeDoFs}{\L},
    \quad
    \mathbf{C}_0 = (\Amat{1}{\activeDoFs}{\L})^\top \mathbf{C}\, \Amat{1}{\activeDoFs}{\L},
    \quad
    \mathbf{B}_0 = (\Amat{0}{\activeDoFs}{\L})^\top \mathbf{B}\, \Amat{1}{\activeDoFs}{\L},
\end{equation}
where $\mathbf{M}$, $\mathbf{C}$, $\mathbf{B}$ are the mass, curl--curl,
and gradient-coupling matrices on $\T_\L$. The resulting block system is
\begin{equation}
    K =
    \begin{pmatrix}
        -\mathbf{M}_0 & \mathbf{B}_0 \\
        \mathbf{B}_0^\top & \mathbf{C}_0
    \end{pmatrix},
\end{equation}
which is symmetric indefinite.}

\FloatBarrier

\section{The Adaptive Loop}

This section describes the adaptive loop used in the numerical experiments. The loop consists of the steps
\begin{equation}
\begin{tikzcd}
    \mathrm{SOLVE} \arrow[r] & \mathrm{ESTIMATE} \arrow[r] & \mathrm{MARK} \arrow[r] & \mathrm{PAD} \arrow[r] & \mathrm{REFINE} \arrow[llll, rounded corners=0pt, to path={(\tikztostart.south) -- ([yshift=-1.em]\tikztostart.south) -- ([yshift=-1.em]\tikztotarget.south) -- (\tikztotarget.south)}]
\end{tikzcd}
\end{equation}

\robert{Algorithm~\ref{alg:adaptive_loop} gives the complete outer loop in pseudocode. Note that $\textsc{FixIsolatedBoundaryVertices}$ is called after $\textsc{DoerflerMarking}$ (which internally calls $\textsc{PadMarkedFaces}$); together they constitute the PAD step of the diagram.}

\robert{\begin{algorithm}[H]
\caption{Adaptive loop}
\label{alg:adaptive_loop}
\KwIn{initial partition $P$, maximum steps $S$, Dörfler parameter $\theta$}
\For{$\mathrm{step} = 0, \ldots, S - 1$}{
    $\D \leftarrow \textsc{BuildActiveDoFs}(P)$ \tcp{Alg.~\ref{alg:buildactivedofs}}
    $(\Amat{0}{\activeDoFs}{\L}, \Amat{1}{\activeDoFs}{\L}) \leftarrow \textsc{BuildSubdivMatrices}(\D)$ \tcp{Alg.~\ref{alg:buildsubdivmatrix}}
    $(\mathbf{M}_0, \mathbf{C}_0, \mathbf{B}_0) \leftarrow \bigl((\Amat{0}{\activeDoFs}{\L})^\top \mathbf{M} \Amat{0}{\activeDoFs}{\L},\; (\Amat{1}{\activeDoFs}{\L})^\top \mathbf{C} \Amat{1}{\activeDoFs}{\L},\; (\Amat{0}{\activeDoFs}{\L})^\top \mathbf{B} \Amat{1}{\activeDoFs}{\L}\bigr)$\;
    $(\sigma_h, u_h) \leftarrow \mathrm{MINRES}$ solve of $K(\sigma, u) = (0,\; (\Amat{1}{\activeDoFs}{\L})^\top f)$ with preconditioner $\mathbf{P}$ \tcp{Section~\ref{sec:preconditioner}}
    $\eta \leftarrow \textsc{AggregateEstimators}(\{\eta^2_T\}_{T \in \T_\L},\, P)$ \tcp{Alg.~\ref{alg:aggregate}}
    $P \leftarrow \textsc{DoerflerMarking}(P, \eta, \theta)$ \tcp{Alg.~\ref{alg:doerfler}}
    $P \leftarrow \textsc{FixIsolatedBoundaryVertices}(P)$ \tcp{Alg.~\ref{alg:fix_boundary}}
}
\end{algorithm}}

\paragraph{SOLVE.}
We solve the discrete system on the hierarchical space $\hierSDFspace{k}{\C}{\L}$ using a standard iterative solver.

\paragraph{ESTIMATE.}
We compute error indicators $\eta_{\l,a}$ per face $\face{\l}{a} \in \C$ of the subdivision partition. The residual is evaluated on the finest level $\L$ and then aggregated to the coarse parent faces:
\begin{equation}
    \eta_{\l,a} = \int_{\Aop{}{\l}{\L}(\face{\l}{a})} \big\| \iota (f_h) - f \big\|^2,
\end{equation}
where $f_h$ is obtained by applying the discrete PDE operator to $u_h$. Note that the interface DoFs are excluded from the indicator computation.

\begin{example}[Vector Laplace estimator]
For the mixed vector Laplace problem with $(\sigma_h, u_h) \in \hierSDFspace{0}{\C}{\L} \times \hierSDFspace{1}{\C}{\L}$, the basis functions are piecewise linear approximations of the subdivision limit functions. Hence $\extd u_h$ is piecewise constant and $\boldsymbol{\delta}\extd u_h$ vanishes elementwise, while its contribution appears in the curl jumps across edges. The indicator takes the form
\begin{equation}
    \eta_{\l,a}^2 = \sum_{T \,\subset\, \Aop{}{\l}{\L}(\face{\l}{a})} \!\! \left( h_T^2 \|f - \extd \sigma_h\|_{L^2(T)}^2 + \|\sigma_h + \boldsymbol{\delta} u_h\|_{L^2(T)}^2 \right) + \sum_{e \,\subset\, \Aop{}{\l}{\L}(\face{\l}{a})} \!\! h_e \big\|[\![\mathrm{curl}(u_h)]\!]\big\|_{L^2(e)}^2.
\end{equation}
The first sum contains the elementwise residuals of both equations, and the second sum contains the curl jump across interior edges $e$ of the fine mesh $\T_\L$.
\end{example}

\robert{The element indicators $\eta^2_T$ are evaluated on every triangle $T$ of the embedding mesh $\T_\L$ and then aggregated onto the active partition by walking each fine face up the quad-tree to its ancestor in $P$. The resulting per-patch value $\eta_{\l,a}$ is the square root of the summed fine-mesh indicators over all level-$\L$ descendants of $\face{\l}{a}$.}

\robert{\begin{algorithm}[H]
\caption{$\textsc{AggregateEstimators}(\{\eta^2_T\}_{T \in \T_\L}, P)$: aggregate fine-mesh indicators onto the active partition}
\label{alg:aggregate}
\KwIn{per-element indicators $\eta^2_T$ for all $T \in \T_\L$, partition $P$}
\KwOut{per-patch estimators $\eta[\l][f]$ for $f \in P[\l]$}
$\zeta[\l][f] \leftarrow 0$ for all $\l$, $f \in P[\l]$\;
\For{each $T \in \T_\L$}{
    $g \leftarrow T$\;
    \For{$\l = \L, \L-1, \ldots, 0$}{
        \If{$g \in P[\l]$}{
            $\zeta[\l][g] \leftarrow \zeta[\l][g] + \eta^2_T$;\quad \textbf{break}\;
        }
        $g \leftarrow \lfloor g/4 \rfloor$ \tcp{parent face (SEC convention: children of $f$ are $\{4f, \ldots, 4f+3\}$)}
    }
}
\Return $\eta[\l][f] = \sqrt{\zeta[\l][f]}$ for all active $(\l, f)$\;
\end{algorithm}}

\paragraph{MARK.}
We apply standard Doerfler marking to the indicators $\eta_{\l,a}$, selecting the smallest set of faces whose indicators sum to a fixed fraction of the total. Faces on level $\L$ are not eligible for marking\robert{: they contribute to the reported global error but cannot be refined further within the present hierarchy, so they are excluded from the threshold total. This means the effective fraction of refineable error selected is at least $\theta$.}

\robert{\begin{algorithm}[H]
\caption{$\textsc{DoerflerMarking}(P, \eta, \theta)$: Dörfler marking on a subdivision partition}
\label{alg:doerfler}
\KwIn{partition $P$, estimators $\eta[\l][f]$, parameter $\theta \in (0,1]$}
\KwOut{refined partition $P_{\mathrm{new}}$}
$\mathrm{total} \leftarrow \sum_{\l < \L,\; f \in P[\l]} \eta[\l][f]^2$ \tcp{level-$\L$ faces excluded}
\lIf{$\mathrm{total} = 0$}{\Return $P$}
Sort all $(\l, f)$ with $f \in P[\l]$, $\l < \L$, by descending $\eta[\l][f]^2$\;
$\mathrm{accum} \leftarrow 0$;\quad $\mathrm{marked}[\l] \leftarrow \emptyset$ for all $\l$\;
\For{each $(\l, f)$ in sorted order while $\mathrm{accum} < \theta \cdot \mathrm{total}$}{
    $\mathrm{marked}[\l] \leftarrow \mathrm{marked}[\l] \cup \{f\}$;\quad $\mathrm{accum} \leftarrow \mathrm{accum} + \eta[\l][f]^2$\;
}
$\textsc{PadMarkedFaces}(\mathrm{marked}, P)$ \tcp{Alg.~\ref{alg:padding}}
$P_{\mathrm{new}} \leftarrow P$\;
\For{each level $\l$ and each $f \in \mathrm{marked}[\l]$}{
    Remove $f$ from $P_{\mathrm{new}}[\l]$; insert $\{4f, 4f+1, 4f+2, 4f+3\}$ into $P_{\mathrm{new}}[\l+1]$\;
}
\Return $P_{\mathrm{new}}$\;
\end{algorithm}}

\paragraph{PAD.}
\robert{The padding step is non-standard and specific to our setting. After marking, the selected faces may form a covering that violates the subdivision-partition admissibility condition of Definition~\ref{def:active_k_form_dofs}. Algorithm~\ref{alg:padding} iteratively enlarges the marked set until every face in every level-$\l$ patch has at least one \emph{interior vertex} — a vertex all of whose adjacent faces lie in the patch (condition~(I) of Definition~\ref{def:active_k_form_dofs}). For an offending face $f$, we choose the vertex $v$ with the fewest missing fan faces and mark the currently-active ancestors of those missing faces (walking up the quad-tree via $g \leftarrow \lfloor g/4 \rfloor$).}

\robert{\begin{algorithm}[H]
\caption{$\textsc{PadMarkedFaces}(\mathrm{marked}, P)$: admissibility padding}
\label{alg:padding}
\KwIn{marked faces $\mathrm{marked}[\l]$ per level (in/out), partition $P$}
\Repeat{$\mathrm{newMarks} = \emptyset$}{
    Build tentative partition $\mathrm{tent}$ by applying $\mathrm{marked}$ to $P$\;
    $\mathrm{newMarks}[\l] \leftarrow \emptyset$ for all $\l$\;
    \For{$\l = 0, \ldots, \L$ with $\mathrm{tent}[\l] \neq \emptyset$}{
        $I_v \leftarrow \{ v : \forall\, g \in \mathrm{vert\_faces}(v),\; g \in \mathrm{tent}[\l] \}$ \tcp{interior vertices of $\mathrm{tent}[\l]$}
        \For{each $f \in \mathrm{tent}[\l]$ with no vertex in $I_v$}{
            $v \leftarrow \arg\min_{v' \in \mathrm{verts}(f)} \#\{ g \in \mathrm{vert\_faces}(v') : g \notin \mathrm{tent}[\l] \}$\;
            \For{each $g \in \mathrm{vert\_faces}(v)$ with $g \notin \mathrm{tent}[\l]$}{
                Walk $g \leftarrow \lfloor g/4 \rfloor$ until $g \in P$; add $g$ to $\mathrm{newMarks}$\;
            }
        }
    }
    $\mathrm{marked} \leftarrow \mathrm{marked} \cup \mathrm{newMarks}$\;
}
\end{algorithm}}

\robert{\todo{Flag: Algorithm~\ref{alg:padding} enforces only condition~(I) of Definition~\ref{def:active_k_form_dofs} (interior-vertex requirement). Condition~(II) (no isolated boundary vertex) is handled separately by Algorithm~\ref{alg:fix_boundary} rather than here. If Definition~\ref{def:active_k_form_dofs} states both conditions, please confirm the current split is correctly described.}}
\robert{This ensures that after refinement the updated subdivision partition satisfies condition~(I) and the active basis is locally complete.

A second, topological repair step is needed at domain-boundary vertices and has no analogue in standard AFEM. After padding, a boundary vertex $v$ may be \emph{isolated}: both faces incident to $v$ along $\partial\Omega$ remain coarse while some interior fan face has been refined. This creates a curl-free 1-form supported near the boundary that is not the gradient of any active 0-form, breaking exactness of the discrete de~Rham sequence $0 \to \hierSDFspace{0}{\C}{\L} \xrightarrow{\nabla} \hierSDFspace{1}{\C}{\L} \xrightarrow{\mathrm{curl}} \hierSDFspace{2}{\C}{\L} \to 0$ at the 1-form level. Algorithm~\ref{alg:fix_boundary} repairs this by walking the fan of faces around $v$ between its two boundary-incident faces $bF_1, bF_2$ and refining the shorter side, so the refined region reaches the domain boundary on at least one side.}

\robert{\begin{algorithm}[H]
\caption{$\textsc{FixIsolatedBoundaryVertices}(P)$: restore de~Rham exactness at boundary vertices}
\label{alg:fix_boundary}
\For{$\l = 0, \ldots, \L - 1$}{
    \For{each $f \in P[\l]$}{
        \For{each boundary vertex $v$ of $f$}{
            $(bF_1, bF_2) \leftarrow$ the two faces incident to $v$ on $\partial\Omega$\;
            \lIf{$bF_1 \notin P[\l]$ or $bF_2 \notin P[\l]$}{\textbf{continue} \tcp*[f]{already fixed}}
            \lIf{every $g \in \mathrm{vert\_faces}(v)$ lies in $P[\l]$}{\textbf{continue} \tcp*[f]{not isolated}}
            $\mathrm{fan} \leftarrow \textsc{TraverseFan}(v, bF_1, bF_2)$ \tcp{ordered faces from $bF_1$ to $bF_2$}
            $d_L \leftarrow$ index of first refined face from $bF_1$ end\;
            $d_R \leftarrow$ index of first refined face from $bF_2$ end\;
            \eIf{$d_L \leq d_R$}{
                Refine $\mathrm{fan}[0], \ldots, \mathrm{fan}[d_L - 1]$ in $P$\;
            }{
                Refine $\mathrm{fan}[|\mathrm{fan}| - d_R], \ldots, \mathrm{fan}[|\mathrm{fan}| - 1]$ in $P$\;
            }
        }
    }
}
\end{algorithm}}

\robert{\todo{Flag: The motivation (broken de~Rham exactness at isolated boundary vertices) follows the comment in the source code. The analytical claim — that this geometric fix is necessary and sufficient to restore exactness — has not been separately verified. Please confirm the framing before publication.}}

\paragraph{REFINE.}
Each marked and padded face is refined by one level via quadrisection.

\robert{\subsection{Preconditioner}
\label{sec:preconditioner}

The block system $K$ is symmetric indefinite and is solved with MINRES.
We use a block-diagonal SPD preconditioner
\begin{equation}
    \mathbf{P} =
    \begin{pmatrix}
        \mathbf{M}_0 & 0 \\
        0 & \mathbf{S}
    \end{pmatrix},
\end{equation}
where $\mathbf{S}$ approximates the Schur complement of the $(1,1)$ block
by replacing $\mathbf{M}_0^{-1}$ with a row-sum lumped inverse:
\begin{equation}
    \mathbf{S} = \mathbf{C}_0 + \mathbf{B}_0^\top\, \mathrm{diag}(\mathbf{M}_0 \mathbf{1})^{-1}\, \mathbf{B}_0,
\end{equation}
where $\mathbf{1}$ is the all-ones vector of length $\dim \D^0$. Row-sum
lumping is preferred over bare diagonal lumping because, for piecewise-linear
elements on triangles, the row sums of the mass matrix bound $\mathbf{M}_0$
from above with a mesh-independent constant, giving a more stable Schur
approximation on the irregular meshes produced by adaptive refinement. Both
blocks of $\mathbf{P}$ are factorised once per adaptive step by sparse
Cholesky (LDLT) and the factorisation is discarded afterwards, since the
active spaces are non-nested between steps. The cost per MINRES iteration is
one triangular back-solve per block.}

\FloatBarrier

\section{Numerical Experiment}

\subsection{Maxwell Eigenvalue problem}

The Maxwell eigenvalue problem is a popular test case for structure-preserving discretisations. The weak form of the continuous Maxwell eigenvalue problem is given by: Find $\lambda \in \mathbb{R}$ and $u \in \HocurlM{\M}$ such that
\begin{equation}
  (\extd u, \extd v)_\M = \lambda (u, v)_\M \qquad \forall v \in \HocurlM{\M}.  
\end{equation}
Applying Loop subdivision alters the domain of the eigenvalue problem slightly. Thus, we cannot directly compare the obtained spectrum with the analytical solution. However, discretisations using the FEEC spaces $\FEECspaceZero{k}{}$ are known to converge to the spectrum of the analytical solution on the altered domain without introducing spurious modes (cf. \cite{Arnold.FEEC}). For this reason, we compare the spectrum obtained using the hierarchical subdivision $k$-form spaces with the finest FEEC spaces $\FEECspaceZero{1}{\L}$.

Figures~\ref{fig:Maxwell_exact_DoFmesh} and \ref{fig:Maxwell_inexact_DoFmesh} present an exact and an inexact subdivision partition, respectively. The eigenvalue spectrum obtained using the subdivision $k$-form spaces defined on both subdivision partitions is visualised in Figure~\ref{fig:Maxwell_eigenvalues}. The exact subdivision partition coincides with the FEEC spectrum. This suggests that the underlying sequence of approximation spaces is exact. On the other hand, the spectrum of the inexact subdivision partition exhibits spurious modes compared to the FEEC discretisation.

\robert{\todo{I was only able to find an additional 0 EV for the inexact partition. That can happen if the spurious 1-form has 0 curl but it is inconsistent with \cite{Cabanas.2026}. I want to investigate that in more detail.}}

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[b]{0.23\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/maxwell/exact_subdiv_partition.png}
        \caption{Exact subdivision partition.}
        \label{fig:Maxwell_exact_DoFmesh}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.23\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/maxwell/inexact_subdiv_partition.png}
        \caption{Inexact subdivision partition.}
        \label{fig:Maxwell_inexact_DoFmesh}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.5\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/maxwell/maxwell_spectra.pdf}
        \caption{Eigenvalue spectra.}
        \label{fig:Maxwell_eigenvalues}
    \end{subfigure}
    \caption{Maxwell eigenvalue problem: DoF meshes for the exact (left) and inexact (centre) subdivision $k$-form complexes, and the corresponding eigenvalue spectra compared to the finest FEEC discretisation $\FEECspaceZero{1}{\L}$ (right).}
    \label{fig:maxwell_overview}
\end{figure}

\robert{\paragraph{Bonus: Instabilities without intermediate interface DoFs.} Finally, we want to see what happens if we do not add the intermediate interface DoFs. We should probably look at two eigenmodes (with and without the DoFs) and compare them. I expect to see similar instabilities to the ones that arise from boundary truncation.}

\FloatBarrier

\subsection{Vector Laplace problem}

The $1$-form Laplace problem is given by
\begin{equation}
  (\extd \boldsymbol{\delta} + \boldsymbol{\delta} \extd) u = f
\end{equation}
on a domain $\M$. Since the operator $\boldsymbol{\delta}$ is not well-defined for a (subdivision) $1$-form, we introduce the auxiliary variable $\boldsymbol{\delta} \sigma = u$ and use the FEEC-typical mixed weak formulation
\begin{align}
  (\sigma, \tau)_\M - (u, \extd \tau)_\M &= 0 \qquad \forall \tau \in H^1(\M), \\
  (\extd \sigma, v)_\M + (\extd u, \extd v)_\M &= (f, v)_\M \qquad \forall v \in \HocurlM{\M}.
\end{align}

\robert{\todo{explain assembly of matrices maybe? i.e., unrefinement}}

This section focuses on two test cases that we will refer to as \textit{circular layer} and \textit{multiscale Gaussians}. The circular layer test case is adopted from \cite{Cabanas.2026} and its analytical solution is given by
\begin{equation}
    \label{eq:circular_layer_test_case}
    \Vec{u}_{\mathrm{CL}}(x) = \tanh \big(\alpha (\vvert{\vvert{x}}^2 - R^2) \big) \; \Vec{b}(x) \quad \text{with} \quad \Vec{b}(x) = \begin{bmatrix}
        \sin(\pi x_1) \\
        \sin(\pi x_2)
    \end{bmatrix}
\end{equation}
and $\alpha = 100$ and $R = 0.3$. The vector part $\Vec{b}(x)$ provides a background field while the $\tanh$ term adds a sharp, radially symmetric transition between $+\Vec{b}$ and $-\Vec{b}$. The right-hand side $f_{\mathrm{CL}}$ corresponding to $\Vec{u}_{\mathrm{CL}}$ can be computed using the (strong) vector laplace equation. The panels of Figure~\ref{fig:circular_layer_adaptive_solution} show the evolution of the solution $\Vec{u}^n_{\mathrm{CL}}$ and the subdivision partition $\C^n_{\mathrm{CL}}$ after $n$ steps of the adaptive loop.

\begin{figure}[htbp]
    \centering
    % Top row: 4 smaller panels, all trimmed on the right
    \begin{subfigure}[b]{0.24\textwidth}
        \centering
        \includegraphics[width=\textwidth, trim=0pt 0pt 4cm 0pt, clip]{images/vector_laplacian/CircularLayer/u0.png}
        \caption{Step $0$.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.24\textwidth}
        \centering
        \includegraphics[width=\textwidth, trim=0pt 0pt 4cm 0pt, clip]{images/vector_laplacian/CircularLayer/u1.png}
        \caption{Step $1$.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.24\textwidth}
        \centering
        \includegraphics[width=\textwidth, trim=0pt 0pt 4cm 0pt, clip]{images/vector_laplacian/CircularLayer/u2.png}
        \caption{Step $2$.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.24\textwidth}
        \centering
        \includegraphics[width=\textwidth, trim=0pt 0pt 4cm 0pt, clip]{images/vector_laplacian/CircularLayer/u3.png}
        \caption{Step $3$.}
    \end{subfigure}
    \\[8pt]
    % Measure u4 with trim to obtain the row height for matching u6
    \savebox{\twosubbox}{%
        \includegraphics[width=0.25\textwidth, trim=0pt 0pt 4cm 0pt, clip]{images/vector_laplacian/CircularLayer/u4.png}%
    }%
    \setlength{\twosubht}{\ht\twosubbox}%
    % Bottom row: 3 larger panels; u4 and u5 trimmed, u6 scaled to same height
    \begin{subfigure}[b]{0.25\textwidth}
        \centering
        \usebox{\twosubbox}
        \caption{Step $4$.}
    \end{subfigure}\qquad
    \begin{subfigure}[b]{0.25\textwidth}
        \centering
        \includegraphics[width=\textwidth, trim=0pt 0pt 4cm 0pt, clip]{images/vector_laplacian/CircularLayer/u5.png}
        \caption{Step $5$.}
    \end{subfigure}\qquad
    \begin{subfigure}[b]{0.25\textwidth}
        \centering
        \includegraphics[height=\twosubht, trim=0pt 0pt 0.8cm 0pt, clip]{images/vector_laplacian/CircularLayer/u6.png}
        \caption{Step $6$.}
    \end{subfigure}
    \caption{Solution $\Vec{u}^n_{\mathrm{CL}}$ and subdivision partition $\C^n_{\mathrm{CL}}$ of the circulay layer test case in Eq.~\eqref{eq:circular_layer_test_case} after step $n$. The color bar of step $6$ is applied across all steps.}
    \label{fig:circular_layer_adaptive_solution}
\end{figure}


\begin{figure}[htbp]
    \centering
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/vector_laplacian/MultiscaleGaussian/u_with_subdiv_partition_L_4.png}
        \caption{}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/vector_laplacian/MultiscaleGaussian/fig_dof_breakdown_bars.pdf}
        \caption{}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/vector_laplacian/MultiscaleGaussian/fig_marking.pdf}
        \caption{}
    \end{subfigure}
    \\[6pt]
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/vector_laplacian/MultiscaleGaussian/fig_convergence_Lsweep.pdf}
        \caption{}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/vector_laplacian/MultiscaleGaussian/fig_estimator_levels_L5.pdf}
        \caption{}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.3\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/vector_laplacian/MultiscaleGaussian/fig_estimator_levels_L4.pdf}
        \caption{}
    \end{subfigure}
    \caption{Multiscale Gaussian test case.}
    \label{fig:multiscale_gaussian}
\end{figure}

\section{Conclusion}

\robert{We have presented a complete matrix-level framework for hierarchical subdivision $k$-form spaces and their use in an adaptive loop for structure-preserving PDE discretisations. The central observation is that any active subdivision basis function is a column of an accumulated subdivision matrix $\Smat{k}{\l}{\L}$; selecting columns according to a subdivision partition produces a hierarchical space $\hierSDFspace{k}{\C}{\L}$ whose linear independence and de Rham exactness are guaranteed by the partition condition proved in the companion paper~\cite{Piel.2026}. Once the hierarchical subdivision matrix $\Amat{k}{\activeDoFs}{\L}$ is assembled, all bilinear forms are obtained by Galerkin restriction of the embedding-level operators without recomputing any integrals, and the active-space linear system reduces to standard sparse linear algebra on the finest mesh $\T_\L$.

The adaptive loop adds two subdivision-specific steps to the standard \textsc{Solve}--\textsc{Estimate}--\textsc{Mark}--\textsc{Refine} cycle. The padding step (Algorithm~\ref{alg:padding}) enlarges the marked set to satisfy the interior-vertex condition of the subdivision partition, and the boundary-vertex repair (Algorithm~\ref{alg:fix_boundary}) corrects an isolated topological defect that would otherwise break de Rham exactness by introducing a curl-free 1-form near the boundary that is not the gradient of any active 0-form. Both steps are local and inexpensive relative to the linear solve.

The numerical experiments confirm the two key structural properties of the framework. For the Maxwell eigenvalue problem, exact subdivision partitions reproduce the reference FEEC spectrum without spurious modes; inexact ones introduce them. This serves as a direct diagnostic for the correctness of the active-DoF construction and the de Rham complex structure. For the mixed vector Laplace equation, the adaptive loop concentrates refinement at the location of steep gradients and achieves convergence behaviour consistent with the hierarchical approximation theory.

Several directions remain open. The precise correspondence between the subdivision PAD step and the $L$-chain completion used in hierarchical B-spline AFEM~\cite{Cabanas.2026} has not been established; clarifying this connection would relate the two frameworks and potentially enable convergence proofs to be transferred. The analytical justification of the boundary-vertex repair step (specifically, that the geometric fix of Algorithm~\ref{alg:fix_boundary} is both necessary and sufficient to restore de Rham exactness) also remains to be proved. On the solver side, the block-diagonal LDLT preconditioner used here is effective for the problem sizes considered, but a multigrid preconditioner that respects the globally supported subdivision basis would reduce the per-step cost for larger problems; this requires a prolongation operator compatible with $\Amat{k}{\activeDoFs}{\L}$ and is left for future work. Finally, a promising direction is to adopt the overlapping patch framework of Bressan~\cite{Bressan.2024} in place of the non-overlapping subdivision partition used here. Overlapping patches can provide a richer set of admissible active-DoF configurations and may admit a more flexible PAD step, at the cost of requiring a partition-of-unity treatment to maintain linear independence; adapting the matrix-level assembly and the de Rham complex to that setting is an open problem.}

\bibliography{references}

\end{document}